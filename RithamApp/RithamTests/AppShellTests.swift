import Testing

// Placeholder — replaced by Task 3 of this plan with the real AppShellTests suite covering
// StepRegistry, OnboardingStepPresenting, and OnboardingFlow. Exists only so the RithamTests
// target has a source file to build against in Task 2.
@Suite("AppShellTests placeholder")
struct AppShellTestsPlaceholder {
    @Test("placeholder passes")
    func placeholderPasses() {
        #expect(true)
    }
}
