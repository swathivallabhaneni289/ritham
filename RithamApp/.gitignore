# Build artefacts — the Ritham.xcodeproj itself IS committed (generated by xcodegen, reviewable
# and reproducible from project.yml), so do not add *.xcodeproj here.
xcuserdata/
build/
DerivedData/
.swiftpm/
*.xcuserstate
