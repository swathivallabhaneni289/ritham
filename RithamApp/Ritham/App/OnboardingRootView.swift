import SwiftUI

// Placeholder — replaced by Task 3 of this plan with the single shared navigation container.
// Exists only so Task 2's app target has something to render and can reach BUILD SUCCEEDED
// before the navigation container and step registry are wired up.
struct OnboardingRootView: View {
    var body: some View {
        Text("Ritham")
    }
}
