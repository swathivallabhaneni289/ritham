---
phase: 04-household-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - RithamApp/Ritham/Recommendations/Views/RecommendationsSectionContent.swift
  - RithamApp/Ritham/Recommendations/Views/RecommendationsView.swift
  - RithamApp/Ritham/Settings/DietPlanSectionContent.swift
  - RithamApp/Ritham/Settings/DietPlanView.swift
  - RithamApp/RithamTests/DietSectionIsolationTests.swift
  - RithamApp/Ritham.xcodeproj/project.pbxproj
autonomous: true
requirements: [CROSSGEN-01]

must_haves:
  truths:
    - "The workout-plan content (idle / loading / plan / error) renders from a plain child view that carries no RithamScreen, ScrollView, header or background of its own, so a host screen can show it inline."
    - "The diet-pattern picker and the allergen picker render from plain child views that persist through HealthDataStore only, and never re-derive or re-save a screening result."
    - "A diet-pattern or allergen write leaves every stored condition tag exactly as it was (D-05 / 04-RESEARCH.md Pitfall 3)."
    - "Opening the diet controls on a cold launch shows the values actually stored on device, not an empty in-memory default."
    - "RecommendationsView still conforms to OnboardingStepPresenting and still registers .recommendations (04-RESEARCH.md Pitfall 4)."
    - "The Settings-presented DietPlanView still owns the food-allergy screening question and its severity follow-up."
  artifacts:
    - RithamApp/Ritham/Recommendations/Views/RecommendationsSectionContent.swift
    - RithamApp/Ritham/Settings/DietPlanSectionContent.swift
    - RithamApp/RithamTests/DietSectionIsolationTests.swift
  key_links:
    - "RecommendationsView.body -> RecommendationsSectionContent(model:flow:) — one content implementation, two hosts."
    - "DietPlanView -> DietPatternPicker / AllergenPicker — one persistence implementation, two hosts."
    - "DietPatternPicker/AllergenPicker -> HealthDataStore.updateProfile / .saveFoodAllergens — the only write paths."
    - "xcodegen generate -> Ritham.xcodeproj/project.pbxproj — new files are invisible to xcodebuild until regenerated."
---

<objective>
Extract the embeddable content of the two already-built screens the Phase 4 dashboard must host
inline — `RecommendationsView` (D-04) and `DietPlanView`'s DIET-01-isolated controls (D-05) — into
plain child views, and prove the diet extraction cannot touch the safety-screening write path.

Purpose: 04-RESEARCH.md's core structural finding is that both screens wrap themselves in
`RithamScreen` (which supplies its own `ZStack`, background, `ScreenHeader` and `ScrollView`).
Embedding either struct's `body` inside the dashboard's own `RithamScreen` would nest a second
full-screen scaffold — broken, not merely ugly. Extraction is the prerequisite for plan 04-02's
dashboard rewrite; nothing about the dashboard layout can be built until this lands.

Output: three new files (two content views, one test suite), two thinned host screens, and a
regenerated `project.pbxproj`. Zero domain-logic change: `RecommendationsModel`,
`WorkoutPlanClient` and every `HealthDataStore` accessor are reused byte-for-byte.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-household-home/04-CONTEXT.md
@.planning/phases/04-household-home/04-RESEARCH.md
@.planning/phases/04-household-home/04-PATTERNS.md
@.planning/phases/04-household-home/04-UI-SPEC.md

@RithamApp/Ritham/Recommendations/Views/RecommendationsView.swift
@RithamApp/Ritham/Settings/DietPlanView.swift
@RithamApp/Ritham/Settings/SettingsView.swift
@RithamApp/RithamTests/Phase3CoverageTests.swift
@RithamApp/RithamTests/MomentumSuiteSerialization.swift
</context>

<source_audit>
## Multi-Source Coverage Audit (phase-wide, all four sources)

| Source | Item | Status | Covered by |
|--------|------|--------|-----------|
| GOAL | ROADMAP Phase 4 criterion 1 — home screen is a real dashboard with logged exercise, sleep, workout-plan and diet-plan sections visible, not one tap deeper | COVERED | 04-02 (layout), 04-03 (structural gate) |
| GOAL | ROADMAP Phase 4 criteria 2 & 3 — household grouping, cheers, visibility spectrum | DEFERRED | ROADMAP.md's dated 2026-09-08 annotation on criteria 2/3 + 04-CONTEXT.md Phase Boundary — later Phase 4 round |
| REQ | CROSSGEN-01 (revised 2026-09-08) | COVERED | 04-01, 04-02, 04-03 |
| REQ | HOUSEHOLD-01 | DEFERRED | 04-CONTEXT.md Deferred Ideas — genuinely new multi-user/social scope, later Phase 4 round |
| REQ | CROSSGEN-04 | DEFERRED | 04-CONTEXT.md Deferred Ideas — has no meaning until a household/sharing concept exists |
| RESEARCH | Pattern 1 — content-extraction re-parenting for Recommendations and Diet | COVERED | 04-01 Tasks 1 & 2 |
| RESEARCH | Pattern 2 — section-card wrapper | COVERED | 04-02 Task 2 |
| RESEARCH | Pattern 3 — `nonisolated static func` derivations; `routingSteps` must drop `.recommendations` | COVERED | 04-02 Tasks 2 & 3 |
| RESEARCH | Pitfall 1 — `HomeHubView` file path / type name / MARK string / `extension HomeHubView` boundary are test-coupled | COVERED | 04-02 Tasks 1 & 2 |
| RESEARCH | Pitfall 2 — Momentum section must physically land under `Ritham/Momentum/` to gain no-sharing coverage | COVERED | 04-02 Task 1; non-vacuity proof in 04-03 Task 1 |
| RESEARCH | Pitfall 3 (HIGH) — diet dashboard section must never re-resolve/re-save the screening | COVERED | 04-01 Tasks 2 & 3 |
| RESEARCH | Pitfall 4 — `.recommendations` stays registered though nothing pushes to it | COVERED | 04-01 Task 1; asserted in 04-03 Task 1 |
| RESEARCH | Pitfall 5 — extracted content carries no "Done"/dismiss control | COVERED | 04-01 Task 2 |
| RESEARCH | Pitfall 6 — Movement Snapshot entry must not neighbour the Momentum section | COVERED | 04-02 Tasks 1 & 2 |
| RESEARCH | Runtime State Inventory — xcodegen regeneration on every file addition | COVERED | 04-01 Tasks 1 & 2, 04-02 Task 1, 04-03 Task 1 |
| RESEARCH | Wave 0 gap — no diet test suite exists (`find RithamApp/RithamTests -iname "*Diet*"` returns nothing) | COVERED | 04-01 Task 3 |
| RESEARCH | Wave 0 gap — re-verify `theSnapshotEntryIsNotAdjacentToTheMomentumSummary` and `noMomentumSurfaceOffersASharingAffordance` against the rewrite | COVERED | 04-02 Tasks 1 & 2, 04-03 Tasks 1 & 2 |
| CONTEXT | D-01 — real dashboard, never a vertical CTA list, "in shape or in spirit" | COVERED | 04-02 Task 2; gated in 04-03 Task 1 |
| CONTEXT | D-02 — logged-exercise section from `MomentumSummary.recentSessions`, never re-derived | COVERED | 04-02 Task 2 |
| CONTEXT | D-03 — sleep check-in entry carried over unchanged, no completed/skipped indicator | COVERED | 04-02 Task 2 |
| CONTEXT | D-04 — workout-plan section surfaces Recommendations inline | COVERED | 04-01 Task 1, 04-02 Task 2 |
| CONTEXT | D-05 (narrowed) — diet section is the DIET-01-isolated controls only | COVERED | 04-01 Tasks 2 & 3, 04-02 Task 2 |
| CONTEXT | D-06 — Momentum summary re-parented, not rebuilt; no-sharing gate keeps passing | COVERED | 04-02 Task 1, 04-03 Task 1 |
| CONTEXT | D-07 — cardio/strength/guidance/Settings reachable as section actions, not a button list | COVERED | 04-02 Task 2; Settings/DietPlanView reachability gated in 04-03 Task 1 |
| CONTEXT | D-08 — steps/calories NOT built; leave structural room, no placeholder card | COVERED | 04-02 Task 2 (uniform card stack, no steps/calories card) |
| CONTEXT | D-09 — onboarding-restart bug already fixed (commit `744784d`) | EXCLUDED | Explicitly out of phase scope per D-09 |
| CONTEXT | Deferred Ideas — HOUSEHOLD-01, CROSSGEN-04, steps/calories | EXCLUDED | 04-CONTEXT.md Deferred Ideas |

No item is MISSING. The three DEFERRED rows carry dated, cited deferral records; they are not
silent omissions.
</source_audit>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New types (all in the `Ritham` app target):

| Symbol | File | Owning plan |
|--------|------|-------------|
| `RecommendationsSectionContent` (struct, `View`) | `RithamApp/Ritham/Recommendations/Views/RecommendationsSectionContent.swift` | 04-01 |
| `DietPatternPicker` (struct, `View`) | `RithamApp/Ritham/Settings/DietPlanSectionContent.swift` | 04-01 |
| `AllergenPicker` (struct, `View`) | `RithamApp/Ritham/Settings/DietPlanSectionContent.swift` | 04-01 |
| `DietPlanSectionContent` (struct, `View`) | `RithamApp/Ritham/Settings/DietPlanSectionContent.swift` | 04-01 |
| `MomentumDashboardSection` (struct, `View`) | `RithamApp/Ritham/Momentum/Components/MomentumDashboardSection.swift` | 04-02 |
| `HomeHubView.dashboardHeadline` (`nonisolated static let String`) | `RithamApp/Ritham/Home/HomeHubView.swift` | 04-02 |
| `HomeHubView.sectionCard(_:)` (private helper) | `RithamApp/Ritham/Home/HomeHubView.swift` | 04-02 |

New test suites:

| Suite | File | Nested in | Owning plan |
|-------|------|-----------|-------------|
| `DietSectionIsolationTests` | `RithamApp/RithamTests/DietSectionIsolationTests.swift` | `MomentumContainerTouchingSuites` | 04-01 |
| (new tests appended to `HomeHubTests`) | `RithamApp/RithamTests/HomeHubTests.swift` | — (flat, unchanged) | 04-02 |
| `Phase4CoverageTests` | `RithamApp/RithamTests/Phase4CoverageTests.swift` | `StepRegistryTouchingSuites` | 04-03 |

Deleted symbols: `RecommendationsView.idleContent(_:)`, `.planContent(_:model:)`,
`.errorContent(_:model:)`, `.content`, `.request(_:)` (moved into
`RecommendationsSectionContent`); `DietPlanView.persistDiet(_:)`, `.persistAllergens(_:)`,
`.dietOptionTitle(_:)`, `.allergenOptionTitle(_:)` (moved into the new pickers);
`HomeHubView.momentumSection`'s inline body (moved into `MomentumDashboardSection`, 04-02).

No new types are added to `RithamCore`. No new external package is introduced anywhere in the
phase.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract RecommendationsSectionContent and thin RecommendationsView to a wrapper</name>

  <files>
    RithamApp/Ritham/Recommendations/Views/RecommendationsSectionContent.swift (new),
    RithamApp/Ritham/Recommendations/Views/RecommendationsView.swift,
    RithamApp/Ritham.xcodeproj/project.pbxproj
  </files>

  <read_first>
    - `RithamApp/Ritham/Recommendations/Views/RecommendationsView.swift` (whole file — closest
      and only analog; the extraction is a cut-and-paste out of it)
    - `.planning/phases/04-household-home/04-PATTERNS.md`, section
      "`RithamApp/Ritham/Recommendations/Views/RecommendationsSectionContent.swift` (new)" — the
      target shape, already written out
    - `.planning/phases/04-household-home/04-RESEARCH.md`, "Pattern 1: Content-extraction
      re-parenting" and "Pitfall 4"
  </read_first>

  <action>
    Create `RecommendationsSectionContent.swift` next to `RecommendationsView.swift`, importing
    `SwiftUI` and `RithamCore` only. Declare `struct RecommendationsSectionContent: View` with two
    stored properties, `let model: RecommendationsModel` and `let flow: OnboardingFlow`.

    Move `RecommendationsView`'s four rendering members into it as a mechanical cut-and-paste
    (04-RESEARCH.md "Don't Hand-Roll": resist rewriting anything while moving it):
    - the `content` switch becomes the new type's `body`, with the outer `if let model` unwrap
      dropped (the host now supplies a non-optional model);
    - `idleContent(_:)` becomes a parameterless `@ViewBuilder private var idleContent`;
    - `planContent(_:model:)` becomes `@ViewBuilder private func planContent(_ plan: WorkoutPlan)`;
    - `errorContent(_:model:)` becomes `@ViewBuilder private func errorContent(_ error: WorkoutPlanClientError)`;
    - `request(_:)` becomes `private func request()` wrapping `Task { await model.requestPlan(flow: flow) }`.
    Every copy string, `RithamType` font role, `RithamColor` style, `fixedSize`, `ForEach` and
    spacing constant is carried over unchanged. Do not add a heading, a card background, a
    `RithamScreen`, a `ScrollView`, a `NavigationStack`, or a dismiss control — this type renders
    content only; its host owns all chrome (04-RESEARCH.md Anti-Patterns).

    Then thin `RecommendationsView.body` to `RithamScreen(surface: DecorativeSurface.flat,
    headline: "Recommendations")` whose content is `if let model { RecommendationsSectionContent(
    model: model, flow: flow) } else { ProgressView() }`, keeping its existing `.onAppear`
    model construction verbatim. `RecommendationsView` keeps its `OnboardingStepPresenting`
    conformance, `static let step: OnboardingStep = .recommendations` and `makeView(flow:)`
    exactly as they are — per 04-RESEARCH.md Pitfall 4, `.recommendations` must stay registered
    after plan 04-02 stops pushing to it, or `StepRegistry.unregisteredSteps` is no longer empty.
    `RecommendationsState` and `RecommendationsModel` stay declared in `RecommendationsView.swift`
    and are not edited at all.

    Update `RecommendationsView`'s header comment where it states the hub reaches this screen via
    a "Recommendations" action: after 04-02 the dashboard embeds the content instead, and this
    screen stays registered rather than pushed. State that, and cite 04-RESEARCH.md Pitfall 4 for
    why the registration remains.

    Finally run `xcodegen generate` from inside `RithamApp/` and stage the regenerated
    `Ritham.xcodeproj/project.pbxproj` in the same commit — xcodegen's source list is a directory
    scan, so `xcodebuild` cannot see the new file until the project is regenerated (04-RESEARCH.md
    Runtime State Inventory; 02-06 precedent in STATE.md).
  </action>

  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/RecommendationsScreenTests</automated>
  </verify>

  <acceptance_criteria>
    - `Scripts/build-app.sh build` succeeds.
    - `Scripts/build-app.sh test -only-testing:RithamTests/RecommendationsScreenTests` passes and
      its Swift Testing summary reports a non-zero test count (a `-only-testing` filter that
      matches nothing exits 0 with zero tests — the count must be read, not just the exit code;
      see 03-07-SUMMARY.md's filter-string mismatch).
    - `Scripts/build-app.sh test -only-testing:RithamTests/StepRegistryTouchingSuites/Phase2CoverageTests`
      still passes — `.recommendations` still resolves to `RecommendationsView`.
    - `git status --short RithamApp/Ritham.xcodeproj/project.pbxproj` shows the regenerated
      project file staged alongside the new source file.
    - `RecommendationsSectionContent.swift` declares no `RithamScreen`, `ScrollView`,
      `NavigationStack` or `@Environment(\.dismiss)` member.
    - `RecommendationsModel`'s source is byte-for-byte unchanged (`git diff` on
      `RecommendationsView.swift` touches only the view struct, its header comment, and the
      removed rendering members).
  </acceptance_criteria>

  <done>
    One content implementation exists for the workout plan, hosted by the full screen today and by
    the dashboard in plan 04-02; `.recommendations` is still registered and still resolves to
    `RecommendationsView`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract the DIET-01-isolated pickers into DietPlanSectionContent.swift</name>

  <files>
    RithamApp/Ritham/Settings/DietPlanSectionContent.swift (new),
    RithamApp/Ritham/Settings/DietPlanView.swift,
    RithamApp/Ritham.xcodeproj/project.pbxproj
  </files>

  <read_first>
    - `RithamApp/Ritham/Settings/DietPlanView.swift` (whole file, including its long header
      comment — that comment is the primary source for which controls are screening-coupled and
      which are isolated)
    - `.planning/phases/04-household-home/04-RESEARCH.md`, "Pitfall 3 (HIGH severity)",
      "Pitfall 5", and the "DIET-01-isolated persistence" code example
    - `.planning/phases/04-household-home/04-CONTEXT.md`, D-05 (the narrowed decision)
    - `RithamApp/Ritham/Persistence/HealthDataStore.swift` — read the declarations of
      `loadProfile()`, `updateProfile(_:)`, `loadFoodAllergens()`, `saveFoodAllergens(_:)` and
      `UserProfileDraft`; note `loadProfile()` throws `HealthDataStoreError.profileMissing` on an
      empty store
    - `RithamApp/Ritham/Components/ChoiceQuestionView.swift` — the two initialisers; only the
      `mode:`/`selection:` one is used here
  </read_first>

  <action>
    Create `DietPlanSectionContent.swift` in `RithamApp/Ritham/Settings/`, importing `SwiftUI` and
    `RithamCore` only (mirror `DietPlanView`'s import list — `ModelContext` reaches this file
    through SwiftUI's environment key without importing SwiftData). Declare three view structs in
    that one file:

    1. `DietPatternPicker` — `let flow: OnboardingFlow`,
       `@Environment(\.modelContext) private var modelContext`,
       `@State private var selection: Set<DietaryPattern> = []`,
       `@State private var hasHydrated = false`, `@State private var showSaveError = false`.
       Its body is the dietary-pattern `ChoiceQuestionView` copied verbatim from
       `DietPlanView` (prompt `OnboardingCopy.Diet.headline`, helper `OnboardingCopy.Diet.helper`,
       `options: DietaryPattern.allCases`, `mode: .single`, `optionTitle: dietOptionTitle`),
       followed by the `showSaveError` text block (`OnboardingCopy.Errors.savingFailed`,
       `RithamType.label`, `RithamColor.hot`, `fixedSize(horizontal: false, vertical: true)`).
       Move `dietOptionTitle(_:)` over verbatim.
    2. `AllergenPicker` — same shape, `@State private var selection: Set<FoodAllergen> = []`, body
       is the allergen `ChoiceQuestionView` copied verbatim (prompt
       `OnboardingCopy.Diet.allergensHeadline`, helper `OnboardingCopy.Diet.allergensHelper`,
       `options: FoodAllergen.allCases`, `mode: .multiple(exclusiveOption: nil)`,
       `optionTitle: allergenOptionTitle`) plus its own `showSaveError` block. Move
       `allergenOptionTitle(_:)` over verbatim.
    3. `DietPlanSectionContent` — `let flow: OnboardingFlow`; body is a
       `VStack(alignment: .leading, spacing: RithamSpacing.md)` of `DietPatternPicker(flow: flow)`
       then `AllergenPicker(flow: flow)`. This is the single symbol the dashboard embeds in plan
       04-02.

    Persistence and binding rules, which are the safety-critical part of this task:
    - Each picker drives its `ChoiceQuestionView` through a computed `Binding` whose setter
      assigns the `@State` and then persists — following `DietPlanView.checklistBinding`'s
      existing in-repo precedent. Do NOT use `.onChange(of:)`: hydration below writes the same
      `@State`, and an `onChange` handler fires on the following render pass, after any
      `hasHydrated` guard has already flipped true, so an `onChange`-based guard cannot suppress
      a hydration-triggered write. A custom `Binding` persists only on genuine user input, by
      construction.
    - `DietPatternPicker`'s setter assigns `flow.answers.dietaryPattern` then calls its private
      `persistDiet(_:)`, which constructs `HealthDataStore(context: modelContext)`, reads
      `try? store.loadProfile()` for the existing age, returns early when there is no profile, and
      calls `store.updateProfile(UserProfileDraft(age: existingAge, dietaryPattern: pattern))`
      inside a `do`/`catch` that sets `showSaveError` on failure (a small improvement on
      `DietPlanView`'s silent `try?`, required by 04-UI-SPEC.md's diet-section error state).
    - `AllergenPicker`'s setter assigns `flow.answers.allergens` then calls its private
      `persistAllergens(_:)`, which calls `store.saveFoodAllergens(allergens)` in the same
      `do`/`catch` shape.
    - These two `HealthDataStore` methods are the ONLY persistence calls permitted anywhere in this
      file. The file must contain no screening re-resolve of any kind, no food-allergy checklist
      control, no severity follow-up, and no `@Environment(\.dismiss)`/"Done" control
      (04-RESEARCH.md Pitfall 5: `dismiss()` silently no-ops outside a presentation context, so on
      an always-visible dashboard it is a dead tap target). Plan 04-01 Task 3 gates this
      structurally.
    - Each picker hydrates in `.onAppear`, guarded by `hasHydrated` so a re-appearance never
      clobbers an in-progress selection: build the store, read
      `(try? store.loadProfile())?.dietaryPattern` (respectively `try? store.loadFoodAllergens()`),
      fall back to `flow.answers.dietaryPattern` / `flow.answers.allergens`, then to
      `DietaryPattern.none` / the empty set, assign the `@State` directly (never through the
      binding), and set `hasHydrated = true`. Both reads are `try?`-tolerant because
      `loadProfile()` throws on an empty store — the same tolerance `MomentumSummaryReader`
      already applies (STATE.md, 03-05). Without this, the dashboard's picker would report
      "no dietary pattern" on every cold launch while the store held a real answer, because
      `OnboardingRootView` constructs a fresh `OnboardingFlow()` with empty answers when rooting at
      `.home` (04-RESEARCH.md Pitfall 3's own observation about cold-launch flow state).

    Then edit `DietPlanView` to host the two new pickers in place of its own copies, preserving its
    current on-screen order exactly: `DietPatternPicker(flow: flow)` where its dietary-pattern
    `ChoiceQuestionView` is today, the food-allergy checklist `ChoiceQuestionView`
    (`checklistBinding`) unchanged after it, then inside the existing
    `if flow.answers.screening.checklist.items.contains(.foodAllergies)` branch the severity
    `ChoiceQuestionView` unchanged followed by `AllergenPicker(flow: flow)`. Delete
    `DietPlanView`'s now-duplicated `dietSelection`/`allergenSelection` state, its
    `dietOptionTitle`/`allergenOptionTitle` helpers, its `persistDiet`/`persistAllergens`
    functions, and the two `.onChange` handlers that fed them. `DietPlanView` keeps
    `severitySelection`, `checklistBinding`, its severity `.onChange`, `resolveAndSaveScreening()`,
    its own `showSaveError`, `@Environment(\.dismiss)` and the "Done" button — the screening
    question stays a Settings-only control, which is the entire premise of D-05's narrowing.

    Run `xcodegen generate` from inside `RithamApp/` and stage the regenerated
    `Ritham.xcodeproj/project.pbxproj` in the same commit.

    <!-- planner-discipline-allow: GateResolution -->
    <!-- planner-discipline-allow: saveScreeningResult -->
    <!-- planner-discipline-allow: dismiss -->
    Writing convention for this file: any explanatory comment that needs to name a symbol this
    file is forbidden to call must be written as a whole-line `//` comment, never as a trailing
    comment on a code line — Task 3's structural gate filters whole-line comments only, exactly as
    `Phase3CoverageTests.noMomentumSurfaceOffersASharingAffordance` already does.
  </action>

  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/EditAnswerFlowTests</automated>
  </verify>

  <acceptance_criteria>
    - `Scripts/build-app.sh build` succeeds.
    - `Scripts/build-app.sh test -only-testing:RithamTests/EditAnswerFlowTests` passes with a
      non-zero test count — the screening re-resolve path `DietPlanView` shares with
      `EditAnswerFlow` is unbroken.
    - `Scripts/build-app.sh test -only-testing:RithamTests/AlwaysFreeListTests -only-testing:RithamTests/WorkoutFrequencyTests`
      passes — the Settings screen that presents `DietPlanView` still builds and behaves.
    - `DietPlanSectionContent.swift` contains exactly two persistence call sites,
      `store.updateProfile(` and `store.saveFoodAllergens(`, and no other `store.` write call.
    - `DietPlanView.swift` still declares `checklistBinding`, `severitySelection` and
      `resolveAndSaveScreening` — the food-allergy screening question did not move.
    - `git status --short RithamApp/Ritham.xcodeproj/project.pbxproj` shows the regenerated project
      file staged alongside the new source file.
  </acceptance_criteria>

  <done>
    The diet-pattern and allergen controls exist as embeddable child views whose only writes are
    the two DIET-01-isolated `HealthDataStore` calls, hydrated from the device store rather than
    from empty in-memory flow state; the screening checkbox remains Settings-only.
  </done>
</task>

<task type="auto">
  <name>Task 3: DietSectionIsolationTests — the DIET-01 isolation gate (Wave 0 gap)</name>

  <files>
    RithamApp/RithamTests/DietSectionIsolationTests.swift (new),
    RithamApp/Ritham.xcodeproj/project.pbxproj
  </files>

  <read_first>
    - `RithamApp/RithamTests/Phase3CoverageTests.swift` — the comment-filtered source-scan
      technique with its non-vacuity guard; copy this shape, do not invent a new one
    - `RithamApp/RithamTests/MomentumSuiteSerialization.swift` — the nesting mechanism and why
      every new container-creating suite must use it
    - `RithamApp/RithamTests/MovementSnapshotTests.swift` — an existing nested,
      container-creating suite's `makeStore()` helper
    - `RithamApp/Ritham/Persistence/HealthDataStore.swift` — `saveScreeningResult(_:answers:now:)`
      and `activeConditionTags(now:)` signatures, for the behavioural half of this gate
    - `.planning/phases/04-household-home/04-RESEARCH.md`, "Wave 0 Gaps" (this suite closes the
      first bullet: `find RithamApp/RithamTests -iname "*Diet*"` returns nothing today)
  </read_first>

  <action>
    Create `DietSectionIsolationTests.swift` declaring
    `extension MomentumContainerTouchingSuites { @MainActor @Suite("DietSectionIsolationTests",
    .serialized) struct DietSectionIsolationTests { ... } }`. The nesting is mandatory, not
    stylistic: this suite instantiates in-memory SwiftData `ModelContainer`s, and STATE.md records
    an open low-frequency crash when unrelated suites do that concurrently — the parent suite is
    the project's established fix.

    Give it a private `makeStore()` returning a `HealthDataStore` over
    `RithamModelContainer.make(inMemory: true)`, matching `MovementSnapshotTests`' helper.

    Write four tests.

    Behavioural half (proves the isolation the dashboard depends on, at the store level the new
    pickers call):
    1. `aDietaryPatternWriteLeavesEveryConditionTagUntouched` — seed a profile via
       `updateProfile(UserProfileDraft(age:))`, build a `ScreeningAnswers` value that resolves to
       at least one condition tag, persist it with `GateResolution.resolve` +
       `saveScreeningResult(_:answers:now:)`, snapshot `Set(try store.activeConditionTags(now:))`,
       then call `updateProfile(UserProfileDraft(age: <same age>, dietaryPattern: .vegan))` and
       assert the tag set is unchanged and `try store.loadProfile().dietaryPattern == .vegan`.
       Assert the snapshot is non-empty first, so this test cannot pass by comparing two empty
       sets.
    2. `anAllergenWriteLeavesEveryConditionTagUntouched` — same fixture, calling
       `saveFoodAllergens([.peanuts, .milk])` instead, asserting the tag set is unchanged and
       `try store.loadFoodAllergens() == [.peanuts, .milk]`.

    Structural half (proves the dashboard-embeddable file cannot reach the screening write path
    even after a future edit):
    3. `theDietSectionContentReachesNoScreeningWritePath` — resolve
       `Ritham/Settings/DietPlanSectionContent.swift` relative to this test file's own `#filePath`
       (two `deletingLastPathComponent()` calls then `appendingPathComponent`, exactly as
       `Phase3CoverageTests` and `MovementSnapshotViewTests` already do). `Issue.record` and return
       if the file does not exist. Read it, drop every line whose trimmed form starts with `//`,
       and assert the remaining source contains none of the banned tokens
       `["GateResolution", "saveScreeningResult", "ScreeningAnswers", "checklist", "ChecklistItem",
       "dismiss"]`. Guard against a vacuous pass by asserting the filtered source is non-empty and
       does contain `"saveFoodAllergens"` — proving the scan really read the intended file.
    4. `theSettingsDietPlanScreenStillOwnsTheScreeningQuestion` — same `#filePath`-relative
       resolution for `Ritham/Settings/DietPlanView.swift`; assert its comment-filtered source
       contains `"checklistBinding"` and `"resolveAndSaveScreening"`. This is the counterpart
       guard: D-05's narrowing is only safe while the screening question still exists somewhere,
       and a careless cleanup that deleted it would otherwise pass every other gate in this phase.

    Run `xcodegen generate` from inside `RithamApp/` and stage the regenerated
    `Ritham.xcodeproj/project.pbxproj`.
  </action>

  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/MomentumContainerTouchingSuites/DietSectionIsolationTests</automated>
  </verify>

  <acceptance_criteria>
    - `Scripts/build-app.sh test -only-testing:RithamTests/MomentumContainerTouchingSuites/DietSectionIsolationTests`
      passes and its Swift Testing summary reports at least 4 tests — the count must be read, not
      just the exit code (a filter that matches nothing exits 0 with zero tests). Splitting any of
      the four into finer `@Test`s is fine; dropping one is not.
    - `find RithamApp/RithamTests -iname "*Diet*"` now returns
      `RithamApp/RithamTests/DietSectionIsolationTests.swift`, closing 04-RESEARCH.md's first
      Wave 0 gap.
    - The suite is declared inside `extension MomentumContainerTouchingSuites`.
    - Negative control, run once by hand and then reverted, recorded in the SUMMARY: temporarily
      adding a screening re-resolve call to `DietPlanSectionContent.swift` makes test 3 fail.
      Without this the gate is unproven.
    - `Scripts/build-app.sh test` (full target) passes.
  </acceptance_criteria>

  <done>
    DIET-01's isolation is enforced by an automated gate for the first time in the project, both
    behaviourally at the store level and structurally at the file level, and the Settings-only home
    of the screening question is itself asserted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory `OnboardingFlow` state -> on-device SwiftData store | A view writes persisted health data derived from an in-memory answers object that may be empty on a cold launch |
| user tap -> `HealthDataStore` write | The only untrusted input on these surfaces is a fixed-choice enum selection; there is no free text and no network write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering (unintentional, self-inflicted) | `DietPlanSectionContent.swift`'s write path | high | mitigate | The extracted pickers call only `HealthDataStore.updateProfile` and `.saveFoodAllergens`, never a screening re-resolve or `saveScreeningResult`; enforced by Task 3's comment-filtered structural gate plus two behavioural tests asserting a diet/allergen write leaves `activeConditionTags(now:)` byte-identical. Matches 04-RESEARCH.md Security Domain's single V4 Access Control finding and D-05's narrowing. |
| T-04-04 | Denial of Service (of a safety control) | `DietPlanView`'s food-allergy screening question | medium | mitigate | Narrowing D-05 could tempt a cleanup that deletes the screening question outright rather than leaving it in Settings. Task 3's `theSettingsDietPlanScreenStillOwnsTheScreeningQuestion` asserts `checklistBinding` and `resolveAndSaveScreening` still exist in `DietPlanView.swift`. |
| T-04-SC | Tampering | npm/pip/cargo/SPM installs | low | accept | No package-manager install task exists in this phase. 04-RESEARCH.md's Package Legitimacy Audit records "Not applicable — this phase introduces no external packages"; `Package.swift`/`project.yml` dependency lists are unchanged. Nothing to verify, so no legitimacy checkpoint is required. |

ASVS L1, blocking on High: T-04-01 is High and is mitigated with an automated gate landing in the
same plan, not deferred.
</threat_model>

<verification>
- `Scripts/build-app.sh build` succeeds.
- `Scripts/build-app.sh test` (full `RithamTests` target) passes with no new failures, including
  `RecommendationsScreenTests`, `EditAnswerFlowTests`, `PhaseCoverageTests`,
  `Phase2CoverageTests`, `Phase3CoverageTests` and `MovementSnapshotViewTests` — this plan touches
  no `HomeHubView` source, so the two source-scan gates over it must be untouched and green.
- `RithamCore/Scripts/test-core.sh` passes — `RithamCore` is not edited by this plan, so this is a
  regression check only.
- `Ritham.xcodeproj/project.pbxproj` is regenerated and committed; a clean `xcodegen generate`
  produces no further diff.
</verification>

<success_criteria>
- `RecommendationsSectionContent` renders the complete idle/loading/plan/error state machine with
  no screen chrome of its own, and `RecommendationsView` renders the identical content wrapped in
  its existing `RithamScreen`.
- `.recommendations` still resolves to `RecommendationsView` and `StepRegistry.unregisteredSteps`
  is still empty.
- `DietPlanSectionContent` renders the dietary-pattern and allergen pickers, hydrated from the
  device store, persisting only through `updateProfile`/`saveFoodAllergens`.
- No dismiss/"Done" control, food-allergy checkbox, or severity follow-up exists in
  `DietPlanSectionContent.swift`.
- `DietSectionIsolationTests` exists, is nested in `MomentumContainerTouchingSuites`, reports 4
  tests, and its structural gate has been shown to fail against a deliberately broken file.
</success_criteria>

<output>
Create `.planning/phases/04-household-home/04-01-SUMMARY.md` when done. Record: the negative-control
result for Task 3's structural gate; any deviation from 04-PATTERNS.md's sketch (the split into
`DietPatternPicker`/`AllergenPicker`/`DietPlanSectionContent` and the custom-`Binding`-instead-of-
`onChange` decision are both deliberate and must be logged with their reasons); and the exact
symbols `HomeHubView` will need to reference in plan 04-02.
</output>
