---
phase: 04-household-home
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - RithamApp/Ritham/Momentum/Components/MomentumDashboardSection.swift
  - RithamApp/Ritham/Home/HomeHubView.swift
  - RithamApp/RithamTests/HomeHubTests.swift
  - RithamApp/Ritham.xcodeproj/project.pbxproj
autonomous: true
requirements: [CROSSGEN-01]

must_haves:
  truths:
    - "The home screen shows the Momentum summary, recent logged sessions, the sleep check-in entry, the workout plan and the diet-plan controls as sections on the screen itself, without navigating anywhere."
    - "No section is a card whose only content is a heading plus a button — every section renders real content above any action control (D-01's actual compliance test)."
    - "No copy on the home screen describes it as interim, temporary or a placeholder."
    - "The Daily Movement Snapshot entry renders inside the exercise section, never inside or immediately after the Momentum section."
    - "Cardio tracking, cardio history, strength logging, strength history, guidance, Settings and the Settings-presented diet plan all remain reachable from the home screen."
    - "The Momentum summary rendering physically lives under Ritham/Momentum/, inside the existing no-sharing directory gate's walk."
    - "The workout-plan section never fetches a plan or opens the pre-assessment without an explicit user tap."
  artifacts:
    - RithamApp/Ritham/Momentum/Components/MomentumDashboardSection.swift
    - RithamApp/Ritham/Home/HomeHubView.swift
  key_links:
    - "HomeHubView.body -> MomentumDashboardSection / RecommendationsSectionContent / DietPlanSectionContent — all three hosted inline, none pushed."
    - "HomeHubView.momentumSection (last struct member, MARK-preceded) -> MomentumDashboardSection — the shape MovementSnapshotViewTests' source scan depends on."
    - "HomeHubView -> SettingsView sheet -> DietPlanView — the only surviving route to the food-allergy screening question."
    - "MomentumSummaryReader.summary(now:) -> both the Momentum card and the exercise card's session list — one read, never re-derived."
---

<objective>
Replace `HomeHubView`'s vertical CTA list with the real sectioned dashboard 04-UI-SPEC.md
specifies (CROSSGEN-01, D-01 through D-08), and re-parent the Momentum summary rendering into
`Ritham/Momentum/Components/` so the existing MOMENTUM-06 no-sharing directory gate actually
covers it.

Purpose: this is the screen the user tested and rejected during Phase 3 sign-off. Everything in
this phase exists to change it. It is also the phase's single riskiest edit: two shipped tests
read this exact file off disk and assert facts about its literal text, so the rewrite is
constrained in ways a normal SwiftUI refactor is not.

Output: one new component file, one rewritten screen body, and new derivation tests. No domain
logic changes anywhere — every reader, model and store call is reused unchanged.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-household-home/04-CONTEXT.md
@.planning/phases/04-household-home/04-RESEARCH.md
@.planning/phases/04-household-home/04-PATTERNS.md
@.planning/phases/04-household-home/04-UI-SPEC.md
@.planning/phases/04-household-home/04-01-SUMMARY.md

@RithamApp/Ritham/Home/HomeHubView.swift
@RithamApp/RithamTests/MovementSnapshotViewTests.swift
@RithamApp/RithamTests/HomeHubTests.swift
@RithamApp/Ritham/Momentum/Components/ShieldRow.swift
@RithamApp/Ritham/Components/RithamScreen.swift
</context>

<test_coupled_constraints>
## Read this before touching HomeHubView.swift

`MovementSnapshotViewTests.theSnapshotEntryIsNotAdjacentToTheMomentumSummary` opens
`RithamApp/Ritham/Home/HomeHubView.swift` off disk, finds the FIRST occurrence of the literal
`MARK: - D-08's Momentum summary section`, then finds the first `\nextension HomeHubView` AFTER
it, and asserts the raw text between those two points never contains the token `movementSnapshot`.
It does **no comment filtering** — comments count. Two of its three failure modes are
`Issue.record` calls (marker not found; extension not found), not assertion failures, so they only
surface when the suite runs.

Five literal, non-negotiable structural rules follow from that:

1. The file stays at `RithamApp/Ritham/Home/HomeHubView.swift` and the type stays named
   `HomeHubView`. The rewrite is a body change, never a rename or a relocation.
2. The literal `MARK: - D-08's Momentum summary section` appears **exactly once** in the file.
   Today's `@State momentumSummary` doc comment says "D-08's Momentum summary section state"
   without the `MARK: - ` prefix and is therefore safe — do not add the prefix to it, and do not
   introduce a second copy of the marker anywhere.
3. `momentumSection` remains a member of `HomeHubView` and is the **last member of the struct**,
   with the MARK comment immediately before it. Its new body is a single call to
   `MomentumDashboardSection`.
4. **No member declaration, doc comment, or line containing the token `movementSnapshot` may
   appear after the MARK line.** The exercise section — which owns the snapshot entry — must be
   declared above the MARK. This is the exact trap 04-PATTERNS.md's phrasing ("keep the marker
   immediately before the call site that invokes `MomentumDashboardSection` inside the new body")
   would walk into: if the marker sits inside `body` and the snapshot entry renders later in
   `body`, the scanned region contains `movementSnapshot` and the test fails.
5. `extension HomeHubView { ... }` stays at the bottom of the file, after the marker, holding the
   `nonisolated static` derivations. Hoisting it above the marker makes the second `range(of:)`
   fail and the test `Issue.record`s.

`Phase3CoverageTests.noMomentumSurfaceOffersASharingAffordance` recursively walks
`Ritham/Momentum/` and `Ritham/MovementSnapshot/` for `ShareLink`, `UIActivityViewController` and
`UIPasteboard`. Placing `MomentumDashboardSection.swift` under `Ritham/Momentum/Components/` is
what brings the dashboard's Momentum rendering inside that walk (04-RESEARCH.md Pitfall 2). Note
that this gate passing proves nothing on its own about the new file — its only non-vacuity guard
is "at least one file scanned", already satisfied by three pre-existing files in that directory.
Plan 04-03 adds the positive file-location assertion that makes the coverage real.
</test_coupled_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Re-parent the Momentum summary into Ritham/Momentum/Components/, body otherwise unchanged</name>

  <files>
    RithamApp/Ritham/Momentum/Components/MomentumDashboardSection.swift (new),
    RithamApp/Ritham/Home/HomeHubView.swift,
    RithamApp/Ritham.xcodeproj/project.pbxproj
  </files>

  <read_first>
    - `RithamApp/Ritham/Home/HomeHubView.swift` (whole file — the analog is the file itself)
    - `.planning/phases/04-household-home/04-PATTERNS.md`, section
      "`RithamApp/Ritham/Momentum/Components/MomentumDashboardSection.swift` (new)" — the target
      shape, already written out
    - `.planning/phases/04-household-home/04-RESEARCH.md`, Pitfall 1 and Pitfall 2
    - `RithamApp/RithamTests/MovementSnapshotViewTests.swift`,
      `theSnapshotEntryIsNotAdjacentToTheMomentumSummary` — read the actual scan before editing
      the file it scans
    - `RithamApp/Ritham/Momentum/Components/ShieldRow.swift` — a sibling component's file shape
      and header-comment convention
  </read_first>

  <action>
    This task is a pure mechanical move with zero behaviour change, per D-06 ("re-parented, not
    rebuilt — it is proven, tested, reconciliation-on-read code"). Deliberately so: it is the clean
    bisect point before Task 2 rewrites the body, and the full suite must be green at the end of
    it.

    Create `RithamApp/Ritham/Momentum/Components/MomentumDashboardSection.swift`, importing
    `SwiftUI` and `RithamCore`. Declare `struct MomentumDashboardSection: View` with
    `let summary: MomentumSummary?`, `let loadFailed: Bool`, `let flow: OnboardingFlow`. Move
    `HomeHubView.momentumSection`'s body into it verbatim, substituting the three parameters for
    the view's own state: `MomentumProgressBlocks`, the `MomentumView.streakLine(for:)` text with
    its `RithamType.heading` + `numerals()` modifiers, `ShieldRow`, the
    `summary.recentSessions.isEmpty` empty-state block, the `PrimaryCTAButton(title: "Momentum")`,
    and the `else if loadFailed` branch rendering `OnboardingCopy.Errors.savingFailed` — all
    unchanged. The empty-state block moves for now and relocates in Task 2; keeping it here first
    is what makes this task a zero-behaviour-change move.

    Carry `momentumSection`'s existing doc comment onto the new type (the MOMENTUM-06 no-sharing
    note and the RECOVERY-01 invariant-3 note), and add a short note on why the file lives under
    `Momentum/Components/` rather than `Home/`, citing 04-RESEARCH.md Pitfall 2. Do not write the
    token `movementSnapshot` anywhere in this new file.

    In `HomeHubView.swift`, change only the `momentumSection` member: it becomes
    `@ViewBuilder private var momentumSection: some View { MomentumDashboardSection(summary:
    momentumSummary, loadFailed: momentumLoadFailed, flow: flow) }`. Leave the `MARK: - D-08's
    Momentum summary section` comment exactly where and as it is, immediately above it. Leave
    `momentumSection` as the struct's last member. Leave `body`, every `@State`, `.onAppear`, both
    `.sheet` modifiers, and the bottom `extension HomeHubView` untouched — the screen still renders
    the old button list after this task, and that is correct.

    Run `xcodegen generate` from inside `RithamApp/` and stage the regenerated
    `Ritham.xcodeproj/project.pbxproj` in the same commit.
  </action>

  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/MomentumContainerTouchingSuites/MovementSnapshotViewTests -only-testing:RithamTests/StepRegistryTouchingSuites/Phase3CoverageTests</automated>
  </verify>

  <acceptance_criteria>
    - `Scripts/build-app.sh build` succeeds.
    - The combined `-only-testing` run above passes with a non-zero test count, including
      `theSnapshotEntryIsNotAdjacentToTheMomentumSummary` and
      `noMomentumSurfaceOffersASharingAffordance`.
    - `Scripts/build-app.sh test` (full target) passes — this task changes no behaviour, so any
      failure is a real regression.
    - `grep -c "MARK: - D-08's Momentum summary section" RithamApp/Ritham/Home/HomeHubView.swift`
      prints exactly `1`.
    - `RithamApp/Ritham/Momentum/Components/MomentumDashboardSection.swift` exists and
      `git diff --stat` for this task lists it as added.
    - `HomeHubView.swift`'s diff for this task touches only the `momentumSection` member — `body`,
      the `@State` declarations, `.onAppear`, both `.sheet` modifiers and the bottom extension are
      unchanged.
  </acceptance_criteria>

  <done>
    The Momentum summary rendering lives under `Ritham/Momentum/Components/`, inside the no-sharing
    directory gate's walk; the app renders exactly as it did before; the test-coupled marker and
    extension boundary are intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite HomeHubView's body as the sectioned dashboard</name>

  <files>
    RithamApp/Ritham/Home/HomeHubView.swift,
    RithamApp/Ritham/Momentum/Components/MomentumDashboardSection.swift
  </files>

  <read_first>
    - `.planning/phases/04-household-home/04-UI-SPEC.md` — the whole document; "Layout & Section
      Contract", "Interaction / State Contract" and "Copywriting Contract" are the binding
      specification for this task
    - `.planning/phases/04-household-home/04-PATTERNS.md`, section
      "`RithamApp/Ritham/Home/HomeHubView.swift` (rewritten body)" and "Shared Patterns"
    - `.planning/phases/04-household-home/04-RESEARCH.md`, Pattern 2, Pattern 3 (including its
      "Required update" paragraph) and Pitfall 6
    - This plan's own `<test_coupled_constraints>` block above — re-read rules 2, 3 and 4 before
      writing a single line of the new body
    - `.planning/phases/04-household-home/04-01-SUMMARY.md` — the exact initialiser signatures of
      `RecommendationsSectionContent` and `DietPlanSectionContent`
    - `RithamApp/Ritham/Settings/ReScreenBanner.swift` — the only existing
      background-plus-clipShape precedent in the codebase
    - `RithamApp/Ritham/Momentum/MomentumSummary.swift` — `MomentumSessionEntry`'s members
      (`id`, `startedAt`, `title`, `verificationLabel`, `qualifies`)
  </read_first>

  <action>
    Rewrite `HomeHubView`'s header comment, `body`, and section members. Keep every `@State` this
    view already owns, add `@State private var recommendationsModel: RecommendationsModel?`, and
    keep the existing `.onAppear` store/reader block verbatim, extending it only to construct
    `recommendationsModel` once when it is `nil` (04-PATTERNS.md shows this exact addition). Per
    D-08's no-aggregate-view-model rule, each section keeps its own independently-loaded state —
    do not introduce a dashboard view model.

    Screen scaffold: keep `RithamScreen(surface: DecorativeSurface.boundedHeaderOnly, ...)`.
    Introduce `nonisolated static let dashboardHeadline = "Home"` on `HomeHubView` and pass it as
    the `headline:` argument, so a test can assert the copy without rendering. Pass no `bodyText:`
    argument at all — the placeholder-framing sentence is deleted outright, not reworded
    (04-UI-SPEC.md Copywriting Contract; 04-RESEARCH.md State of the Art marks it deprecated).
    Rewrite the type's own header comment as well: it currently describes the screen as a
    stop-gap, and 04-CONTEXT.md D-01 requires that framing gone in shape and in spirit.
    <!-- planner-discipline-allow: interim -->
    <!-- planner-discipline-allow: temporary -->
    No word in the file may describe this screen as interim, temporary or a placeholder.

    Add a private section-card helper on `HomeHubView`,
    `@ViewBuilder private func sectionCard<Content: View>(@ViewBuilder _ content: () -> Content)
    -> some View`, applying exactly 04-UI-SPEC.md's Dashboard Section Card values:
    a `VStack(alignment: .leading, spacing: RithamSpacing.md)` of the content, then
    `.padding(RithamSpacing.md)`, `.frame(maxWidth: .infinity, alignment: .leading)`,
    `.background(RithamColor.paper.opacity(0.06))`,
    `.clipShape(RoundedRectangle(cornerRadius: RithamSpacing.sm))`. Do not reuse
    `ReScreenBanner`'s solid-paper fill: four solid off-white slabs would invert the 60/30/10
    split. Every section below is wrapped in this helper, and the outer `VStack` keeps its existing
    `spacing: RithamSpacing.md`.

    Build `body` as one `VStack(alignment: .leading, spacing: RithamSpacing.md)` of six entries, in
    exactly this order (04-UI-SPEC.md fixes the order; it is not open arrangement discretion):

    1. `sectionCard { momentumSection }` — the Momentum card.
    2. `sectionCard { sleepSection }` — a heading-weight `Text(MomentumCopy.Sleep.headline)` plus a
       single `SecondaryCTAButton` opening `.sleepCheckIn`, carried over unchanged from today's
       hub per D-03. RECOVERY-01 invariant 3, which D-03 restates, is absolute here: no badge, dot,
       checkmark, count, "logged today" marker, or any state derived from sleep check-in history,
       on this card or any other.
    3. `sectionCard { exerciseSection }` — a `Text` section heading at `RithamType.heading`, then
       the recent-session rows, then the log actions, then the snapshot entry. Rows come from
       `momentumSummary?.recentSessions ?? []` — read through the `MomentumSummaryReader` result
       already loaded in `onAppear`, never re-derived from `HealthDataStore` a second way (D-02).
       Render each entry as its `title` at `RithamType.body` and, when `verificationLabel` is
       non-nil, that label at `RithamType.label` with `RithamType.fineprint()`. `.caption`,
       `.caption2` and `.footnote` are forbidden anywhere in this file — `RithamType.label` (16pt)
       is the floor, and de-emphasis is opacity, never a smaller point size. When
       `recentSessions` is empty, render the `MomentumCopy.Empty.noSessionsHeadline` /
       `.noSessionsBody` pair here instead of the rows, and delete that same block from
       `MomentumDashboardSection` so the empty state exists on exactly one card. (This is a
       deliberate departure from 04-PATTERNS.md's verbatim extraction sketch: 04-UI-SPEC.md
       section 1 enumerates the Momentum card as progress blocks, streak line and shield row, and
       section 3 assigns the no-sessions empty state to the exercise card. The approved UI contract
       governs; log the deviation in the SUMMARY.) Below the rows, place the four existing entry
       points unchanged in wording — `PrimaryCTAButton("Track cardio")` opening
       `.cardioActivityPicker`, `SecondaryCTAButton("Cardio history")` opening `.cardioHistory`,
       `PrimaryCTAButton("Log strength")` opening `.strengthSession`,
       `SecondaryCTAButton("Strength history")` opening `.strengthHistory` — and last, the existing
       `if HomeHubView.showsMovementSnapshotEntry(optIn: isMovementSnapshotEnabled)` block with its
       `SecondaryCTAButton("Daily Movement Snapshot")`. Placing the snapshot entry inside the
       exercise card, two cards below the Momentum card, is what satisfies 04-RESEARCH.md
       Pitfall 6's locked adjacency rule — this is a carried-forward Phase 3 constraint, not a
       layout preference.
    4. `sectionCard { workoutPlanSection }` — a `Text` heading at `RithamType.heading`, then
       `if let recommendationsModel { RecommendationsSectionContent(model: recommendationsModel,
       flow: flow) } else { ProgressView() }`. The section must NOT call `requestPlan` from
       `onAppear` or from any lifecycle hook: `RecommendationsModel.requestPlan` opens
       `.preAssessment` when the pre-assessment flag is false, so an auto-request would push the
       pre-assessment screen on top of home with no user action on a cold launch, and would hit the
       Go plan service on every launch. 04-UI-SPEC.md's state table specifies `.idle` plus the
       "Get my plan" control as the landing state; the user's tap is the only trigger.
    5. `sectionCard { dietPlanSection }` — a `Text` heading at `RithamType.heading`, then
       `DietPlanSectionContent(flow: flow)`. Nothing else: the food-allergy screening checkbox and
       its severity follow-up stay in the Settings-presented `DietPlanView` (D-05, 04-RESEARCH.md
       Pitfall 3).
    6. `overflowRow` — Guidance and Settings as one compact `HStack(spacing: RithamSpacing.md)` of
       two plain `Button`s labelled "Guidance" and "Settings", each `RithamType.body`,
       `RithamColor.paper`, `.frame(minHeight: RithamSpacing.minimumTapTarget)`, with
       `.accessibilityLabel` set to its title. Not a card, and specifically not two full-width
       `PrimaryCTAButton`/`SecondaryCTAButton` rows — a leftover button stack under real sections
       is the rejected pattern with extra steps (04-UI-SPEC.md section 6). "Guidance" opens
       `.guidance`; "Settings" sets `isPresentingSettings = true`. Both `.sheet` modifiers
       (`SettingsView` with its `onOpenHealthProfile` callback, and `HealthProfileView`) stay
       exactly as they are — the `SettingsView` sheet is the only surviving route to the
       food-allergy screening question, so losing it would silently delete a safety control.

    Declare `sleepSection`, `exerciseSection`, `workoutPlanSection`, `dietPlanSection`,
    `overflowRow` and `sectionCard` as members **above** the `MARK: - D-08's Momentum summary
    section` comment. `momentumSection` stays the struct's last member, immediately after that
    marker, and nothing between the marker and the closing brace may contain the token
    `movementSnapshot` — see this plan's `<test_coupled_constraints>` rules 2 through 5.

    Do not introduce `List`, `Form`, a second `NavigationStack`, a nested `ScrollView`, or a ring,
    arc or radial progress form of any kind. `RithamColor.volt` stays reserved for the header's
    static ring-and-dot ornament and must not appear in any section. Do not build a
    steps/calories card or a "coming soon" placeholder — D-08 says leave structural room and show
    nothing.

    Finally, in the bottom `extension HomeHubView`, remove `.recommendations` from
    `routingSteps(movementSnapshotOptIn:)`'s array: after this task nothing in `body` calls
    `flow.open(.recommendations)`, and leaving it in would make the function lie about what the
    screen does (04-RESEARCH.md Pattern 3, "Required update"). Change nothing else in that array.
    Amend the function's doc comment to say it enumerates the destinations `body` opens
    **directly**, noting that `.preAssessment` is reachable indirectly through the embedded
    workout-plan section's own model and is deliberately not listed. No new file is added by this
    task, so no `xcodegen` regeneration is required.
  </action>

  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/MomentumContainerTouchingSuites/MovementSnapshotViewTests -only-testing:RithamTests/HomeHubTests</automated>
  </verify>

  <acceptance_criteria>
    - `Scripts/build-app.sh build` succeeds.
    - The combined `-only-testing` run above passes with a non-zero test count. In particular
      `theSnapshotEntryIsNotAdjacentToTheMomentumSummary` passes — if it reports "could not locate
      the Momentum summary section marker" or "could not locate the end of HomeHubView's struct
      body", rules 2 through 5 of `<test_coupled_constraints>` have been broken.
    - `Scripts/build-app.sh test` (full target) passes.
    - `grep -c "MARK: - D-08's Momentum summary section" RithamApp/Ritham/Home/HomeHubView.swift`
      prints exactly `1`.
    - `HomeHubView.swift` references all three embedded section types by name:
      `MomentumDashboardSection`, `RecommendationsSectionContent`, `DietPlanSectionContent`.
    - `HomeHubView.swift` still references `SettingsView(` and `HealthProfileView(`, and
      `RithamApp/Ritham/Settings/SettingsView.swift` still references `DietPlanView(flow:` — the
      Settings route to the food-allergy screening question is intact.
    - `HomeHubView.routingSteps(movementSnapshotOptIn: true)` no longer contains `.recommendations`
      and still contains `.sleepCheckIn`, `.cardioActivityPicker`, `.cardioHistory`,
      `.strengthSession`, `.strengthHistory`, `.guidance` and `.movementSnapshot`.
    - No **non-comment** occurrence of `List(`, `List {`, `Form(`, `Form {`, `.caption`,
      `.footnote`, `NavigationStack` or `ScrollView` appears in `HomeHubView.swift`. Check this the
      way plan 04-03's gate will: drop every line whose trimmed form begins with `//`, then search
      the remainder. A whole-line comment explaining why this screen introduces no second scroll
      container is legitimate and must not trip the check — a raw file-wide search would make the
      explanation self-invalidating.
  </acceptance_criteria>

  <done>
    Opening the app lands on a scrollable stack of section cards showing Momentum progress, recent
    sessions with their verification labels, the sleep entry, the live workout plan and the diet
    controls — with the log/track actions living on the exercise card and Guidance/Settings reduced
    to a compact overflow row.
  </done>
</task>

<task type="auto">
  <name>Task 3: Dashboard derivation tests in HomeHubTests</name>

  <files>
    RithamApp/RithamTests/HomeHubTests.swift
  </files>

  <read_first>
    - `RithamApp/RithamTests/HomeHubTests.swift` (whole file — new tests append to this suite,
      following its existing `MARK:` sectioning convention)
    - `RithamApp/RithamTests/MovementSnapshotViewTests.swift`, the two `routingSteps` tests — the
      existing assertions this task must not contradict
    - `.planning/phases/04-household-home/04-UI-SPEC.md`, Copywriting Contract
  </read_first>

  <action>
    Append a `// MARK: - Plan 04-02: the dashboard's derivations` section to the existing
    `HomeHubTests` suite. Do not create a new suite and do not nest this one — `HomeHubTests` is
    deliberately flat, documented in its own header as never touching `StepRegistry`'s shared
    static state.

    Add four tests:
    1. `theDashboardNoLongerRoutesToTheRecommendationsScreen` — assert
       `!HomeHubView.routingSteps(movementSnapshotOptIn: true).contains(.recommendations)`, with a
       comment recording that the plan is now embedded inline (D-04) while `.recommendations`
       stays registered for `StepRegistry.unregisteredSteps` (04-RESEARCH.md Pitfall 4).
    2. `theDashboardStillRoutesToEveryTrackingDestination` — assert that the routing list for
       `movementSnapshotOptIn: true`, read as a `Set`, equals exactly `.sleepCheckIn`,
       `.cardioActivityPicker`, `.cardioHistory`, `.strengthSession`, `.strengthHistory`,
       `.guidance` and `.movementSnapshot`, and that `.movementSnapshot` is its last element. Pin
       membership, not the array literal's internal sequence: the ordering constraint this phase
       actually carries (04-RESEARCH.md Pitfall 6) is about render order inside `body`, not about
       this list, so asserting the literal's sequence would fail on a harmless future reorder.
       Say that in a comment on the test.
    3. `theDashboardHeadlineIsNotPlaceholderFraming` — assert
       `HomeHubView.dashboardHeadline == "Home"` and that its lowercased form contains none of
       "interim", "temporary" or "placeholder".
    4. `theExerciseSectionRendersTheSummarysOwnSessionList` — build an in-memory store using the
       suite's existing `makeStore()` helper, save one cardio session and one lift session, read a
       `MomentumSummary` through a `MomentumSummaryReader`, and assert `recentSessions` is
       non-empty and every entry's `verificationLabel` is either
       `MomentumCopy.Verification.sensorVerified`, `MomentumCopy.Verification.manuallyEntered`, or
       `nil` — proving the exercise card's data source is the summary reader's own already-labelled
       list (D-02) rather than a second derivation.
  </action>

  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/HomeHubTests</automated>
  </verify>

  <acceptance_criteria>
    - `Scripts/build-app.sh test -only-testing:RithamTests/HomeHubTests` passes and its Swift
      Testing summary reports at least 16 tests (the suite's 12 existing tests plus these 4) — not
      zero.
    - `Scripts/build-app.sh test -only-testing:RithamTests/MomentumContainerTouchingSuites/MovementSnapshotViewTests`
      still passes: test 2's ordered list must agree with
      `hubRoutingListOmitsSnapshotWhenOptInOff`'s `offSteps == onSteps.filter { $0 != .movementSnapshot }`
      expectation.
    - `Scripts/build-app.sh test` (full target) passes.
  </acceptance_criteria>

  <done>
    The dashboard's routing surface, headline copy and session-list data source are pinned by
    tests that run off the main actor without rendering the view, matching the codebase's
    established `nonisolated static func` derivation idiom.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| always-visible home screen -> on-device SwiftData store | A control embedded on the first screen of every launch writes persisted health data |
| home screen -> Go workout-plan service (`WorkoutPlanClient`) | An outbound network request originating from a screen the user sees on every launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Tampering (unintentional, self-inflicted) | `HomeHubView`'s diet-plan section | high | mitigate | The dashboard embeds `DietPlanSectionContent` only — never `DietPlanView` and never a re-created food-allergy checklist control — so no dashboard interaction can re-resolve or re-save a screening result against the empty `OnboardingFlow.answers.screening` a cold launch produces (04-RESEARCH.md Pitfall 3). Task 2's acceptance criteria pin the embedded symbol; plan 04-03's `Phase4CoverageTests` gates it structurally. |
| T-04-05 | Denial of Service / unintended egress | Embedded workout-plan section -> `WorkoutPlanClient` | medium | mitigate | The section is forbidden from calling `requestPlan` on appear or from any lifecycle hook; the landing state is `.idle` with an explicit "Get my plan" control, so no launch generates an unrequested network round trip and no launch pushes `.preAssessment` unprompted. |
| T-04-06 | Repudiation (loss of a safety control) | `SettingsView` sheet route to `DietPlanView` | medium | mitigate | The overflow-row restyle keeps `isPresentingSettings` and both `.sheet` modifiers; Task 2's acceptance criteria assert `HomeHubView.swift` still references `SettingsView(` and that `SettingsView.swift` still references `DietPlanView(flow:`. |
| T-04-SC | Tampering | npm/pip/cargo/SPM installs | low | accept | No package-manager install task exists in this phase; 04-RESEARCH.md's Package Legitimacy Audit records "Not applicable — this phase introduces no external packages". |

ASVS L1, blocking on High: T-04-02 is High and is mitigated within this plan (embedded-symbol
constraint plus acceptance criteria), with a durable structural gate landing in 04-03.
</threat_model>

<verification>
- `Scripts/build-app.sh build` succeeds.
- `Scripts/build-app.sh test` (full `RithamTests` target) passes, specifically including
  `MovementSnapshotViewTests` (both source-scan tests), `Phase3CoverageTests`,
  `PhaseCoverageTests`, `Phase2CoverageTests` and `HomeHubTests`.
- `RithamCore/Scripts/test-core.sh` passes — `RithamCore` is untouched by this plan.
- `xcodegen generate` in `RithamApp/` produces no further diff after Task 1's commit.
- The full suite is green at the end of Task 1 as well as at the end of the plan, so the
  extraction and the rewrite stay independently bisectable.
</verification>

<success_criteria>
- `HomeHubView` renders six ordered section entries — Momentum, sleep, exercise, workout plan,
  diet plan, overflow — each real section wrapped in the Dashboard Section Card treatment, with no
  vertical stack of full-width CTA rows anywhere on the screen.
- Every section shows real content above its action controls; no card is a heading plus a button.
- The Momentum rendering lives in `Ritham/Momentum/Components/MomentumDashboardSection.swift`.
- The Movement Snapshot entry renders inside the exercise card, two cards below the Momentum card.
- `routingSteps` no longer advertises `.recommendations`, and `.recommendations` is still
  registered.
- No placeholder framing copy, no `List`/`Form`, no second navigation container, no `.caption`/
  `.footnote`, no steps/calories card.
</success_criteria>

<output>
Create `.planning/phases/04-household-home/04-02-SUMMARY.md` when done. Record: the final member
order inside `HomeHubView` (so a future editor can see the marker constraint at a glance); the
deliberate relocation of the no-sessions empty state from the Momentum card to the exercise card
and why 04-UI-SPEC.md governs over 04-PATTERNS.md there; the chosen headline string; and anything
about the layout left to discretion (section heading wording, whether a session row shows a date)
so plan 04-03's UAT checkpoint can name it.
</output>
