---
phase: 04-household-home
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - RithamApp/RithamTests/Phase4CoverageTests.swift
  - RithamApp/Ritham.xcodeproj/project.pbxproj
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CROSSGEN-01]

must_haves:
  truths:
    - "A future edit that moves the Momentum summary rendering back out of Ritham/Momentum/ fails a test, instead of silently reducing the no-sharing gate to zero coverage."
    - "A future edit that replaces an inline dashboard section with a navigation button fails a test."
    - "A future edit that embeds the food-allergy screening control on the dashboard fails a test."
    - "Every OnboardingStep still resolves to a registered presenter, and .home resolves to HomeHubView."
    - "The whole RithamTests target and the whole RithamCore suite are green."
    - "A human has seen the dashboard render in the Simulator and confirmed it is the screen the product owner asked for."
  artifacts:
    - RithamApp/RithamTests/Phase4CoverageTests.swift
  key_links:
    - "Phase4CoverageTests -> Ritham/Momentum/Components/MomentumDashboardSection.swift on disk — the positive assertion that makes Phase3CoverageTests' directory walk non-vacuous for this phase."
    - "Phase4CoverageTests -> Ritham/Home/HomeHubView.swift source text — the D-01 compliance gate."
    - "StepBootstrap.registerAllSteps -> .home / .recommendations — registration completeness after the rewrite."
---

<objective>
Close Phase 4's first round: make the phase's structural guarantees durable as automated gates,
prove the full test surface is green, and get a human look at the dashboard the whole phase exists
to deliver.

Purpose: three of this phase's most important properties are structural rather than behavioural —
the Momentum section's physical file location, the dashboard's inline-not-navigational shape, and
the diet section's isolation from the screening write path. Each of them passes today because the
previous two plans put the code in the right place; none of them would fail tomorrow if someone
moved it back. This plan converts them from decisions into gates, following the
`Phase2CoverageTests`/`Phase3CoverageTests` precedent this project already established for exactly
this purpose.

Output: one new coverage suite, a green full-target run, updated ROADMAP/REQUIREMENTS status, and
a discharged human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-household-home/04-CONTEXT.md
@.planning/phases/04-household-home/04-RESEARCH.md
@.planning/phases/04-household-home/04-UI-SPEC.md
@.planning/phases/04-household-home/04-01-SUMMARY.md
@.planning/phases/04-household-home/04-02-SUMMARY.md

@RithamApp/RithamTests/Phase3CoverageTests.swift
@RithamApp/RithamTests/Phase2CoverageTests.swift
@RithamApp/RithamTests/StepRegistrySerialization.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase4CoverageTests — CROSSGEN-01's structural gates</name>

  <files>
    RithamApp/RithamTests/Phase4CoverageTests.swift (new),
    RithamApp/Ritham.xcodeproj/project.pbxproj
  </files>

  <read_first>
    - `RithamApp/RithamTests/Phase3CoverageTests.swift` (whole file) — the closest analog: same
      role, same nesting, same directory-walk technique, same non-vacuity guard. Copy its shape.
    - `RithamApp/RithamTests/Phase2CoverageTests.swift` — the sibling precedent for a
      per-phase "each step resolves to its real screen type" gate
    - `RithamApp/RithamTests/StepRegistrySerialization.swift` — why a registry-touching suite must
      be declared inside `extension StepRegistryTouchingSuites`
    - `.planning/phases/04-household-home/04-RESEARCH.md`, Pitfall 2 (including its "Warning
      signs" paragraph about the gate passing while covering nothing) and Pitfall 4
    - `.planning/phases/04-household-home/04-UI-SPEC.md`, "Layout & Section Contract" — the
      card-vs-button-list discriminator this suite encodes
    - `.planning/phases/04-household-home/04-02-SUMMARY.md` — the final member order and symbol
      names inside `HomeHubView`
  </read_first>

  <action>
    Create `RithamApp/RithamTests/Phase4CoverageTests.swift` declaring
    `extension StepRegistryTouchingSuites { @Suite("Phase4CoverageTests", .serialized) @MainActor
    struct Phase4CoverageTests { ... } }`, with an `init()` that calls `StepRegistry.reset()` then
    `StepBootstrap.registerAllSteps()`. The nesting is mandatory: this suite mutates
    `StepRegistry`'s shared static state, and STATE.md records that a per-suite `.serialized` trait
    alone does not order suites against each other — nesting inside the parent suite is the fix
    plan 02-16 landed after a ten-run verification, and `build-app.sh`'s
    `-parallel-testing-enabled NO` did not prevent the race on its own.

    Write a private `rithamDirectory` helper resolving `Ritham/` from this file's own `#filePath`
    via two `deletingLastPathComponent()` calls, and a private
    `nonCommentSource(of relativePath: String) throws -> String?` helper that reads a file under
    that directory, returns `nil` when it does not exist, and otherwise drops every line whose
    trimmed form begins with `//` — the same comment filter `Phase3CoverageTests` already uses, so
    a comment documenting a prohibition never trips the gate it documents.

    Write these tests.

    Registry completeness (Pitfall 4):
    1. `theHomeStepResolvesToTheDashboardScreen` — `StepRegistry.registeredPresenterType(for: .home)`
       is non-nil and equals `HomeHubView.self`.
    2. `theRecommendationsStepStaysRegisteredThoughNothingPushesToIt` —
       `registeredPresenterType(for: .recommendations)` equals `RecommendationsView.self`. Comment
       that the dashboard embeds the plan inline now, and that registration is required by
       `unregisteredSteps`-is-empty regardless of reachability.
    3. `theRegistryReportsNoUnregisteredStepsAfterBootstrap` — `StepRegistry.unregisteredSteps`
       is empty.

    Momentum relocation, the non-vacuity proof for 04-RESEARCH.md Pitfall 2:
    4. `theMomentumDashboardSectionLivesUnderTheMomentumFeatureDirectory` — assert
       `FileManager.default.fileExists(atPath:)` for
       `Ritham/Momentum/Components/MomentumDashboardSection.swift`, resolved from `#filePath`.
       This is the assertion that makes `Phase3CoverageTests.noMomentumSurfaceOffersASharingAffordance`
       genuinely cover the dashboard's Momentum rendering: that gate's only non-vacuity guard is
       "at least one file scanned", already satisfied by the three pre-existing components in that
       directory, so it would stay green if the file were moved to `Ritham/Home/`. Add a comment
       saying so, citing Pitfall 2, and citing this test as the durable replacement for that
       pitfall's suggested temporary-`ShareLink` manual check.

    Dashboard shape — CROSSGEN-01 / D-01 (all over `Ritham/Home/HomeHubView.swift`'s
    comment-filtered source; `Issue.record` and return if the file cannot be read, so a path
    mistake fails loudly instead of passing vacuously):
    5. `theDashboardHostsEverySectionInline` — the source contains `MomentumDashboardSection`,
       `RecommendationsSectionContent` and `DietPlanSectionContent`. This is the mechanical form
       of "sections visible on the home screen itself, not one tap deeper".
    6. `theDashboardCarriesNoPlaceholderFraming` — the lowercased source contains none of
       "interim", "temporary" or "placeholder".
    7. `theDashboardIntroducesNoListOrForm` — the source contains none of `List {`, `List(`,
       `Form {` or `Form(`. The codebase has zero `List`/`Form` usage and 04-RESEARCH.md's
       Alternatives Considered rules them out here.
    8. `theDashboardHoldsNoSubLabelTypography` — the source contains neither `.caption` nor
       `.footnote`. 04-UI-SPEC.md makes `RithamType.label` (16pt) the hard floor and names a small
       grey timestamp under each session row as the most natural-looking mistake on a dense
       dashboard.
    9. `theDashboardIntroducesNoSecondNavigationContainer` — the source contains neither
       `NavigationStack` nor `NavigationView` nor `ScrollView` (CROSSGEN-05, and
       04-RESEARCH.md's nested-scaffold anti-pattern; `RithamScreen` supplies the one `ScrollView`
       this screen needs).

    Safety-control reachability (the regression D-05's narrowing would otherwise permit):
    10. `theSettingsRouteToTheScreeningQuestionSurvives` — `HomeHubView.swift`'s source contains
        `SettingsView(`, and `Ritham/Settings/SettingsView.swift`'s source contains
        `DietPlanView(flow:`. Comment that D-05's narrowing is only safe while the Settings route
        to the food-allergy screening question exists.

    No-sharing coverage extension (closes the other half of Pitfall 2's gap):
    11. `noHomeSurfaceOffersASharingAffordance` — walk `Ritham/Home/` recursively for `.swift`
        files, assert the scanned count is greater than zero, and assert no file's
        comment-filtered source contains `ShareLink`, `UIActivityViewController` or
        `UIPasteboard`. `Phase3CoverageTests` walks only `Momentum/` and `MovementSnapshot/`;
        the home directory has never been covered even though it has rendered Momentum state since
        Phase 3. Do not edit `Phase3CoverageTests.swift` to add the directory — that file is
        Phase 3's, and 04-RESEARCH.md's assumption A2 prefers not touching it.

    Run `xcodegen generate` from inside `RithamApp/` and stage the regenerated
    `Ritham.xcodeproj/project.pbxproj` in the same commit.
  </action>

  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/StepRegistryTouchingSuites/Phase4CoverageTests</automated>
  </verify>

  <acceptance_criteria>
    - `Scripts/build-app.sh test -only-testing:RithamTests/StepRegistryTouchingSuites/Phase4CoverageTests`
      passes and its Swift Testing summary reports exactly 11 tests — not zero, since a
      `-only-testing` filter that matches nothing exits 0 with zero tests (03-07-SUMMARY.md's
      filter-string mismatch is the precedent).
    - The suite is declared inside `extension StepRegistryTouchingSuites`.
    - `Scripts/build-app.sh test -only-testing:RithamTests/StepRegistryTouchingSuites/PhaseCoverageTests -only-testing:RithamTests/StepRegistryTouchingSuites/Phase2CoverageTests -only-testing:RithamTests/StepRegistryTouchingSuites/Phase3CoverageTests`
      still passes — adding a fourth registry-touching suite to the serialized subtree broke none
      of the existing three.
    - Negative controls, each run once by hand, reverted, and recorded in the SUMMARY: moving
      `MomentumDashboardSection.swift` to `Ritham/Home/` fails test 4 (and, tellingly, leaves
      `Phase3CoverageTests` green — record that observation, it is the whole point of test 4);
      deleting the `DietPlanSectionContent` reference from `HomeHubView.swift` fails test 5.
    - `git status --short RithamApp/Ritham.xcodeproj/project.pbxproj` shows the regenerated project
      file staged alongside the new test file.
  </acceptance_criteria>

  <done>
    Phase 4's structural guarantees are automated gates with proven negative controls, following
    the same per-phase coverage-suite pattern Phases 2 and 3 already established.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green and roadmap/requirements status update</name>

  <files>
    RithamApp/RithamTests/Phase4CoverageTests.swift,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>

  <read_first>
    - `.planning/ROADMAP.md`, the Phase 4 block (its criterion 1 text and the dated 2026-09-08
      annotations on criteria 1 and 2/3)
    - `.planning/REQUIREMENTS.md`, the CROSSGEN-01 entry and the requirement/phase status table
    - `.planning/STATE.md`, Blockers/Concerns — specifically the pre-existing 1-in-17
      `PersistenceTests.makeContext()` `ModelContainer` flake, so a single occurrence of it is
      recognised as the known pre-existing issue rather than misdiagnosed as this phase's
      regression
    - `.planning/phases/03-momentum-recovery/03-10-SUMMARY.md` — the Phase 3 close-out precedent
      for what a phase-completion doc update looks like here
  </read_first>

  <action>
    Run the whole test surface and settle it green:
    `Scripts/build-app.sh test` for the full `RithamTests` target, and
    `RithamCore/Scripts/test-core.sh` for the package. Fix any failure this phase introduced. If
    `PersistenceTests.makeContext()` crashes, re-run once and check STATE.md's Blockers/Concerns
    before treating it as a regression — it is a documented, pre-existing, low-frequency SwiftData
    flake with its own untriaged entry, and it is explicitly out of this phase's scope to fix.
    Record which it was in the SUMMARY either way.

    Then update the planning docs, using scoped edits only — never rewrite either file wholesale:
    - `.planning/ROADMAP.md`: mark Phase 4's criterion 1 as delivered by this round, set the
      Phase 4 `**Plans**` line to `3/3 plans complete` with the three plan filenames and their
      one-line objectives, and tick them. Leave the existing dated 2026-09-08 annotations on
      criterion 1 and on criteria 2/3 exactly as they are — they are the deferral record. Update
      the Progress table's Phase 4 row. Do not mark the Phase 4 checkbox in the top-level phase
      list as complete: criteria 2 and 3 (HOUSEHOLD-01, CROSSGEN-04) are deferred to a later
      Phase 4 round, so the phase itself is not finished. Add a short dated note on the Phase 4
      block recording that round 1 shipped CROSSGEN-01's dashboard and that HOUSEHOLD-01/
      CROSSGEN-04 remain open, mirroring Phase 2 criterion 6's and Phase 3 criterion 5's existing
      annotation style.
    - `.planning/REQUIREMENTS.md`: tick CROSSGEN-01 and set its row in the requirement/phase status
      table to Complete. Leave HOUSEHOLD-01 and CROSSGEN-04 Pending.
  </action>

  <verify>
    <automated>Scripts/build-app.sh test</automated>
  </verify>

  <acceptance_criteria>
    - `Scripts/build-app.sh test` (full `RithamTests` target, no `-only-testing` filter) passes.
    - `RithamCore/Scripts/test-core.sh` passes.
    - `Scripts/build-app.sh build` succeeds.
    - `.planning/ROADMAP.md` shows Phase 4 at `3/3 plans complete` with all three plan lines
      ticked, its Progress-table row updated, and both dated 2026-09-08 annotations still present
      verbatim.
    - `.planning/REQUIREMENTS.md` shows CROSSGEN-01 ticked and Complete; HOUSEHOLD-01 and
      CROSSGEN-04 still Pending.
    - The Phase 4 entry in ROADMAP.md's top-level phase list is still unticked, with a dated note
      stating why.
  </acceptance_criteria>

  <done>
    Every automated check in the repository is green, and the planning documents record exactly
    what this round shipped and what is still owed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the dashboard in the Simulator</name>

  <files>
    (no files changed — verification only; any fix lands as a follow-up edit to
    RithamApp/Ritham/Home/HomeHubView.swift)
  </files>

  <read_first>
    - `.planning/phases/04-household-home/04-02-SUMMARY.md` — the discretionary choices (section
      heading wording, headline string, whether a session row shows a date) this checkpoint should
      put in front of the product owner
    - `.planning/phases/04-household-home/04-UI-SPEC.md`, "Layout & Section Contract" — the
      card-vs-button-list discriminator step 3 below is checking
    - `.planning/STATE.md`, Session Continuity — why device and AX3/AX5 checks are out of scope here
  </read_first>

  <action>
    Pause and hand the running app to the product owner using the steps below. Do not self-approve
    this checkpoint and do not proceed to the SUMMARY until an explicit verdict comes back. If the
    verdict names a problem, fix it, re-run `Scripts/build-app.sh test`, and re-present.
  </action>

  <what-built>
    `HomeHubView` is now a sectioned dashboard instead of a vertical button list. On launch, the
    home screen shows, top to bottom: a Momentum card (weekly progress blocks, streak line, shield
    row, "Momentum" button); a sleep card ("How did you sleep?" plus its button); an exercise card
    (recent logged sessions with their "Sensor-verified"/"Manually entered" labels, or the
    no-sessions empty state, then Track cardio / Cardio history / Log strength / Strength history,
    then the Daily Movement Snapshot entry when that opt-in is on); a workout-plan card with the
    Recommendations content embedded live; a diet-plan card with the dietary-pattern and allergen
    pickers embedded live; and a compact Guidance / Settings row. Every card is a low-opacity tint
    on charcoal with 16pt padding and an 8pt corner radius. Automated coverage is green, including
    the two source-scan gates that constrain this file.

    What is deliberately absent: steps and calories (D-08 — no data source exists in the codebase),
    household grouping and the visibility spectrum (HOUSEHOLD-01/CROSSGEN-04, deferred to a later
    Phase 4 round), and the food-allergy screening checkbox on the dashboard (D-05 — it stays in
    Settings, where its screening re-resolve has live flow state).
  </what-built>

  <how-to-verify>
    Scope: Simulator only. Do not attempt a physical-device run or an AX3/AX5 accessibility pass —
    both stay in the end-of-project batch verification pass the product owner scheduled on
    2026-09-06 (see STATE.md and commit `e62edcc`).

    1. Build and launch: run `Scripts/build-app.sh build`, then open the project in Xcode and run
       the `Ritham` scheme on any available iPhone simulator. (STATE.md records that no
       touch-injection tool is available in this environment, so this step is a human one.)
    2. Complete or re-enter onboarding if the simulator has no profile, until you land on the home
       screen.
    3. Confirm the home screen is a stack of grouped section cards, not a column of full-width
       buttons. Specifically confirm no card is just a heading with a single button under it —
       every card should show real content (progress blocks, session rows, plan rows, or a picker)
       above any action control.
    4. Confirm the screen headline reads as a real home screen and that no text anywhere on it
       describes the screen as temporary or a work in progress.
    5. Confirm the sleep card shows no badge, dot, checkmark or "you haven't logged sleep" nudge —
       before or after logging a check-in. Log one, return home, and confirm the card looks
       identical.
    6. Tap "Get my plan" on the workout-plan card and confirm the plan (or the pre-assessment, on a
       first run) appears. Then force-quit and relaunch, and confirm the plan card sits idle
       showing "Get my plan" — it must not fetch a plan or jump to the pre-assessment on its own.
    7. On the diet card, confirm the dietary-pattern picker shows the value you previously saved
       (set one in Settings > Diet plan first, then force-quit and relaunch home to check). Change
       it on the dashboard, then open Settings > Health profile and confirm every condition tag you
       had before is still listed — this is 04-RESEARCH.md Pitfall 3's own before/after check.
    8. Confirm the Daily Movement Snapshot entry is inside the exercise card (when its Settings
       opt-in is on), not next to the Momentum card, and that it disappears entirely when the
       opt-in is off.
    9. Confirm Settings opens from the compact overflow row, and that Settings > Diet plan still
       shows the food-allergy checkbox and its severity follow-up.
    10. Report anything that looks wrong, including purely visual judgements — section order,
        heading wording, card spacing and whether a session row wants a date were all left to
        implementation discretion and are the right things to push back on now.
  </how-to-verify>

  <resume-signal>
    Type "approved" to close Phase 4 round 1, or describe what looks wrong and it will be fixed
    before the phase closes.
  </resume-signal>

  <verify>
    <automated>Scripts/build-app.sh build</automated>
    <human-check>The product owner has walked steps 1-10 above in the Simulator and returned an
    explicit verdict.</human-check>
  </verify>

  <done>
    The product owner has confirmed the dashboard is the screen they asked for during Phase 3
    sign-off, or their findings have been implemented and re-presented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| structural test gate -> the source files it claims to cover | A gate that resolves the wrong path, or scans a set that no longer includes the file of interest, reports safety it is not providing |
| shared `StepRegistry` static state -> concurrently running test suites | A new registry-touching suite can reintroduce a cross-suite race that produces false green/red results |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering (false assurance) | `Phase3CoverageTests.noMomentumSurfaceOffersASharingAffordance` coverage of the dashboard's Momentum rendering | medium | mitigate | Task 1 test 4 asserts `Ritham/Momentum/Components/MomentumDashboardSection.swift` exists on disk, so relocating it fails a test rather than silently reducing the directory walk to pre-existing files; Task 1 test 11 extends the same no-sharing walk to `Ritham/Home/`, which has never been covered. Both are proven by a recorded negative control, per 04-RESEARCH.md Pitfall 2's "Warning signs". |
| T-04-07 | Tampering (nondeterministic verification) | `Phase4CoverageTests`' mutation of `StepRegistry` shared static state | medium | mitigate | The suite is declared inside `extension StepRegistryTouchingSuites`, the cross-suite serialization mechanism plan 02-16 landed after a ten-run verification; acceptance criteria re-run the three existing registry-touching coverage suites to confirm the enlarged subtree is still stable. |
| T-04-01 | Tampering (unintentional, self-inflicted) | Diet write path on an always-visible screen | high | mitigate | Inherited from 04-01/04-02 and now gated durably: `DietSectionIsolationTests` (04-01) covers the write path, and Task 1 test 10 asserts the Settings route to the screening question survives. Re-verified green by Task 2's full-target run. |
| T-04-SC | Tampering | npm/pip/cargo/SPM installs | low | accept | No package-manager install task exists in this phase; 04-RESEARCH.md's Package Legitimacy Audit records "Not applicable — this phase introduces no external packages". |

ASVS L1, blocking on High: T-04-01 is High, mitigated across 04-01/04-02 and confirmed green here;
no High-severity threat is left undischarged at phase close.
</threat_model>

<verification>
- `Scripts/build-app.sh build` succeeds.
- `Scripts/build-app.sh test` (full `RithamTests` target) passes, including `Phase4CoverageTests`,
  `DietSectionIsolationTests`, `HomeHubTests`, `MovementSnapshotViewTests`, `Phase3CoverageTests`,
  `Phase2CoverageTests` and `PhaseCoverageTests`.
- `RithamCore/Scripts/test-core.sh` passes.
- Both of Task 1's negative controls have been run, observed to fail, and reverted, with the
  results recorded in the SUMMARY.
- The human verification checkpoint is discharged with an explicit "approved", or its findings are
  fixed and the checkpoint re-run.
</verification>

<success_criteria>
- `Phase4CoverageTests` exists, is nested in `StepRegistryTouchingSuites`, reports 11 tests, and
  covers registration completeness, Momentum file location, dashboard inline-section shape,
  forbidden layout/typography primitives, Settings-route survival, and home-directory no-sharing.
- The whole repository's automated checks are green.
- ROADMAP.md and REQUIREMENTS.md record CROSSGEN-01 as complete and HOUSEHOLD-01/CROSSGEN-04 as
  still open, with the Phase 4 phase checkbox correctly left unticked.
- A human has confirmed the dashboard in the Simulator, or their findings have been addressed.
</success_criteria>

<output>
Create `.planning/phases/04-household-home/04-03-SUMMARY.md` when done. Record: both negative-control
results from Task 1 (especially that `Phase3CoverageTests` stays green while
`MomentumDashboardSection.swift` sits in the wrong directory — that observation is the justification
for test 4 existing); whether the `PersistenceTests.makeContext()` flake appeared during the
full-target run; the human checkpoint's verdict and any discretionary layout feedback it produced;
and an explicit statement that HOUSEHOLD-01 and CROSSGEN-04 remain open for a later Phase 4 round,
so the next `/gsd-progress` picks them up rather than treating Phase 4 as finished.
</output>
