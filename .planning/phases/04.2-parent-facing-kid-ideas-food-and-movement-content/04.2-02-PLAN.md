---
phase: 04.2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - RithamCore/Sources/RithamCore/Copy/KidContentCopy.swift
  - RithamCore/Tests/RithamCoreTests/KidContentCopyTests.swift
autonomous: true
requirements: [KIDCONTENT-01]

must_haves:
  truths:
    - "Every locked string the Kid Ideas screen shows lives in one reviewable RithamCore file, not scattered across view code"
    - "The screen states that the ideas are reproduced from published USDA and CDC sources and are not an individualized plan for a child"
    - "The screen states that Ritham stores nothing about a specific child beyond the entry a parent creates"
    - "No shipped string asks for or names a child's age, weight, growth, height, or birthday"
    - "No shipped string offers to save, favourite, or mark an idea"
  artifacts:
    - path: "RithamCore/Sources/RithamCore/Copy/KidContentCopy.swift"
      provides: "KidContentCopy.Screen and KidContentCopy.Children locked strings"
      min_lines: 70
      contains: "public enum KidContentCopy"
    - path: "RithamCore/Tests/RithamCoreTests/KidContentCopyTests.swift"
      provides: "Lexicon sweep, house-style dash gate, and locked-value assertions"
      min_lines: 80
      contains: "KidContentCopyTests"
  key_links:
    - from: "docs/kid-content.md"
      to: "RithamCore/Sources/RithamCore/Copy/KidContentCopy.swift"
      via: "section 6's disclaimer transcribed into Screen.disclaimer"
      pattern: "disclaimer"
    - from: "RithamCore/Sources/RithamCore/Copy/KidContentCopy.swift"
      to: "RithamCore/Tests/RithamCoreTests/KidContentCopyTests.swift"
      via: "the test's shippedStrings table names every constant the namespace exposes"
      pattern: "KidContentCopy\\."
---

<objective>
Build `KidContentCopy` in `RithamCore` -- the locked, reviewable product strings the Kid Ideas
screen renders around the catalog content: screen headline, the two section titles for the one
combined food-and-movement screen (D-05), the section 6 disclaimer transcribed from
`docs/kid-content.md` (D-01, D-02), and the child-entry management strings KIDCONTENT-02 needs
(D-07) written so they never ask for a name, an age, or anything about a specific child.

Purpose: every prior phase puts its locked, verbatim product strings in
`RithamCore/Sources/RithamCore/Copy/`, and this phase gets no exception -- one file a clinician or
counsel can review at Phase 5 (LAUNCH-02/LAUNCH-03) instead of wording spread across view code.
This plan is independent of plan 04.2-01: it touches different files and imports nothing the
catalog declares, so the two run in parallel in wave 1.

Output: `KidContentCopy.swift` plus `KidContentCopyTests.swift`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-CONTEXT.md
@.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-RESEARCH.md
@docs/kid-content.md
@RithamCore/Sources/RithamCore/Copy/ScreeningCopy.swift
</context>

## Artifacts this phase produces (this plan's share)

New file `RithamCore/Sources/RithamCore/Copy/KidContentCopy.swift`, declaring:

| Symbol | Kind |
|--------|------|
| `KidContentCopy` | `public enum` namespace |
| `KidContentCopy.Screen` | nested `public enum` namespace |
| `KidContentCopy.Screen.headline` | `public static let String` |
| `KidContentCopy.Screen.intro` | `public static let String` |
| `KidContentCopy.Screen.foodSectionTitle` | `public static let String` |
| `KidContentCopy.Screen.movementSectionTitle` | `public static let String` |
| `KidContentCopy.Screen.disclaimer` | `public static let String` |
| `KidContentCopy.Screen.doneCTA` | `public static let String` |
| `KidContentCopy.Screen.sourceLine(attribution:)` | `public static func (String) -> String` |
| `KidContentCopy.Children` | nested `public enum` namespace |
| `KidContentCopy.Children.sectionTitle` | `public static let String` |
| `KidContentCopy.Children.explainer` | `public static let String` |
| `KidContentCopy.Children.emptyState` | `public static let String` |
| `KidContentCopy.Children.addCTA` | `public static let String` |
| `KidContentCopy.Children.ordinalLabel(_:)` | `public static func (Int) -> String` |
| `KidContentCopy.Children.nicknameFieldLabel` | `public static let String` |
| `KidContentCopy.Children.renameCTA` | `public static let String` |
| `KidContentCopy.Children.saveCTA` | `public static let String` |
| `KidContentCopy.Children.cancelCTA` | `public static let String` |
| `KidContentCopy.Children.deleteCTA` | `public static let String` |
| `KidContentCopy.Children.deleteConfirmation` | `public static let String` |
| `KidContentCopy.Children.deleteAccessibilityLabel(for:)` | `public static func (String) -> String` |
| `KidContentCopy.Children.saveFailed` | `public static let String` |

New file `RithamCore/Tests/RithamCoreTests/KidContentCopyTests.swift`, declaring suite
`KidContentCopyTests` (Swift Testing, no `ModelContainer`, no `StepRegistry`, nests under no
parent suite).

<tasks>

<task type="auto">
  <name>Task 1: Write KidContentCopy with the locked screen and child-entry strings</name>

  <read_first>
    - `RithamCore/Sources/RithamCore/Copy/ScreeningCopy.swift` -- read its header comment and its
      nested-namespace layout. That header states which source document its strings came from and
      that Unicode em and en dashes in the source prose are rewritten to plain punctuation; this
      new file's header must make the equivalent statements for its own source.
    - `RithamCore/Sources/RithamCore/Copy/SocialCopy.swift` -- the most recent example of this
      convention, including how a string carrying interpolation is exposed as a static function
      rather than a format string.
    - `docs/kid-content.md` section 6 -- the disclaimer's source text.
    - `.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-CONTEXT.md` --
      the Claude's Discretion paragraph on multi-child labelling, which this file's `Children`
      strings implement: ordinal slots with an optional short nickname, never a required name.
  </read_first>

  <files>RithamCore/Sources/RithamCore/Copy/KidContentCopy.swift</files>

  <action>
    Create `KidContentCopy.swift` declaring `public enum KidContentCopy` with the two nested
    namespaces and every constant in this plan's Artifacts table.

    File header comment must state: (a) these are locked product strings for the Kid Ideas screen,
    following this directory's existing convention; (b) `Screen.disclaimer` is transcribed from
    `docs/kid-content.md` section 6 with one documented change -- the source sentence's trailing
    parenthetical naming two internal requirement identifiers is dropped, because a requirement ID
    is an internal planning reference and must never appear on a user-facing screen; (c) per D-03
    no string here names a calorie, portion, serving size, BMI, or weight-management figure, and no
    string asks the parent for a child's age, weight, height, growth, or birthday; (d) per D-09 no
    string offers to save, favourite, or mark an idea, because the screen is read-only by design;
    (e) the no-em-dash and no-en-dash house style this directory enforces applies here and is
    asserted by `KidContentCopyTests`.

    Values, exactly:

    `Screen.headline` -- `"Kid Ideas"`.

    `Screen.intro` -- `"General food and movement ideas for a preteen, reproduced from published
    USDA and CDC guidance."` Ritham-authored screen chrome, not source text; its doc comment must
    say so.

    `Screen.foodSectionTitle` -- `"Food ideas"`. `Screen.movementSectionTitle` -- `"Movement
    ideas"`. Both are group headings inside ONE screen (D-05): the file must not declare anything
    suggesting two separate screens or a second entry point.

    `Screen.disclaimer` -- the three sentences of `docs/kid-content.md` section 6, transcribed
    verbatim, with the final sentence's trailing parenthetical cross-reference removed so the
    sentence ends after the words "final release". The word "above" in the second sentence stays
    as published; plan 04.2-04 renders this string below the idea lists so that reading is correct.

    `Screen.doneCTA` -- `"Done"`, matching every sibling Settings sub-screen's explicit dismissal
    action.

    `Screen.sourceLine(attribution:)` -- takes the assembled attribution string plan 04.2-01's
    `KidContentCitation.attribution` produces and returns `"Source: "` prefixed to it. Pure
    interpolation, no branching. Declared here so the citation label's wording is reviewable
    alongside the disclaimer rather than living inline in the view.

    `Children.sectionTitle` -- `"Your children"`.

    `Children.explainer` -- `"Ritham stores nothing about your child except this entry. No age, no
    weight, no growth, and no activity history."` This is the one string in this file that names
    those four words, and it names them to negate them. Its doc comment must record that
    `KidContentCopyTests` treats it as the single named exception to the banned-term sweep and
    asserts positively that it still contains them, so the exception can never silently widen to a
    second constant.

    `Children.emptyState` -- `"No children added yet."`

    `Children.addCTA` -- `"Add a child"`.

    `Children.ordinalLabel(_ position:)` -- returns `"Child "` interpolated with the position,
    producing `"Child 1"` for position 1. The position is the entry's one-based place in
    creation order; this function does no clamping and no zero handling, and its doc comment must
    say callers pass a one-based position.

    `Children.nicknameFieldLabel` -- `"Nickname (optional)"`. Its doc comment must record that a
    required name field is explicitly forbidden here (04.2-RESEARCH.md Pitfall 2): the word
    "optional" is load-bearing, and changing this label to a bare name prompt is a design decision
    needing user confirmation, not a copy tweak.

    `Children.renameCTA` -- `"Rename"`. `Children.saveCTA` -- `"Save"`. `Children.cancelCTA` --
    `"Cancel"`. `Children.deleteCTA` -- `"Delete"`. These four mirror `PrivacyZonesView`'s existing
    row controls so the two list screens read identically.

    `Children.deleteConfirmation` -- `"Remove this child entry?"`

    `Children.deleteAccessibilityLabel(for label:)` -- returns `"Delete "` interpolated with the
    passed row label, mirroring `PrivacyZonesView`'s existing per-row delete accessibility label.

    `Children.saveFailed` -- `"That change could not be saved. Try again."`

    Authoring constraint that applies to every string in this file except `Children.explainer`: the
    test in Task 2 sweeps case-insensitively for the substrings that name a child's body or
    identity, and `age` is one of them. That means no other string here may contain a word
    containing those letters -- avoid storage, manage, message, page, average, and any similar
    word. `Children.sectionTitle` is `"Your children"` rather than a manage-phrased alternative for
    exactly this reason.
  </action>

  <verify>
    <automated>cd RithamCore && swift build && grep -c 'public static' Sources/RithamCore/Copy/KidContentCopy.swift</automated>
  </verify>

  <acceptance_criteria>
    - `cd RithamCore && swift build` completes with no error.
    - `grep -c 'public static' RithamCore/Sources/RithamCore/Copy/KidContentCopy.swift` outputs at
      least `20`.
    - `grep -c 'LAUNCH-02\|LAUNCH-03' RithamCore/Sources/RithamCore/Copy/KidContentCopy.swift`
      counted over non-comment lines only outputs `0` -- an internal requirement identifier may be
      discussed in this file's header comment but must never sit inside a shipped string value.
      Check with `grep -v '^\s*//' <file> | grep -c 'LAUNCH-0'`.
    - `KidContentCopy.Screen.headline == "Kid Ideas"` and
      `KidContentCopy.Children.nicknameFieldLabel == "Nickname (optional)"`, both confirmed by
      Task 2's tests.
    - The file declares exactly two nested namespaces, `Screen` and `Children`, and no third.
  </acceptance_criteria>

  <done>
    `KidContentCopy.swift` compiles and exposes every screen and child-entry string plan 04.2-04
    renders, with the section 6 disclaimer transcribed and the internal requirement reference
    stripped.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add KidContentCopyTests with the lexicon sweep, dash gate, and locked-value pins</name>

  <read_first>
    - `RithamCore/Tests/RithamCoreTests/SocialCopyTests.swift` -- the file to mirror structurally:
      its `shippedStrings` tuple table calling every constant and every function with representative
      arguments, its non-vacuity count test, its em/en dash test, and its single named-exception
      test (the exclamation point allowed only in one locked constant). That exception test is the
      exact shape `Children.explainer` needs here.
    - `RithamCore/Tests/RithamCoreTests/ScreeningCopyTests.swift` -- the locked-value assertion
      style used for copy that came from a source document.
    - `RithamCore/Sources/RithamCore/Copy/KidContentCopy.swift` -- the file as Task 1 left it.
  </read_first>

  <files>RithamCore/Tests/RithamCoreTests/KidContentCopyTests.swift</files>

  <action>
    Create `KidContentCopyTests.swift` as `@Suite("KidContentCopyTests") struct KidContentCopyTests`
    importing `Foundation`, `Testing`, and `@testable import RithamCore`. It creates no
    `ModelContainer` and touches no `StepRegistry`, so it nests under no parent suite.

    Declare `private static let shippedStrings: [(name: String, value: String)]` listing every
    constant `KidContentCopy` exposes and every function called with a representative argument:
    `Screen.sourceLine(attribution:)` with a realistic citation attribution,
    `Children.ordinalLabel(_:)` with `1`, and `Children.deleteAccessibilityLabel(for:)` with
    `"Child 1"`. Name each entry by its dotted path, as `SocialCopyTests` does. Add a header
    comment recording that Swift offers no reflection over an enum namespace's static members, so a
    constant added to `KidContentCopy` without being added here is caught only by the count
    assertion below.

    Declare `private static let bannedTerms: [String]` containing exactly: `calorie`, `calories`,
    `bmi`, `weight loss`, `weight-loss`, `lose weight`, `healthy weight`, `portion`,
    `serving size`, `age`, `weight`, `growth`, `height`, `birthday`, `birth date`, `favorite`,
    `favourite`, `bookmark`, `streak`, `track`. Above it, a comment recording two things: that the
    four body-and-identity terms are banned because D-07 and KIDCONTENT-02 forbid this feature from
    asking for or storing anything about a specific child, and that `age` being a bare substring
    means no shipped string may contain storage, manage, message, page, or average either.

    Tests to write:

    1. Non-vacuity: `shippedStrings.count >= 18`, with a failure message saying the sweeps below
      cannot be trusted until the table is populated.
    2. No shipped string is empty or whitespace-only after trimming.
    3. Banned-term sweep with one named exception: for every entry whose `name` is not
      `"Children.explainer"`, assert the lowercased value contains none of `bannedTerms`, reporting
      the constant name and the offending term on failure.
    4. Exception integrity: assert positively that `KidContentCopy.Children.explainer` DOES contain
      each of `age`, `weight`, and `growth`, with a failure message saying the exception exists
      solely because that one string names those words in order to negate them, and that if the
      string no longer negates them the exception must be deleted rather than kept. This mirrors
      `SocialCopyTests.exclamationPointOnlyInLockedCompletionConfirmation`.
    5. House style: no shipped string contains an em dash (`\u{2014}`) or an en dash (`\u{2013}`).
      This is the copy half of 04.2-VALIDATION.md row TBD-02; plan 04.2-01's
      `KidContentCatalogTests` covers the catalog half, deliberately kept in a separate suite so
      the two plans stay independent in wave 1.
    6. Locked values, asserted as full-string equality: `Screen.headline`,
      `Children.nicknameFieldLabel`, `Children.addCTA`, `Children.emptyState`, and
      `Children.deleteConfirmation`.
    7. Disclaimer integrity, its own test: assert `Screen.disclaimer` contains the phrase
      `not an individualized meal or exercise plan`, contains `not written by Ritham`, contains
      `pending clinical and legal review`, and does NOT contain the substring `LAUNCH-0`. The last
      assertion is the gate against an internal requirement identifier reaching a user-facing
      screen.
    8. Interpolation: `Children.ordinalLabel(1) == "Child 1"` and
      `Children.ordinalLabel(2) == "Child 2"`, and `Screen.sourceLine(attribution: "X")` begins
      with `"Source: "`.
  </action>

  <verify>
    <automated>cd RithamCore && swift test --filter KidContentCopyTests</automated>
  </verify>

  <acceptance_criteria>
    - `cd RithamCore && swift test --filter KidContentCopyTests` exits zero with every test passing
      and at least eight tests reported.
    - `cd RithamCore && swift test` exits zero -- no pre-existing RithamCore test regressed.
    - Temporarily changing `Children.sectionTitle` to a value containing the word `manage` makes the
      banned-term sweep fail; revert after checking. This proves the substring sweep is live.
    - Temporarily changing `Children.explainer` to a value that no longer names the three negated
      words makes the exception-integrity test fail; revert after checking.
    - `grep -c 'ModelContainer\|StepRegistry'
      RithamCore/Tests/RithamCoreTests/KidContentCopyTests.swift` outputs `0`.
  </acceptance_criteria>

  <done>
    `KidContentCopyTests` passes, sweeps at least eighteen shipped strings, enforces the house dash
    style, and pins the disclaimer against both a reword and an internal requirement identifier
    leaking onto a user-facing screen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/kid-content.md` section 6 -> `KidContentCopy.Screen.disclaimer` | The user-facing safety statement crosses here; a weakened wording is a compliance risk, not a runtime one. |
| `KidContentCopy.Children.*` -> parent's screen | These strings define what this feature asks a parent for. A field label is the mechanism by which new child data would be collected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-04 | Information Disclosure | `Children` copy prompting for identifying child data | high | mitigate | A future edit turning `nicknameFieldLabel` into a required name prompt, or adding an age or birthday prompt, is the concrete path by which this feature starts collecting data about a specific child. Task 2's banned-term sweep fails on any new string naming age, weight, growth, height, or birthday, and the one exception is asserted positively so it cannot silently widen. |
| T-04.2-05 | Tampering | `Screen.disclaimer` weakened or removed | medium | mitigate | Task 2's disclaimer-integrity test pins three load-bearing phrases, so a reword that drops the "not an individualized plan" or "pending clinical and legal review" framing fails the suite rather than shipping quietly ahead of Phase 5 review. |
| T-04.2-06 | Information Disclosure | Internal requirement identifier rendered to a user | low | mitigate | The section 6 source sentence carries a parenthetical naming two internal requirement IDs. Task 1 strips it and Task 2 asserts its absence from the shipped string. |
| T-04.2-SC | Tampering | Package-manager installs | n/a | n/a | Not applicable. This plan installs no npm, pip, or cargo package and adds no SwiftPM dependency. See 04.2-RESEARCH.md's Package Legitimacy Audit. No legitimacy checkpoint is required. |
</threat_model>

<verification>
- `cd RithamCore && swift build` clean.
- `cd RithamCore && swift test` fully green including `KidContentCopyTests`.
- Every shipped string is non-empty, dash-clean, and free of body-or-identity vocabulary except the
  one named negating string.
- `Screen.disclaimer` carries the section 6 safety framing and no internal requirement identifier.
</verification>

<success_criteria>
Every locked string the Kid Ideas screen will render lives in one reviewable `RithamCore` file, and
automated tests stand behind the two rules this copy exists to hold: never ask for anything about a
specific child (D-07, KIDCONTENT-02), and never soften the not-an-individualized-plan disclaimer
this content ships behind pending Phase 5 review (D-01, D-03).
</success_criteria>

<output>
Create `.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-02-SUMMARY.md`
when done.
</output>
