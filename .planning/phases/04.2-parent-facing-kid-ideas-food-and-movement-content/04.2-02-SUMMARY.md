---
phase: 04.2-parent-facing-kid-ideas-food-and-movement-content
plan: 02
subsystem: copy
tags: [swift, swift-testing, locked-copy, disclaimer, house-style]

# Dependency graph
requires: []
provides:
  - "KidContentCopy.swift: KidContentCopy.Screen (headline/intro/section titles/disclaimer/doneCTA/sourceLine) and KidContentCopy.Children (sectionTitle/explainer/emptyState/addCTA/ordinalLabel/nicknameFieldLabel/renameCTA/saveCTA/cancelCTA/deleteCTA/deleteConfirmation/deleteAccessibilityLabel/saveFailed) -- 20 locked public members"
  - "KidContentCopyTests.swift: shippedStrings sweep, banned-term sweep with the Children.explainer named exception, house dash-style gate, disclaimer-integrity pins, and locked-value assertions"
affects: [04.2-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Locked *Copy namespace convention (matches ScreeningCopy.swift/SocialCopy.swift/MomentumCopy.swift) applied to a new content-adjacent screen so its strings are reviewable in one file ahead of Phase 5 LAUNCH-02/LAUNCH-03 sign-off"
    - "Single named-exception pattern for a banned-term lexicon sweep (Children.explainer negates age/weight/growth), mirroring SocialCopyTests' exclamation-point-exception precedent"

key-files:
  created:
    - RithamCore/Sources/RithamCore/Copy/KidContentCopy.swift
    - RithamCore/Tests/RithamCoreTests/KidContentCopyTests.swift
  modified: []

key-decisions:
  - "Screen.disclaimer transcribes docs/kid-content.md section 6 verbatim except for dropping the source sentence's trailing parenthetical naming LAUNCH-02/LAUNCH-03, per the plan's explicit instruction that an internal requirement identifier must never reach a user-facing string."
  - "Children.ordinalLabel/nicknameFieldLabel implement 04.2-CONTEXT.md's Claude's Discretion multi-child labelling guidance literally: ordinal slots (\"Child 1\") plus an explicitly optional nickname field, never a required name."

requirements-completed: [KIDCONTENT-01]

coverage:
  - id: D1
    description: "Every locked string the Kid Ideas screen renders lives in one reviewable RithamCore file (KidContentCopy.swift), covering the screen chrome, the section 6 disclaimer, and the child-entry management strings KIDCONTENT-02 needs"
    requirement: "KIDCONTENT-01"
    verification:
      - kind: unit
        ref: "RithamCore/Tests/RithamCoreTests/KidContentCopyTests.swift#atLeastEighteenStringsScanned, #lockedValuesMatchExactly, #disclaimerPinsSafetyFramingAndDropsRequirementID"
        status: pass
    human_judgment: false
  - id: D2
    description: "No shipped string asks for or names a child's age, weight, growth, height, or birthday, except Children.explainer which names them solely to negate them; no shipped string offers to save/favourite/mark an idea"
    requirement: "KIDCONTENT-01"
    verification:
      - kind: unit
        ref: "RithamCore/Tests/RithamCoreTests/KidContentCopyTests.swift#noShippedStringContainsBannedTermExceptExplainer, #explainerNamesBannedTermsToNegateThem"
        status: pass
    human_judgment: false

duration: 15min
completed: 2026-09-12
status: complete
---

# Phase 04.2 Plan 02: KidContentCopy Summary

**Locked `KidContentCopy` namespace in `RithamCore` -- 20 public members across `Screen` (headline, intro, section titles, the section 6 disclaimer with its internal requirement-ID reference stripped, done CTA, source-line formatter) and `Children` (ordinal-slot child-entry management strings, with a single named exception negating age/weight/growth), backed by a 9-test suite sweeping banned terms, house dash style, and locked values.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-09-12 (session start)
- **Completed:** 2026-09-12T04:56:36+05:30
- **Tasks:** 2 completed
- **Files modified:** 2 (both new)

## Accomplishments
- `KidContentCopy.swift` declares `KidContentCopy.Screen` (7 members: `headline`, `intro`, `foodSectionTitle`, `movementSectionTitle`, `disclaimer`, `doneCTA`, `sourceLine(attribution:)`) and `KidContentCopy.Children` (13 members: `sectionTitle`, `explainer`, `emptyState`, `addCTA`, `ordinalLabel(_:)`, `nicknameFieldLabel`, `renameCTA`, `saveCTA`, `cancelCTA`, `deleteCTA`, `deleteConfirmation`, `deleteAccessibilityLabel(for:)`, `saveFailed`) -- exactly 20 `public static` members, exactly two nested namespaces.
- `Screen.disclaimer` transcribes `docs/kid-content.md` section 6's three sentences verbatim, with the trailing `(see LAUNCH-02/LAUNCH-03)` parenthetical dropped so no internal requirement identifier reaches the shipped string (`grep -v '^\s*//' <file> | grep -c 'LAUNCH-0'` returns `0`).
- `Children.explainer` is the file's sole named exception to the banned-term lexicon: it names age, weight, and growth explicitly in order to negate them ("Ritham stores nothing about your child except this entry. No age, no weight, no growth, and no activity history.").
- `KidContentCopyTests.swift` (`KidContentCopyTests`, no `ModelContainer`, no `StepRegistry`) sweeps all 20 shipped strings/functions: non-vacuity (>=18), non-emptiness, a 19-term banned-lexicon sweep with the `Children.explainer` exception, a positive exception-integrity check, the house no-em/en-dash rule, five locked full-string equality checks, disclaimer-integrity phrase pins plus the `LAUNCH-0` absence gate, and interpolation checks for `ordinalLabel`/`sourceLine`. 9 tests total, all passing.
- Verified live that the banned-term sweep is not vacuous: temporarily changed `Children.sectionTitle` to `"Manage children"`, confirmed the sweep failed with the expected "contains banned term \"age\"" message (the `manage` substring trips the bare `age` stem, exactly the failure mode the file's own authoring constraint warns about), then reverted before committing.
- Full `RithamCore` test suite green: 458 tests across 34 suites, no regression (including plan 04.2-01's independently-committed `KidContentCatalogTests`, confirming the two wave-1 plans coexist without conflict).

## Task Commits

Each task was committed atomically:

1. **Task 1: Write KidContentCopy with the locked screen and child-entry strings** - `0fc2955` (feat)
2. **Task 2: Add KidContentCopyTests with the lexicon sweep, dash gate, and locked-value pins** - `fc6c2da` (test)

**Plan metadata:** (this commit, following SUMMARY.md creation)

## Files Created/Modified
- `RithamCore/Sources/RithamCore/Copy/KidContentCopy.swift` - `Screen`/`Children` namespaces, 20 locked public members, header documenting the LAUNCH-02/LAUNCH-03 strip, D-03/D-09 prohibitions, and the house dash-style rule.
- `RithamCore/Tests/RithamCoreTests/KidContentCopyTests.swift` - 9 tests: non-vacuity, non-emptiness, banned-term sweep + named exception + exception-integrity, dash gate, locked values, disclaimer integrity, interpolation.

## Decisions Made
- Dropped the disclaimer's trailing `(see LAUNCH-02/LAUNCH-03)` parenthetical per the plan's explicit instruction; verified via a non-comment-line grep that no `LAUNCH-0` substring reaches the shipped string.
- Verified every other string in the file avoids the bare `age` substring per the plan's authoring constraint (checked `guidance`, `ideas`, `pediatrician`, `registered`, `legal`, `change`, `again`, and every other word for an accidental `age`/`ing`-adjacent collision) -- no accidental trip found, so no rewording was needed.
- Used the plan's exact literal string values throughout; no deviation from the plan's Artifacts table or Values section was required.

## Deviations from Plan

None - plan executed exactly as written. Both tasks' automated verification commands (`swift build` + member count, `swift test --filter KidContentCopyTests`) passed on the first attempt with no fixes needed.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `KidContentCopy` is ready for plan 04.2-04 (the Kid Ideas screen view) to render directly: `Screen.*` for the combined screen's chrome and disclaimer, `Children.*` for the child-entry management UI, and `Screen.sourceLine(attribution:)` wrapping plan 04.2-01's `KidContentCitation.attribution` for the per-idea source line.
- Content ships pending Phase 5's LAUNCH-02/LAUNCH-03 clinical and legal review, per the file's own header and `docs/kid-content.md`'s status note -- no action needed from this phase, flagged for whoever plans Phase 5.
- No blockers for subsequent 04.2 plans. This plan touched only `RithamCore/Sources/RithamCore/Copy/` and its own test file, confirmed with zero conflict against plan 04.2-01's independently-committed `KidContentCatalog.swift`/`KidContentCatalogTests.swift`.

---
*Phase: 04.2-parent-facing-kid-ideas-food-and-movement-content*
*Completed: 2026-09-12*

## Self-Check: PASSED

- FOUND: RithamCore/Sources/RithamCore/Copy/KidContentCopy.swift
- FOUND: RithamCore/Tests/RithamCoreTests/KidContentCopyTests.swift
- FOUND: .planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-02-SUMMARY.md
- FOUND commit: 0fc2955
- FOUND commit: fc6c2da
