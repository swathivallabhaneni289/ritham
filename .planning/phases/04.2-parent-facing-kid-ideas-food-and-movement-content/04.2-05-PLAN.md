---
phase: 04.2
plan: 05
type: execute
wave: 3
depends_on: ["04.2-01", "04.2-02", "04.2-03", "04.2-04"]
files_modified:
  - RithamApp/RithamTests/Phase42CoverageTests.swift
  - RithamApp/Ritham.xcodeproj/project.pbxproj
autonomous: false
requirements: [KIDCONTENT-01, KIDCONTENT-02]

must_haves:
  truths:
    - "The Kid Ideas screen is reachable from Settings and from nowhere else"
    - "No dashboard surface and no onboarding screen mentions kid content"
    - "No persisted type anywhere records which ideas were viewed, tried, or liked"
    - "The Kid Ideas screen reads and writes nothing on the user's own health-screening path"
    - "The Kid Ideas screen offers no sharing affordance"
    - "The whole test suite is green across two consecutive full-target runs"
  artifacts:
    - path: "RithamApp/RithamTests/Phase42CoverageTests.swift"
      provides: "Structural gates converting this phase's locked decisions into failing tests"
      min_lines: 150
      contains: "Phase42CoverageTests"
  key_links:
    - from: "RithamApp/RithamTests/Phase42CoverageTests.swift"
      to: "RithamApp/Ritham/Settings/SettingsView.swift"
      via: "source scan asserting the one route to KidIdeasView still exists"
      pattern: "KidIdeasView\\("
    - from: "RithamApp/RithamTests/Phase42CoverageTests.swift"
      to: "RithamApp/Ritham/Home/HomeHubView.swift"
      via: "source scan asserting the dashboard never mentions kid content"
      pattern: "HomeHubView"
---

<objective>
Convert this phase's locked decisions from things that happen to be true today into automated gates
that fail tomorrow if someone changes them back: the Settings-only entry point (D-04), the one
combined screen (D-05), the browsable-not-shown-once framing (D-08), the read-only no-saved-state
rule (D-09), and the verbatim citation discipline (D-01) -- then verify the whole suite twice and
hand the visual check to the project's batched verification pass.

Purpose: two failure modes in this repo's own history motivate this plan. Plan 04.1-14 shipped a
screen that was registered but reachable from nothing, discovered only at phase close. And
04.2-RESEARCH.md Pitfall 1 names the dashboard reflex as the single most likely way this feature
ends up in front of every user, including those with no child. Both become one-line test failures
here.

Output: `Phase42CoverageTests.swift`, two consecutive green full-target runs, and a recorded
human-verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-CONTEXT.md
@.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-RESEARCH.md
@.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-VALIDATION.md
@.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-04-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

New file `RithamApp/RithamTests/Phase42CoverageTests.swift`:

| Symbol | Kind |
|--------|------|
| `Phase42CoverageTests` | `@MainActor @Suite("Phase42CoverageTests", .serialized) struct`, declared inside `extension StepRegistryTouchingSuites` |
| `Phase42CoverageTests.rithamDirectory` | `private var URL` -- resolves `RithamApp/Ritham/` from `#filePath` |
| `Phase42CoverageTests.nonCommentSource(of:)` | `private func (String) throws -> String?` |
| `Phase42CoverageTests.swiftFiles(under:)` | `private func (String) -> [URL]` |

<tasks>

<task type="auto">
  <name>Task 1: Add Phase42CoverageTests structural gates</name>

  <read_first>
    - `RithamApp/RithamTests/Phase4CoverageTests.swift` in full -- the suite to mirror: its nesting
      inside `extension StepRegistryTouchingSuites`, its `init()` calling `StepRegistry.reset()`
      then `StepBootstrap.registerAllSteps()`, its `rithamDirectory` computed from `#filePath`, its
      `nonCommentSource(of:)` comment filter and the reason that filter exists (so a comment
      documenting a prohibition never trips the gate it documents), its use of `Issue.record` when
      a file cannot be read so a path mistake fails loudly instead of passing vacuously, and its
      `noHomeSurfaceOffersASharingAffordance` directory walk with an explicit scanned-file-count
      guard.
    - `RithamApp/RithamTests/Phase3CoverageTests.swift` -- the directory-walk precedent plan 03-10
      established for a structural no-sharing gate.
    - `RithamApp/RithamTests/StepRegistrySerialization.swift` -- the parent suite and why registry-
      touching suites must nest under it.
    - `RithamApp/Ritham/Settings/KidIdeasView.swift`, `RithamApp/Ritham/Settings/ChildEntryStore.swift`,
      and `RithamApp/Ritham/Settings/SettingsView.swift` as plan 04.2-04 left them -- so every path
      and symbol this suite asserts against is the real one, not a guessed one.
  </read_first>

  <files>RithamApp/RithamTests/Phase42CoverageTests.swift, RithamApp/Ritham.xcodeproj/project.pbxproj</files>

  <action>
    Create `Phase42CoverageTests.swift` importing `Foundation`, `SwiftUI`, `Testing`, `RithamCore`,
    and `@testable import Ritham`. Declare it inside `extension StepRegistryTouchingSuites { ... }`
    as `@MainActor @Suite("Phase42CoverageTests", .serialized) struct Phase42CoverageTests`, with an
    `init()` that resets and re-bootstraps `StepRegistry`.

    Nesting parent matters and is not interchangeable: this suite touches `StepRegistry`'s shared
    static state, so it belongs under `StepRegistryTouchingSuites`, not under
    `MomentumContainerTouchingSuites` where plans 04.2-03 and 04.2-04's container-creating suites
    live. A Swift Testing suite cannot nest under two parents, so this file must create no
    `ModelContainer` at all. State both facts in the file header.

    Copy `rithamDirectory` and `nonCommentSource(of:)` from `Phase4CoverageTests` and add a
    `swiftFiles(under:)` helper returning every `.swift` file under a directory relative to
    `rithamDirectory`, for the two directory walks below.

    Gates to write, each as its own named test with a failure message that says what regressed and
    why it matters:

    1. Registry completeness: `StepRegistry.unregisteredSteps.isEmpty`. This phase added no
      `OnboardingStep` case, and this assertion is what catches someone adding one for this feature
      without a presenter.
    2. The screen is not an onboarding step: `Settings/KidIdeasView.swift`'s comment-filtered source
      contains none of `OnboardingStepPresenting`, `static let step`, or `OnboardingFlow`. Failure
      message must name D-04 and say this screen is Settings-reached only, and that
      `PrivacyZonesView`'s registered-but-not-routed shape is deliberately NOT the precedent here
      because no `OnboardingStep` case exists for kid content.
    3. The one route exists -- the blocking reachability gate:
      `Settings/SettingsView.swift`'s comment-filtered source contains `KidIdeasView(`. Failure
      message must say the only route to the Kid Ideas screen is gone and the screen is now
      unreachable dead code, citing plan 04.1-14's registered-but-unreachable outcome as the
      precedent this gate exists to prevent repeating.
    4. The dashboard never mentions this feature: `Home/HomeHubView.swift`'s comment-filtered source
      contains none of `KidIdeasView`, `KidContentCatalog`, `KidContentCopy`, `ChildEntryStore`, or
      `ChildEntryRecord`. Failure message must cite D-04 and 04.2-RESEARCH.md Pitfall 1 -- not every
      user has a child, which is the entire reason this is not a dashboard section.
    5. Onboarding never mentions this feature: walk every `.swift` file under `Onboarding/` and
      assert none of their comment-filtered sources contains `KidIdeasView`, `KidContentCatalog`, or
      `ChildEntryRecord`. Guard with an explicit assertion that at least one file was scanned, so a
      path mistake fails loudly rather than passing on an empty walk.
    6. No per-idea state (D-09): `Settings/KidIdeasView.swift`'s comment-filtered source contains
      none of these tokens -- `favorite`, `favourite`, `bookmark`, `isTried`, `markTried`,
      `viewedIdea`, `seenIdea`, `@Model`, `FetchDescriptor<`. Failure message must say D-09 forbids
      anything that could read as a log of what a specific child ate or did. Add a comment recording
      that the first two tokens are checked against the VIEW's source only and deliberately not
      against `KidContentCatalog`, because two shipped USDA tips legitimately contain that word in
      their own published text.
    7. No persisted content-interaction type: walk every `.swift` file under `Persistence/` and
      assert no comment-filtered source declares a type whose name contains `Idea`, `KidContent`,
      `Favorite`, `Favourite`, or `Viewed`. Guard with a scanned-file-count assertion of at least
      ten files, since that directory holds well over ten models today.
    8. Screening isolation: `Settings/KidIdeasView.swift` and `Settings/ChildEntryStore.swift`
      comment-filtered sources contain none of `contentPermission`, `ConditionTag`,
      `GateResolution`, `saveScreeningResult`, or `TagDerivation`. Failure message must say a child
      entry must never influence the user's own clearance gate, and that this content carries no
      condition-tag dimension (04.2-RESEARCH.md Pitfall 4).
    9. No sharing affordance: `Settings/KidIdeasView.swift`'s comment-filtered source contains none
      of `ShareLink`, `UIActivityViewController`, or `UIPasteboard`, extending the same structural
      discipline `Phase3CoverageTests` and `Phase4CoverageTests` already apply to their own
      directories. Pediatric content and a child entry are exactly the data this project's
      privacy posture keeps unshareable.
    10. Content reachability, so the gates above cannot pass on an empty feature: assert
      `KidContentCatalog.ideas.count == 27` and that
      `KidContentCopy.Screen.headline == SettingsView.kidIdeasRowTitle`. This ties the structural
      suite to the real shipped content and the real row label rather than to file paths alone.

    Every file-reading test must use `Issue.record` plus an early return when the file does not
    exist, following `Phase4CoverageTests`, so a renamed file fails the suite instead of silently
    passing.

    Regenerate the project: `cd RithamApp && xcodegen generate`, and commit the updated
    `project.pbxproj`.
  </action>

  <verify>
    <automated>cd RithamApp && xcodegen generate && xcodebuild test -project Ritham.xcodeproj -scheme Ritham -destination 'platform=iOS Simulator,name=iPhone 15' -only-testing:RithamTests/Phase42CoverageTests -quiet</automated>
  </verify>

  <acceptance_criteria>
    - The targeted `xcodebuild test` exits zero with all ten gates passing. Use the nested-suite
      fallback plan 04.2-03 documents if `-only-testing:` does not resolve.
    - `grep -c 'extension StepRegistryTouchingSuites' RithamApp/RithamTests/Phase42CoverageTests.swift`
      outputs exactly `1`, and `grep -c 'ModelContainer'` on the same file outputs `0`.
    - Non-vacuity, checked once and reverted: temporarily add a line to `Home/HomeHubView.swift`
      referencing `KidIdeasView` and confirm gate 4 fails; temporarily comment out the
      `.sheet(isPresented: $isShowingKidIdeas)` presentation in `SettingsView.swift` and confirm
      gate 3 fails. Revert both. Record in the SUMMARY that both were checked.
    - `grep -c 'Phase42CoverageTests.swift' RithamApp/Ritham.xcodeproj/project.pbxproj` is greater
      than `0`.
  </acceptance_criteria>

  <done>
    Ten structural gates pass, and two of them are proved to actually discriminate by being made to
    fail and then restored.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify the whole phase across two consecutive full-target runs</name>

  <read_first>
    - `.planning/STATE.md` Blockers/Concerns -- the two recorded cross-suite test-infrastructure
      issues: the `StepRegistry` race fixed in plan 02-16, and the still-open low-frequency
      in-memory `ModelContainer` crash affecting suites that create containers concurrently. This
      phase added two new container-creating suites (`ChildEntryTests`, `KidIdeasTests`) and one new
      registry-touching suite (`Phase42CoverageTests`), which is exactly the shape that tipped the
      target into deterministic failure in plan 04.1-11.
    - `.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-VALIDATION.md` --
      its Sampling Rate section, which requires a full `swift test` and a full `xcodebuild test`
      before verification, plus both structural negative gates passing explicitly.
  </read_first>

  <files>RithamApp/RithamTests/Phase42CoverageTests.swift</files>

  <action>
    Run the phase's full verification and fix anything it surfaces.

    First, `cd RithamCore && swift test` -- the whole RithamCore suite, including
    `KidContentCatalogTests` and `KidContentCopyTests`.

    Then run the full app target twice in a row:
    `xcodebuild test -project RithamApp/Ritham.xcodeproj -scheme Ritham -destination 'platform=iOS
    Simulator,name=iPhone 15'`. Two consecutive runs, both green, is the bar plan 04.1-11 set after
    a third suite touching a shared resource tipped the target into deterministic failure; one green
    run is not sufficient evidence for a phase that added three new suites.

    If either run fails or flakes, diagnose before changing anything. If the failure is a
    cross-suite resource race involving one of this phase's new suites, the fix is the nesting
    mechanism this repo already uses -- confirm `ChildEntryTests` and `KidIdeasTests` are nested
    under `MomentumContainerTouchingSuites` and `Phase42CoverageTests` under
    `StepRegistryTouchingSuites`, and correct any that are not. Do not add a `.serialized` trait to
    a pre-existing unserialized suite as a workaround: STATE.md records those as a separate,
    pre-existing item deliberately left untouched, and widening the fix would make this phase's
    change set unreviewable.

    If a failure is instead a real defect in this phase's code, fix the code, not the test.

    Record in the SUMMARY: the exact commands run, the pass counts from both full-target runs, and
    whether any flake was observed. If a flake was observed and not fixed, record it as a
    carried-forward item in the same style STATE.md's existing Blockers/Concerns entries use.
  </action>

  <verify>
    <automated>cd RithamCore && swift test && cd ../RithamApp && xcodebuild test -project Ritham.xcodeproj -scheme Ritham -destination 'platform=iOS Simulator,name=iPhone 15' -quiet</automated>
  </verify>

  <acceptance_criteria>
    - `cd RithamCore && swift test` exits zero.
    - Two consecutive full `xcodebuild test` runs of the `RithamTests` target both exit zero, with
      the same test count reported by each. Record both counts in the SUMMARY.
    - `KidContentCatalogTests`, `KidContentCopyTests`, `ChildEntryTests`, `KidIdeasTests`, and
      `Phase42CoverageTests` all appear in the results as passing, and none is reported skipped.
    - Every pre-existing suite is still green -- in particular `Phase4CoverageTests`,
      `HealthDataStoreTests`, `PersistenceTests`, and the Keychain-touching suites.
    - `git status` shows no uncommitted temporary edit left over from any non-vacuity check run in
      Task 1 or in plans 04.2-01 through 04.2-04.
  </acceptance_criteria>

  <done>
    The whole project builds and tests green twice in a row with this phase's five new suites
    included, and any observed flake is either fixed or recorded.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the Kid Ideas screen</name>

  <files>.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-05-SUMMARY.md</files>

  <read_first>
    - `.planning/PROJECT.md` Key Decisions, the 2026-09-06 entry batching every physical-device,
      visual, and accessibility check into one end-of-project pass.
    - `.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-VALIDATION.md`
      Manual-Only Verifications table, which already routes this phase's visual check there.
    - `.planning/phases/04.1-group-goal-events-accountability-circles/04.1-UAT.md` -- the format to
      reuse when recording a deferral so it is tracked rather than lost.
  </read_first>

  <action>
    Present the checkpoint below to the developer verbatim, then record the outcome. Do not run any
    of the eight steps yourself: this environment has Simulator access via `simctl` but no
    touch-injection tool (no idb, no XCUITest driver), and no attached physical device, so none of
    the interactive or accessibility steps is executable here -- the same constraint plans 02-16 and
    04.1-17 recorded.

    On "approved": record the date, the outcome, and any observation in `04.2-05-SUMMARY.md`.

    On "defer": record the deferral in `04.2-05-SUMMARY.md` with the date and the reason, and add a
    matching row to this phase's UAT tracking using `04.1-UAT.md`'s format so the batched
    end-of-project pass picks it up. Do not mark the phase's requirement checkboxes complete on the
    strength of a deferral alone.

    On a reported problem: capture exactly what the developer described, do not attempt a fix inside
    this task, and record it as a gap for a follow-up plan.
  </action>

  <what-built>
    A "Kid Ideas" row in Settings opens a read-only screen listing 19 food ideas and 8 movement
    ideas, each group carrying the USDA or CDC source it was transcribed from, followed by a
    disclaimer and an optional section where a parent can add, rename, and remove child entries.
    Everything automatable has been automated: 27 content items pinned by test, no prohibited
    nutrition figure, per-entry persistence isolation proved, and ten structural gates covering
    reachability, the absence of any dashboard or onboarding route, and the absence of any per-idea
    saved state. What remains is visual and interactive judgment, which no tool in this environment
    can exercise.
  </what-built>

  <how-to-verify>
    This project's standing policy (PROJECT.md Key Decisions, 2026-09-06) batches every
    physical-device, visual, and AX3/AX5 accessibility check into one end-of-project pass rather
    than gating each phase, and 04.2-VALIDATION.md's Manual-Only Verifications table already routes
    this phase's visual check there. Plans 02-16 and 04.1-17 set the same precedent. So the expected
    outcome here is a recorded deferral, not a blocked phase.

    When the batched pass runs, these are the steps:

    1. Launch the app on the Simulator and complete or restore onboarding to reach the dashboard.
    2. Open Settings. Confirm a row labelled "Kid Ideas" appears between "Privacy Zones" and the
       "Screening answers" group, and that the dashboard itself shows no kid-content section
       anywhere.
    3. Tap the row. Confirm the screen opens as a sheet and shows, in order: the intro line, the
       food ideas with their two source preambles and two source attributions, the movement ideas
       with their source attribution, the disclaimer, then the child-entry section, then Done.
    4. Confirm no idea row is tappable and that nothing on the screen offers to save, favourite, or
       mark an idea.
    5. Tap "Add a child" twice. Confirm two rows appear labelled "Child 1" and "Child 2". Rename the
       first; confirm the second is unchanged. Delete the first; confirm the remaining row renumbers
       to "Child 1".
    6. Force-quit and relaunch. Reopen the screen and confirm the child entries survived.
    7. Set the system text size to AX3 and then AX5. Confirm every idea body, the disclaimer, and
       every control remains legible and reachable, and that no text truncates.
    8. Confirm the screen is unreachable from the dashboard and from any onboarding screen.

    If any step fails, describe what you saw rather than approving.
  </how-to-verify>

  <verify>
    <automated>cd RithamApp && xcodebuild test -project Ritham.xcodeproj -scheme Ritham -destination 'platform=iOS Simulator,name=iPhone 15' -only-testing:RithamTests/Phase42CoverageTests -quiet</automated>
  </verify>

  <acceptance_criteria>
    - Either the developer runs the eight steps and reports the result, or the developer defers this
      check to the batched end-of-project pass.
    - Whichever happens is recorded in `04.2-05-SUMMARY.md` with the date and, on deferral, added to
      the phase's UAT tracking in the same format `04.1-UAT.md` uses so it is not lost.
    - The automated gate above is green regardless, so a deferral never leaves the structural claims
      unverified.
  </acceptance_criteria>

  <resume-signal>
    Type "approved" if the steps pass, "defer" to route this to the batched end-of-project
    verification pass, or describe what you saw if anything looks wrong.
  </resume-signal>

  <done>
    The visual and accessibility check is either completed and approved, or explicitly deferred and
    recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future contributor -> this phase's locked decisions | The only boundary this plan defends. Every gate here exists because a plausible, well-intentioned future edit crosses it. |
| Test suite -> shared static state | `StepRegistry` and in-memory `ModelContainer` instantiation are shared resources this phase's three new suites touch concurrently with existing ones. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-17 | Elevation of Privilege | A second route putting pediatric content in front of every user | high | mitigate | Gates 2, 4 and 5 fail the suite on any dashboard reference, any onboarding reference, and any `OnboardingStepPresenting` conformance on this screen. |
| T-04.2-18 | Denial of Service | The screen becoming unreachable dead code | medium | mitigate | Gate 3 asserts `SettingsView.swift` still constructs `KidIdeasView`, the exact failure plan 04.1-14 shipped and discovered only at phase close. |
| T-04.2-19 | Information Disclosure | A per-idea state field reintroduced as a child activity log | high | mitigate | Gates 6 and 7 scan the view and the whole persistence directory, the latter with a ten-file non-vacuity guard so the walk cannot pass on a path mistake. |
| T-04.2-20 | Elevation of Privilege | A child entry influencing the user's own clearance gate | medium | mitigate | Gate 8 asserts neither the view nor the entry facade names any gate, tag, or screening-write symbol. |
| T-04.2-21 | Information Disclosure | Pediatric content or a child entry shared off-device | high | mitigate | Gate 9 forbids every sharing affordance token in the view, extending the identical discipline Phase 3 and Phase 4 already apply to their own directories. |
| T-04.2-22 | Tampering | A cross-suite test race masking a real regression | medium | mitigate | Task 2 requires two consecutive green full-target runs rather than one, and requires this phase's new suites to sit under the correct serialization parent rather than widening `.serialized` onto pre-existing suites. |
| T-04.2-SC | Tampering | Package-manager installs | n/a | n/a | Not applicable. This plan installs no npm, pip, or cargo package and adds no SwiftPM dependency; only the generated `project.pbxproj` is rewritten by `xcodegen`. No legitimacy checkpoint is required. |
</threat_model>

<verification>
- `Phase42CoverageTests` green, with gate 3 and gate 4 each proved to fail when deliberately broken.
- `cd RithamCore && swift test` green.
- Two consecutive full `xcodebuild test` runs green with identical test counts.
- All five suites this phase added are present and passing, none skipped.
- The human-verification checkpoint is either approved or explicitly deferred and recorded.
</verification>

<success_criteria>
Every decision this phase locked is now a test that fails if it is undone: the screen is reachable
from exactly one place, appears on no dashboard and in no onboarding flow, records nothing about
which ideas were seen, touches no health gate, and cannot be shared. The suite proves it twice in a
row, and the remaining visual judgment is routed to the project's batched verification pass rather
than silently skipped.
</success_criteria>

<output>
Create `.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-05-SUMMARY.md`
when done.
</output>
