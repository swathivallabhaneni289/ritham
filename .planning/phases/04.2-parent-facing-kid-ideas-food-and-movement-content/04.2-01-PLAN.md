---
phase: 04.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift
  - RithamCore/Tests/RithamCoreTests/KidContentCatalogTests.swift
autonomous: true
requirements: [KIDCONTENT-01]

must_haves:
  truths:
    - "Every shipped kid-content idea is word-for-word the text a named USDA or CDC/HHS document publishes, never Ritham's own wording"
    - "Every idea carries the publishing body, document identifier, and publication date it came from"
    - "No shipped idea names a calorie, macro, portion, serving size, BMI, or weight-management figure"
    - "The tenth DG TipSheet No. 24 tip that docs/kid-content.md excluded in full never appears in the catalog"
    - "Food ideas and movement ideas are separable by category without any condition-tag or permission lookup"
  artifacts:
    - path: "RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift"
      provides: "KidContentCategory, KidContentCitation, KidContentIdea, KidContentCatalog with 27 transcribed ideas"
      min_lines: 200
      contains: "public enum KidContentCatalog"
    - path: "RithamCore/Tests/RithamCoreTests/KidContentCatalogTests.swift"
      provides: "Count, shape, prohibited-content, and verbatim spot-check assertions"
      min_lines: 90
      contains: "KidContentCatalogTests"
  key_links:
    - from: "docs/kid-content.md"
      to: "RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift"
      via: "verbatim transcription of sections 1, 2 and 4 -- the doc is the sole source, the original USDA/CDC PDFs are not"
      pattern: "kid-content\\.md"
    - from: "RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift"
      to: "RithamCore/Tests/RithamCoreTests/KidContentCatalogTests.swift"
      via: "the test suite sweeps every title, body, and intro string the catalog exposes"
      pattern: "KidContentCatalog\\.ideas"
---

<objective>
Build `KidContentCatalog` in `RithamCore` -- the static, citation-bearing food and movement content
KIDCONTENT-01 ships -- transcribed verbatim from `docs/kid-content.md` (D-01, D-02), carrying no
calorie/macro/portion/BMI/weight-management figure anywhere (D-03), scoped to the preteen band the
phase narrowed to with no toddler/preschool/school-age banding built "for future use" (D-06).

Purpose: this is the phase's one genuinely load-bearing content artifact. Everything else in
Phase 04.2 renders, persists around, or gates access to what this file declares. The content
research is already finished -- `docs/kid-content.md` is the sole source, and the original USDA
and CDC PDFs must not be consulted (that is exactly how the deliberately-excluded tenth snack tip
gets reintroduced; see 04.2-RESEARCH.md Pitfall 5).

Output: `KidContentCatalog.swift` (four public types, three citation constants, two source-preamble
constants, 27 ideas, one category accessor) plus `KidContentCatalogTests.swift`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-CONTEXT.md
@.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-RESEARCH.md
@docs/kid-content.md
@RithamCore/Sources/RithamCore/Guidance/NutritionGuidanceCatalog.swift
</context>

## Artifacts this phase produces (this plan's share)

New file `RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift`, declaring:

| Symbol | Kind | Notes |
|--------|------|-------|
| `KidContentCategory` | `public enum`, `String`-raw-valued, `Sendable, Equatable, CaseIterable` | cases `food`, `movement` -- exactly two, no age-band case (D-06) |
| `KidContentCitation` | `public struct`, `Sendable, Equatable` | `documentTitle`, `documentIdentifier`, `publishingBody`, `publicationDate` (all `public let String`), memberwise `public init`, computed `public var attribution: String` |
| `KidContentIdea` | `public struct`, `Sendable, Equatable, Identifiable` | `id: String` (stable slug), `category: KidContentCategory`, `title: String`, `body: String`, `citation: KidContentCitation`, memberwise `public init` |
| `KidContentCatalog` | `public enum` (namespace) | statics below |
| `KidContentCatalog.veggiesAndFruitsCitation` | `public static let KidContentCitation` | DG TipSheet No. 11 |
| `KidContentCatalog.snackTipsCitation` | `public static let KidContentCitation` | DG TipSheet No. 24 |
| `KidContentCatalog.physicalActivityCitation` | `public static let KidContentCitation` | CDC / HHS Physical Activity Guidelines |
| `KidContentCatalog.veggiesAndFruitsIntro` | `public static let String` | section 1 preamble, verbatim |
| `KidContentCatalog.snackTipsIntro` | `public static let String` | section 2 preamble as trimmed in the doc, verbatim |
| `KidContentCatalog.ideas` | `public static let [KidContentIdea]` | 27 entries: 19 `.food`, 8 `.movement` |
| `KidContentCatalog.ideas(in:)` | `public static func (KidContentCategory) -> [KidContentIdea]` | filter only, no permission lookup |

New file `RithamCore/Tests/RithamCoreTests/KidContentCatalogTests.swift`, declaring suite
`KidContentCatalogTests` (Swift Testing, `@Suite("KidContentCatalogTests")`, no `ModelContainer`,
no `StepRegistry` -- it nests under no parent suite).

<tasks>

<task type="auto">
  <name>Task 1: Declare the catalog types, citations, source preambles, and the ten veggies-and-fruits ideas</name>

  <read_first>
    - `docs/kid-content.md` -- the ONLY source for every transcribed string in this task. Read
      sections 1, 3, and 5 in full before writing a line. Section 3 records exactly which source
      sentences were already removed and why; re-adding any of them is a D-03 violation.
    - `RithamCore/Sources/RithamCore/Guidance/NutritionGuidanceCatalog.swift` -- read its first
      thirty lines. Its header comment is the citation/transcription discipline this new file's
      own header must state in its own words: verbatim transcription rather than paraphrase, no
      arithmetic anywhere in the file, every string traceable to a named publishing body, ships
      pending LAUNCH-02/LAUNCH-03 review.
    - `RithamCore/Sources/RithamCore/Guidance/WorkoutGuidanceCatalog.swift` -- skim for the same
      header discipline applied to movement content.
  </read_first>

  <files>RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift</files>

  <action>
    Create `KidContentCatalog.swift` with a file-header comment, the four public types listed in
    this plan's Artifacts table, the three citation constants, the two source-preamble constants,
    and the first ten `KidContentIdea` entries (docs/kid-content.md section 1).

    File header comment must state, in the executor's own words: (a) every string in this file is
    transcribed verbatim from `docs/kid-content.md`, which is the sole source -- the original USDA
    and CDC PDFs must not be consulted when revising this file, because that document already
    applied an exclusion rule (its section 3) that the PDFs do not carry; (b) there is no
    arithmetic anywhere in this file, so no figure here can ever become a number computed from a
    specific child's data; (c) per D-03 this file carries no calorie, macro, portion, serving-size,
    BMI, or weight-management content, extending HEALTH-04's `Under 18 (Minor)` prohibition;
    (d) per D-06 the scope is preteens and `KidContentCategory` has exactly two cases -- no
    toddler/preschool/school-age band exists or should be added speculatively; (e) per
    04.2-RESEARCH.md Pitfall 4 this catalog is NOT gated: it must never call
    `GuidanceCatalog.contentPermission(for:domain:)`, must never take a `ConditionTag` parameter,
    and must never add a `domain` case anywhere, because this content has no condition-tag
    dimension at all; (f) this content ships pending Phase 5's LAUNCH-02/LAUNCH-03
    clinical and legal review, same posture as `NutritionGuidanceCatalog`.

    `KidContentCategory`: `public enum KidContentCategory: String, Sendable, Equatable,
    CaseIterable` with exactly `case food` and `case movement`.

    `KidContentCitation`: `public struct KidContentCitation: Sendable, Equatable` with four
    `public let` `String` properties in this order -- `documentTitle`, `documentIdentifier`,
    `publishingBody`, `publicationDate` -- a memberwise `public init` taking them in the same
    order, and a computed `public var attribution: String` that joins them for display as
    `"{documentTitle}. {documentIdentifier}, {publishingBody}, {publicationDate}."`. String
    interpolation only; no conditional logic, no optionals.

    `KidContentIdea`: `public struct KidContentIdea: Sendable, Equatable, Identifiable` with
    `public let id: String`, `public let category: KidContentCategory`, `public let title: String`,
    `public let body: String`, `public let citation: KidContentCitation`, and a memberwise
    `public init` in that order. `id` is a stable slug, not a `UUID` -- these are compile-time
    constants, and a slug keeps a test failure legible.

    `KidContentCatalog`: `public enum KidContentCatalog` (a namespace, never instantiated).

    Three citation constants on `KidContentCatalog`, values exactly:
    - `veggiesAndFruitsCitation` -- documentTitle `"10 tips Nutrition Education Series: veggies and
      fruits"`, documentIdentifier `"DG TipSheet No. 11"`, publishingBody `"USDA Center for
      Nutrition Policy and Promotion"`, publicationDate `"June 2011"`.
    - `snackTipsCitation` -- documentTitle `"10 tips Nutrition Education Series: snack tips for
      parents"`, documentIdentifier `"DG TipSheet No. 24"`, publishingBody `"USDA Center for
      Nutrition Policy and Promotion"`, publicationDate `"March 2013"`.
    - `physicalActivityCitation` -- documentTitle `"Physical Activity Guidelines for Americans, 2nd
      edition"`, documentIdentifier `"CDC Physical Activity Basics: What Counts for Children and
      Teens"`, publishingBody `"Centers for Disease Control and Prevention / U.S. Department of
      Health and Human Services"`, publicationDate `"2018"`.

    Two source-preamble constants on `KidContentCatalog`, each transcribed verbatim from
    `docs/kid-content.md` and each carrying a doc comment naming which citation constant covers it:
    - `veggiesAndFruitsIntro` -- the section 1 preamble sentence, all three sentences, verbatim.
    - `snackTipsIntro` -- the section 2 preamble AS TRIMMED IN THE DOC (the doc's own third and
      fourth source sentences only). The doc's first two source sentences were already dropped
      under section 3's rule; do not restore them. Its doc comment must additionally record that
      the ChooseMyPlate.gov domain named inside this verbatim sentence is shipped as published and
      is a Phase 5 re-verification item, not something to silently modernise here -- D-01 forbids
      editing source wording.

    Then the first ten `KidContentIdea` entries, from section 1's numbered list, in the doc's own
    order. Each entry: `category: .food`, `citation: KidContentCatalog.veggiesAndFruitsCitation`,
    `title` = the doc's bolded tip heading exactly, `body` = the doc's quoted tip text exactly
    (without the surrounding quotation marks the doc uses to delimit it). Slugs, in order:
    `food.smoothie-creations`, `food.delicious-dippers`, `food.caterpillar-kabobs`,
    `food.personalized-pizzas`, `food.fruity-peanut-butterfly`, `food.frosty-fruits`,
    `food.bugs-on-a-log`, `food.homemade-trail-mix`, `food.potato-person`,
    `food.put-kids-in-charge`.

    Declare `public static let ideas: [KidContentIdea]` holding these ten now (Task 2 appends the
    rest to the same array), and `public static func ideas(in category: KidContentCategory) ->
    [KidContentIdea]` returning `ideas.filter { $0.category == category }` -- a filter and nothing
    else, no permission check, no tag parameter.

    Transcription rules, stated once and applied to every `body` and `title` in this task and
    Task 2: copy the characters exactly. Do not paraphrase, do not "fix" grammar, do not
    Americanise or Britishise spelling, do not expand or contract any word. The doc's own em
    dashes are its prose separator between a tip title and its quoted body and are never inside a
    quoted body -- so no dash rewriting is needed for any idea string, and none of these strings
    may contain an em dash or an en dash. Section 1 tip 8's body legitimately contains the word
    "favorite" and section 1 tip 4's body legitimately contains "favorites" -- that is source text
    about choosing foods, not a saved-state feature, and both stay exactly as published.
  </action>

  <verify>
    <automated>cd RithamCore && swift build && test "$(grep -c 'id: "food\.' Sources/RithamCore/Guidance/KidContentCatalog.swift)" -eq 10</automated>
  </verify>

  <acceptance_criteria>
    - `cd RithamCore && swift build` completes with no error and no warning introduced by this file.
    - `grep -c 'id: "food\.' RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift`
      outputs exactly `10`.
    - `grep -c 'case food' RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift` and
      `grep -c 'case movement' ...` each output exactly `1` -- `KidContentCategory` has exactly two
      cases.
    - `grep -c 'contentPermission\|ConditionTag\|GuidanceCatalog'
      RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift` outputs `0`: the file names
      no gating symbol anywhere, comments included (04.2-RESEARCH.md Pitfall 4).
    - A `swift` REPL or a scratch test is not required; the two greps plus the build are the gate
      for this task. Behavioural assertions land in Task 3.
    - Each of the ten `body` strings is character-identical to the corresponding quoted text in
      `docs/kid-content.md` section 1. Verify by diffing each string against the doc before
      committing; a single reworded clause is a D-01 violation even if it reads better.
  </acceptance_criteria>

  <done>
    `KidContentCatalog.swift` compiles, declares all four public types plus the three citation and
    two intro constants, and holds exactly ten verbatim `.food` ideas from section 1.
  </done>
</task>

<task type="auto">
  <name>Task 2: Transcribe the nine snack ideas and the eight movement ideas</name>

  <read_first>
    - `docs/kid-content.md` sections 2, 3, 4 and 5 -- section 2 for the nine snack ideas (three of
      them marked `(trimmed)`, whose shipped text is the doc's already-trimmed text, NOT the source
      PDF's fuller sentence), section 3 for the tenth tip that is excluded in full, section 4 for
      all movement content, section 5 for the delegated movement-grouping decision this task
      resolves.
    - `RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift` -- the file as Task 1 left
      it, so the append matches its existing formatting and the `ideas` array is extended rather
      than redeclared.
  </read_first>

  <files>RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift</files>

  <action>
    Append seventeen more `KidContentIdea` entries to `KidContentCatalog.ideas`, bringing the array
    to twenty-seven.

    Nine `.food` ideas from section 2, in the doc's own order, each with
    `citation: KidContentCatalog.snackTipsCitation`, `title` = the doc's bolded tip heading exactly
    (WITHOUT the doc's own `(trimmed)` marker, which is editorial annotation, not source text), and
    `body` = the doc's quoted text exactly as the doc shipped it. Slugs, in order:
    `food.save-time-by-slicing-veggies`, `food.mix-it-up`, `food.grab-a-glass-of-milk`,
    `food.go-for-great-whole-grains`, `food.nibble-on-lean-protein`,
    `food.fruits-are-quick-and-easy`, `food.consider-convenience`, `food.swap-out-the-sugar`,
    `food.prepare-homemade-goodies`.

    For the three tips the doc marks `(trimmed)`, the shipped `body` is the doc's quoted remainder
    only. The doc prints each removed clause after the quoted remainder, inside a parenthetical
    beginning "Source continues" or "Source reads" -- that parenthetical is the doc's own record of
    what was cut and must not become part of any shipped string. Specifically:
    `food.fruits-are-quick-and-easy` ends after the doc's second sentence; `food.consider-
    convenience` ends at the doc's own trailing ellipsis and ships as the doc prints it including
    that ellipsis; `food.swap-out-the-sugar` ships the doc's two-sentence quoted text with no
    measured juice figure anywhere in it.

    `food.prepare-homemade-goodies` ships in full, including its recipe-ratio phrasing -- section 3
    explicitly carves recipe-batch ingredient ratios out of the exclusion rule because they adjust
    a dish being prepared rather than telling a child how much of the finished food to eat.

    Do not add a tenth section 2 idea. The tenth tip in DG TipSheet No. 24 was excluded in full by
    section 3 and has no slug reserved for it; a tenth entry here is the exact regression
    04.2-RESEARCH.md Pitfall 5 names. Add a comment at the end of the section 2 block recording
    that nine of the source's ten tips ship and that the tenth was excluded by
    `docs/kid-content.md` section 3, so a future reader does not "fix" the count.

    Eight `.movement` ideas from section 4, each with
    `citation: KidContentCatalog.physicalActivityCitation`. Section 5 leaves the movement grouping
    to this plan; this is the resolved grouping -- four ideas carrying section 4's four verbatim
    guideline quotes, plus four ideas carrying CDC's own example-activity categories. Slugs,
    titles, and bodies, in order:

    1. `movement.minutes-every-day`, title `"Minutes every day"`, body = section 4's "Daily target"
       first verbatim quote (the one naming the age range and the daily minimum as a single
       sentence).
    2. `movement.weekly-mix`, title `"The weekly mix"`, body = section 4's two "three required
       components each week" verbatim quotes, joined into one string with a single space between
       them, in the order the doc prints them.
    3. `movement.vigorous-days`, title `"Vigorous days"`, body = section 4's "Vigorous-intensity
       aerobic frequency" first verbatim quote (the longer of the two, beginning with the word
       "Most").
    4. `movement.intensity-check`, title `"Telling moderate from vigorous"`, body = section 4's
       "Intensity guidance" verbatim quote, both sentences.
    5. `movement.aerobic-moderate`, title `"Moderate-intensity aerobic activity"`, body = section
       4a's three moderate-intensity example activities, verbatim, joined as sentences separated by
       a period and a space, with a trailing period.
    6. `movement.aerobic-vigorous`, title `"Vigorous-intensity aerobic activity"`, body = section
       4a's three vigorous-intensity example activities, joined the same way.
    7. `movement.muscle-strengthening`, title `"Muscle-strengthening activity"`, body = section 4b's
       two example activities plus the companion-page push-up example the doc names, joined the
       same way, with the push-up example sentence-capitalised.
    8. `movement.bone-strengthening`, title `"Bone-strengthening activity"`, body = section 4c's
       three example activities, joined the same way.

    Titles 1 through 4 are Ritham's own short labels: section 4 prints those four quotes under
    descriptive headings, not under source-authored tip titles, so a label had to be supplied.
    Titles 5 through 8 are CDC's own activity-category names. Record this distinction in a comment
    immediately above the movement block, alongside a note that joining CDC's bulleted example
    activities into period-separated sentences and sentence-capitalising the push-up example are
    presentation transforms of the same class as the dash rewrite `NutritionGuidanceCatalog`'s
    header describes -- no word is added, removed, or reordered.

    Do not introduce any per-idea numeric interpretation, any "3 minutes" reading of the vigorous
    figure (section 4a warns about exactly that misreading), any age band beyond the source's own
    stated range, and no new type.
  </action>

  <verify>
    <automated>cd RithamCore && swift build && test "$(grep -c 'id: "food\.' Sources/RithamCore/Guidance/KidContentCatalog.swift)" -eq 19 && test "$(grep -c 'id: "movement\.' Sources/RithamCore/Guidance/KidContentCatalog.swift)" -eq 8</automated>
  </verify>

  <acceptance_criteria>
    - `cd RithamCore && swift build` completes with no error.
    - `grep -c 'id: "food\.' RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift`
      outputs exactly `19`.
    - `grep -c 'id: "movement\.' RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift`
      outputs exactly `8`.
    - `grep -c 'contentPermission\|ConditionTag\|GuidanceCatalog'` on the same file still outputs
      `0`.
    - Every one of the seventeen new `body` strings is character-identical to the corresponding
      quoted text in `docs/kid-content.md`, except the four movement example-activity bodies, whose
      only permitted difference from the doc is the period-and-space joining and the single
      sentence-capitalisation named in the action above.
    - No shipped `body` or `title` contains the fully-excluded tenth snack tip's wording. Task 3
      turns this into a standing automated assertion.
  </acceptance_criteria>

  <done>
    `KidContentCatalog.ideas` holds exactly 27 entries -- 19 `.food`, 8 `.movement` -- every one
    verbatim from `docs/kid-content.md`, with the excluded tenth snack tip absent.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add KidContentCatalogTests with counts, prohibited-content sweep, and the Pitfall 5 regression gate</name>

  <read_first>
    - `RithamCore/Tests/RithamCoreTests/SocialCopyTests.swift` -- the lexicon-sweep shape to mirror:
      a private static banned-phrase array with a header comment explaining why each entry is a
      phrase rather than a bare stem, a non-vacuity "at least N strings scanned" test, and a
      per-string loop that reports the offending constant name in its failure message.
    - `RithamCore/Tests/RithamCoreTests/NutritionGuidanceCatalogTests.swift` -- the existing
      catalog-test conventions in this target.
    - `RithamCore/Sources/RithamCore/Guidance/KidContentCatalog.swift` -- the file as Tasks 1 and 2
      left it.
    - `docs/kid-content.md` section 3 -- the exclusion rule and the exact wording of the tip
      excluded in full, which this task's regression gate asserts against.
  </read_first>

  <files>RithamCore/Tests/RithamCoreTests/KidContentCatalogTests.swift</files>

  <action>
    Create `KidContentCatalogTests.swift` as `@Suite("KidContentCatalogTests") struct
    KidContentCatalogTests`, importing `Foundation`, `Testing`, and `@testable import RithamCore`.
    It creates no `ModelContainer` and touches no `StepRegistry`, so it nests under no parent suite
    and needs no `.serialized` trait.

    Build one private static `scannedStrings: [(name: String, value: String)]` covering every
    string the catalog exposes: for each of the 27 ideas, one entry for its `title` and one for its
    `body` (named by slug plus which field, e.g. `"food.mix-it-up.body"`), plus
    `veggiesAndFruitsIntro`, `snackTipsIntro`, and the four `String` fields of each of the three
    citation constants. Build it by mapping over `KidContentCatalog.ideas` rather than hand-listing
    27 entries, so a future idea is swept automatically.

    Tests to write:

    1. Non-vacuity: `scannedStrings.count >= 60` with a failure message saying the sweep below
      cannot be trusted until it scans real strings. This is `SocialCopyTests
      .atLeastTwentyStringsScanned`'s role -- without it every negative assertion passes on an
      empty array.
    2. Exact counts: `KidContentCatalog.ideas.count == 27`,
      `KidContentCatalog.ideas(in: .food).count == 19`,
      `KidContentCatalog.ideas(in: .movement).count == 8`, and
      `KidContentCategory.allCases.count == 2`.
    3. Identity: every `ideas` slug is unique (compare `Set(ids).count` to `ideas.count`), and
      every slug's prefix matches its own category -- a `.food` idea's slug begins `food.` and a
      `.movement` idea's begins `movement.`.
    4. Shape: no `title`, `body`, or citation field anywhere is empty or whitespace-only after
      trimming.
    5. Prohibited content (D-03): case-insensitively, no swept string contains any of exactly these
      phrases -- `calorie`, `calories`, `kcal`, `bmi`, `body mass index`, `weight loss`,
      `weight-loss`, `lose weight`, `healthy weight`, `weight management`, `portion`,
      `serving size`, `serving sizes`, `macro`, and the vulgar-fraction one-half character. A
      header comment above the array must record why the list is phrases and not bare stems, naming
      the specific shipped strings that a bare stem would falsely trip: the word weight appears
      inside a CDC resistance-exercise example; the word serving appears inside a yogurt-container
      snack tip; the words limit, cup, half, amount and favorite all appear in legitimately shipped
      USDA tip text; and a bare percent sign appears in two snack tips as a juice-purity
      descriptor. None of those six may be added to the banned list.
    6. Pitfall 5 regression gate, as its own test: case-insensitively, no swept string contains any
      of `how much is enough`, `control serving sizes`, or `keep an eye on the size`. Its failure
      message must say the tip `docs/kid-content.md` section 3 excluded in full has been
      reintroduced, and that a revision must transcribe from that document rather than from the
      source PDF.
    7. House style: no swept string contains an em dash (`\u{2014}`) or an en dash (`\u{2013}`),
      mirroring `SocialCopyTests.noShippedStringContainsADash`. This is the catalog half of
      04.2-VALIDATION.md's row TBD-02; plan 04.2-02's `KidContentCopyTests` covers the copy half,
      so the two suites stay independent and run in parallel.
    8. Verbatim spot checks, one per source document, each asserting a full `body` string equals its
      exact expected literal: `food.bugs-on-a-log` (DG TipSheet No. 11),
      `food.grab-a-glass-of-milk` (DG TipSheet No. 24), and `movement.intensity-check` (CDC). These
      are the tripwires that catch a well-meaning reword that the phrase sweep would let through.
    9. Citation integrity: every `.food` idea's citation is one of the two USDA constants and every
      `.movement` idea's citation is the CDC constant; and `KidContentCitation.attribution` for
      `veggiesAndFruitsCitation` equals its expected assembled literal.

    Add a comment near the top recording a deliberate non-test: `docs/kid-content.md` section 3's
    broader rule (exclude any clause naming a measured serve amount of a consumed food) is NOT
    asserted here, because two strings the document itself ships would fail it -- section 2 tip 3's
    milk quantity and section 2 tip 9's recipe-ratio phrasing, the latter explicitly carved out by
    section 3 itself. Resolving that tension is a Phase 5 clinical-review judgment (04.2-RESEARCH.md
    Assumptions Log A2 and Open Question 2), not this phase's to settle in code.
  </action>

  <verify>
    <automated>cd RithamCore && swift test --filter KidContentCatalogTests</automated>
  </verify>

  <acceptance_criteria>
    - `cd RithamCore && swift test --filter KidContentCatalogTests` exits zero with every test
      passing and at least twelve tests reported.
    - `cd RithamCore && swift test` (the whole RithamCore suite) exits zero -- no pre-existing test
      regressed.
    - Temporarily appending a 28th idea whose body contains the word `calories` makes the
      prohibited-content test fail, and removing it makes it pass again. Run this check, then
      revert it; do not commit the temporary idea. This proves the sweep is live rather than
      vacuous.
    - `grep -c 'ModelContainer\|StepRegistry'
      RithamCore/Tests/RithamCoreTests/KidContentCatalogTests.swift` outputs `0` -- this suite must
      not become a container-touching or registry-touching suite, which would require nesting it
      under a serialization parent that does not exist in this target.
  </acceptance_criteria>

  <done>
    `KidContentCatalogTests` passes, pins the 19/8/27 counts, proves no prohibited nutrition figure
    or excluded-tip wording ships, and holds three full-string verbatim tripwires.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/kid-content.md` -> `KidContentCatalog.swift` | The citation trail crosses here. Nothing untrusted enters the process, but the provenance claim this content ships on is only as good as this transcription step. |
| Compiled catalog -> parent's screen | Static, compile-time data; no runtime input, no network, no per-user computation crosses this edge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.2-01 | Tampering | `KidContentCatalog.swift` content strings | medium | mitigate | A future edit that rewords a shipped string breaks the verbatim claim LAUNCH-02/LAUNCH-03 review depends on. Task 3's three full-string verbatim spot checks plus the file-header rule naming `docs/kid-content.md` as sole source convert the worst instances into test failures; the rest stays a code-review control, exactly as `NutritionGuidanceCatalog`'s own verbatim claim is enforced today. |
| T-04.2-02 | Information Disclosure | Prohibited pediatric content (D-03) | high | mitigate | A reintroduced calorie, portion, or weight-management figure aimed at a child is the phase's highest-consequence content failure. Task 3's phrase sweep plus the dedicated Pitfall 5 gate fail the build on the specific wording `docs/kid-content.md` section 3 already excluded. |
| T-04.2-03 | Elevation of Privilege | Gating bypass via `GuidanceCatalog` | low | accept | This catalog deliberately has no permission gate (04.2-RESEARCH.md Pitfall 4) because the content carries no condition-tag dimension. Accepted: the content is general, non-personalized public health guidance with no user-specific input. The grep in Tasks 1 and 2 keeps a fake gate from being wired in by reflex. |
| T-04.2-SC | Tampering | Package-manager installs | n/a | n/a | Not applicable. This plan installs no npm, pip, or cargo package and adds no SwiftPM dependency -- `RithamCore/Package.swift` is untouched. 04.2-RESEARCH.md's Package Legitimacy Audit records the same finding for the whole phase. No legitimacy checkpoint is required. |
</threat_model>

<verification>
- `cd RithamCore && swift build` clean.
- `cd RithamCore && swift test` fully green, including the new `KidContentCatalogTests`.
- `KidContentCatalog.ideas.count == 27`, split 19 food / 8 movement.
- No string exposed by the catalog contains a prohibited nutrition figure, the excluded tenth tip's
  wording, an em dash, or an en dash.
- `KidContentCatalog.swift` names no permission, gate, domain, or `ConditionTag` symbol anywhere.
</verification>

<success_criteria>
KIDCONTENT-01's content layer exists in `RithamCore`, every idea traceable to a named publishing
body and date, with automated gates standing behind the three content rules this phase cannot
afford to lose: verbatim transcription (D-01/D-02), no weight or quantity framing (D-03), and no
reintroduction of the source tip already excluded on those grounds.
</success_criteria>

<output>
Create `.planning/phases/04.2-parent-facing-kid-ideas-food-and-movement-content/04.2-01-SUMMARY.md`
when done.
</output>
