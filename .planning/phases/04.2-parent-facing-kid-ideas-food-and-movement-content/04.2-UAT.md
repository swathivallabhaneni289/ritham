---
status: partial
phase: 04.2-parent-facing-kid-ideas-food-and-movement-content
source: [04.2-05-SUMMARY.md, 04.2-05-PLAN.md Task 3]
started: 2026-09-12T00:00:00Z
updated: 2026-09-12T00:00:00Z
---

## Current Test

[testing paused — 8 items outstanding, all blocked on the project's standing 2026-09-06
batched end-of-project physical-device/infrastructure verification pass, per PROJECT.md Key
Decisions. Not phase-specific failures — every automated check that can run in this environment
has run and passed. See `04.2-05-SUMMARY.md`'s Checkpoint Resolution section and
`Phase42CoverageTests` (ten structural gates, two consecutive green full-target runs) for the
full automated evidence trail.]

## Tests

### 1. Settings reachability and dashboard silence
expected: Launch the app, complete or restore onboarding to reach the dashboard, open Settings.
A row labelled "Kid Ideas" appears between "Privacy Zones" and the "Screening answers" group, and
the dashboard itself shows no kid-content section anywhere.
result: blocked
blocked_by: physical-device
reason: Requires driving Simulator UI (launch, onboarding, navigation) or a physical device; no
touch-injection tool (idb/XCUITest) is available in this environment, only simctl. The structural
half (dashboard never mentions kid content, Settings still constructs `KidIdeasView`) is already
proven by `Phase42CoverageTests` gates 3 and 4, including non-vacuity proof by deliberate breakage
and revert.

### 2. Screen ordering
expected: Tap the "Kid Ideas" row. The screen opens as a sheet and shows, in order: the intro
line, the food ideas with their two source preambles and two source attributions, the movement
ideas with their source attribution, the disclaimer, then the child-entry section, then Done.
result: blocked
blocked_by: physical-device
reason: Requires visual inspection of on-screen ordering; not executable without touch injection
or a physical device in this environment.

### 3. No per-idea tap affordance
expected: No idea row is tappable, and nothing on the screen offers to save, favourite, or mark
an idea.
result: blocked
blocked_by: physical-device
reason: Requires interactive tap-testing on Simulator or device. The structural half (no
`favorite`/`favourite`/`bookmark`/`isTried`/`markTried`/`viewedIdea`/`seenIdea`/`@Model`/
`FetchDescriptor<` token in `KidIdeasView.swift`'s source, D-09) is already proven by
`Phase42CoverageTests` gate 6.

### 4. Multi-child add/rename/delete/renumber
expected: Tap "Add a child" twice. Two rows appear labelled "Child 1" and "Child 2". Renaming the
first leaves the second unchanged. Deleting the first renumbers the remaining row to "Child 1".
result: blocked
blocked_by: physical-device
reason: Requires multi-step interactive UI testing (tap, text entry, delete) on Simulator or
device; no touch-injection tool available. The underlying CRUD/renumbering logic is unit-tested
in `ChildEntryTests` against `ChildEntryStore`/`HealthDataStore`, but the live on-screen behavior
is unverified.

### 5. Persistence across relaunch
expected: Force-quit and relaunch the app. Reopening the Kid Ideas screen shows the child entries
survived.
result: blocked
blocked_by: physical-device
reason: Requires an actual force-quit/relaunch cycle on Simulator or device; not executable
without touch injection. The persistence layer itself (SwiftData `ChildEntryRecord` round trip)
is unit-tested.

### 6. AX3/AX5 legibility
expected: Set the system text size to AX3 and then AX5. Every idea body, the disclaimer, and
every control remains legible and reachable, with no text truncating.
result: blocked
blocked_by: physical-device
reason: Requires changing system accessibility text-size settings and visually inspecting layout
on Simulator or device — the same class of AX3/AX5 accessibility check plans 02-16 and 04.1-17
already deferred to this project's standing batched pass.

### 7. Dashboard/onboarding unreachability (interactive confirmation)
expected: Confirm the screen is unreachable from the dashboard and from any onboarding screen, by
navigating both flows live.
result: blocked
blocked_by: physical-device
reason: Requires live navigation through the dashboard and onboarding flows on Simulator or
device. The structural half is already proven by `Phase42CoverageTests` gates 4 (dashboard) and 5
(onboarding directory walk).

### 8. Overall developer sign-off on the Kid Ideas screen
expected: A human runs steps 1-7 above end-to-end and confirms the Kid Ideas screen behaves and
reads correctly as a whole, reporting any deviation instead of approving.
result: blocked
blocked_by: physical-device
reason: This is the composite human-judgment step from plan 04.2-05 Task 3's `<how-to-verify>`
block; it depends on steps 1-7 above and carries the same environment constraint. The developer
explicitly deferred this checkpoint (response: "defer") to the batched end-of-project pass rather
than attempting it now.

## Summary

total: 8
passed: 0
issues: 0
pending: 0
skipped: 0
blocked: 8

## Gaps

<!-- None of the 8 items above are gaps in the "something is broken" sense — all are expected,
     environment-constrained deferrals per PROJECT.md's locked batching decision, explicitly
     confirmed by the developer's "defer" response to plan 04.2-05's Task 3 checkpoint. Every
     automated check that can run in this environment (Phase42CoverageTests' ten structural
     gates, two consecutive green full-target xcodebuild runs, one green RithamCore swift test
     run) has run and passed — see 04.2-05-SUMMARY.md. No YAML gap entries; nothing here should
     route through /gsd-plan-phase --gaps. -->
