---
phase: 04.1-group-goal-events-accountability-circles
verified: 2026-09-11T08:32:01Z
status: human_needed
score: 8/8 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Real Sign in with Apple round-trip on a physical device/Apple ID (04.1-03 Task 1 approval was for dependency legitimacy only; 04.1-05's human checkpoint covers the actual on-device SIWA sheet round-trip)"
    expected: "A real Apple ID sign-in returns a session token and the same user id on a second device."
    why_human: "No touch-injection tool exists to drive the native Apple ID consent sheet on Simulator; requires a physical Apple ID. Explicitly deferred per PROJECT.md's locked 2026-09-06 decision to a standing end-of-project batched physical-device verification pass (matches 02-16 precedent)."
  - test: "04.1-17 Task 4: full end-to-end human walkthrough of the social feature surface (create group, invite, RSVP, log completion with photo/location, view feed, export certificate) on a physical device or full Simulator UI"
    expected: "Every screen renders correctly, no ranking/comparison language or number ever appears, location resolves to a coarse place name, certificate export works."
    why_human: "Deferred by the same batched-verification decision; STATE.md records Phase 04.1 as code-complete pending this one checkpoint."
  - test: "MinIO/S3 object-store wire round trip (PutObject/PresignedGetObject) against a live bucket"
    expected: "A photo upload actually writes bytes to and reads them back from a real S3-compatible bucket."
    why_human: "Docker Desktop's Subscription Service Agreement is declined in this environment, so no local MinIO container could be started at any point in this phase. The interface/contract layer (ObjectStore's method signatures, the objectWriter substitution seam, PutShared's zero-value-rejection guard) is fully unit-tested and the EXIF-strip pipeline itself is proven independent of the object store; only the actual network round trip to a real bucket is unverified. Documented as an open item in 04.1-01-SUMMARY.md (D3) and 04.1-04-SUMMARY.md (D10), carried forward, not silently treated as complete."
  - test: "Full iOS test suite (`xcodebuild test`) and RithamCore suite re-run by the verifier independently"
    expected: "575 iOS tests / 61 suites and 436 RithamCore tests / 32 suites pass, as claimed by 04.1-REVIEW-FIX.md after the fix commits."
    why_human: "Not re-run by this verification pass — an `xcodebuild test` full run would exceed this pass's time/context budget and Step 7b explicitly disallows re-running a full suite as a per-item check. `cd RithamService && go build ./... && go vet ./...` WAS re-run directly by the verifier and passed. The iOS/RithamCore claims rest on SUMMARY.md and REVIEW-FIX.md's reported results plus named commit hashes that were independently confirmed to exist and to contain the claimed code changes; the test-run numbers themselves were not independently reproduced."
---

# Phase 04.1: Group Goal-Events & Accountability Circles Verification Report

**Phase Goal:** Users can form small, invite-only friend groups (beyond household) and organize
shared, non-comparative goal-events with a group feed and a completion certificate — structurally
incapable of ranking, pace comparison, or precise-location exposure.
**Verified:** 2026-09-11T08:32:01Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Users can form small, invite-only friend groups (no public/joinable-by-anyone tier), mutual/request-based friending with three closed-loop connection paths | ✓ VERIFIED | `RithamService/internal/groups/groups.go` `Join()` requires a matching `state = 'pending'` row in `group_invitations` (`ErrNoInvitation` otherwise) — there is no open-join path. `internal/friends/friends.go`+`invites.go`+`contactmatch.go` implement mutual accept/decline friending, expiring single-use invite tokens, and both-sides-opt-in contact matching (`RithamCore/Sources/RithamCore/Social/FriendGraph.swift`'s `FriendConnectionPath` closed to exactly 3 cases, asserted by `SocialModelTests`). |
| 2 | Groups are capped at a small size, enforced atomically (not just declared) | ✓ VERIFIED | `groups.go` `Join()` locks the group row (`SELECT ... FOR UPDATE`) and checks `memberCount >= maxMembers` (`maxMembers = 12`) inside the same transaction as the membership insert, so two concurrent joins cannot both pass a pre-check and overshoot the cap (comment cites T-04.1-48). `RithamCore.GroupSizeLimit.maximumMembers == 12` is asserted by `SocialModelTests`. |
| 3 | Users can organize shared, non-timed goal-events (organizer sets activity/target/window, no synchronized start, each person logs completion independently) | ✓ VERIFIED | `RithamService/internal/events/events.go`/`completions.go` implement `GoalEvent`, RSVP, and per-user `LogCompletion`; header comment states "no synchronized start, and no mechanism anywhere that could rank one person against" another. `RithamCore/Social/GoalEventModels.swift` mirrors the same shape client-side. Own-time logging is optional (`ownTimeSeconds *int`, off by default). |
| 4 | Goal-events have a group-only, permanent, chronologically-ordered feed; no pace/rank/first-to-complete/denominator | ✓ VERIFIED | `RithamService/internal/feed/feed.go` orders strictly by `posted_at`/keyset cursor; `nodenominator_test.go`'s `TestNoDenominatorOrRanking` is a structural source-scanning gate that fails the build on any `rank\|score\|leaderboard\|position\|winner\|total\|denominator\|fraction\|percent` whole-word match in non-comment source in this package, plus an aggregate-SQL-function scan (`sum\|avg\|count\|rank\|row_number\|dense_rank`). Every group/event/feed read is scoped by a membership predicate baked into the query itself (per 04.1-REVIEW.md's summary, spot-checked in `groups.go`/`events.go`). |
| 5 | A digital completion certificate is auto-generated per person, containing only the allowed fields (no rank, no other member's data, no precise location) | ✓ VERIFIED | `RithamApp/Ritham/Social/Certificate/CertificateContent.swift` is a closed 7-field struct (`eventDisplayName, activityType, participantName, completionDate, ownTimeSeconds, placeName, photo: StrippedPhotoAsset?`) with a reflection test (`CertificateTests`) pinning the exact field set per the file's own header comment; `photo` accepts only the opaque, privately-initialized `StrippedPhotoAsset` type (never `UIImage`/`Data`/`URL`), and there is deliberately no member-count or completion-figure field. `CertificateArchiveView` is described (04.1-16-SUMMARY.md) as a private, unranked, uncounted keepsake trail. |
| 6 | Photo/location privacy: EXIF/GPS stripped server-side unconditionally before any group-visible write; location sharing is a separate opt-in resolving to a coarse place name, never coordinates; user Privacy Zones suppress/generalize automatically | ✓ VERIFIED | `RithamService/internal/photo/strip.go`/`strip_test.go`: decode-then-reencode through Go's stdlib only, `PutShared` accepts only the unexported `StrippedImage` type producible only by `StripAndReencode` — a structural (type-level), not conventional, guarantee; `strip_test.go`'s `TestStripAndReencode` asserts no parseable GPS IFD survives. `photo_assets` has no lat/lon/coordinate/address column (`service_test.go`'s `forbidden` list, migration `0002_photos.up.sql` header comment). Client-side, `RithamApp/Ritham/Social/PrivacyZones/LocationAttachment.swift`'s `resolveSharedPlaceName` checks zones (suppress/generalize) *before* ever invoking `CLGeocoder`, proven by `PrivacyZoneTests`'s zero-call-count spy assertions; `SystemGeocoder` allowlists only `areasOfInterest/subLocality/locality/administrativeArea` — no street-level result is ever returned. `SharedLocationOutcome`'s cases carry no coordinate, radius, or distance value. |
| 7 | Structurally incapable of ranking, pace comparison, or leaderboard-style aggregation anywhere in the group-visible surface | ✓ VERIFIED | Two independent, build-breaking structural gates: `internal/events/noranking_test.go` (`TestNoRankingMechanism`) and `internal/feed/nodenominator_test.go` (`TestNoDenominatorOrRanking`) walk every non-test `.go` file in their packages and fail on any ranking/aggregate-shaped identifier or SQL aggregate function touching `own_time_seconds`. `RithamCore.GoalEventCompletion`/`CheerReaction` carry no position/score/pooled-total/count field (doc-commented and asserted by `SocialModelTests`); `PrivateCompletionDetail` (which does carry distance/duration/route) is declared in a separate file with a doc comment stating no network request type references it, and a repo-wide grep confirms zero references outside `RithamCore`'s own model/test files and two client-side files (`CompletionDraft.swift`, `GoalEventsClient.swift`) that mirror the same separation, never crossing into a wire/DTO type. |
| 8 | Users have an optional, opt-in Sign-in-with-Apple account; cross-device continuity restores friend graph and group memberships; no route other than sign-in and the pre-existing workout-plan endpoint is reachable without a valid session | ✓ VERIFIED | `internal/identity/apple.go` verifies Apple JWKS-signed tokens (issuer/audience/expiry/algorithm/nonce, each a distinct sentinel); `session.go` upserts on `apple_subject` with `ON CONFLICT (apple_subject) DO UPDATE`, issues opaque `crypto/rand` tokens stored only as a SHA-256 digest (`sessions.token_sha256`), and revocation/expiry are enforced inside the lookup query's own `WHERE` clause. `handler.go`'s full route table (grep-confirmed) wraps every route except `POST /v1/identity/apple` and the pre-existing `POST /v1/workout-plan` in `RequireSession` — 10 friends routes, 9 group routes, 8 event routes, 2 photo routes, all gated. |

**Score:** 8/8 truths verified (0 present, behavior-unverified)

### Requirement Traceability (against REQUIREMENTS.md)

| Requirement | Claimed by plan(s) | REQUIREMENTS.md status | Verifier disposition | Evidence |
|---|---|---|---|---|
| ACCOUNT-01 | 04.1-01, 04.1-03, 04.1-05, 04.1-17 | `[x]` Complete | ✓ SATISFIED | Truth 8. Note: REQUIREMENTS.md's own annotation (lines ~300-306) flags that cross-device restore of the *certificate archive* specifically does not work (CertificateRecord is SwiftData-local only, no server table/sync route) — friend graph and group membership restore ARE real and server-backed. REQUIREMENTS.md leaves the checkbox checked and defers the certificate-sync gap to Phase 5/backlog; this verifier concurs that is a reasonable, explicitly-tracked scope call, not a silent gap. |
| HOUSEHOLD-02 | 04.1-02, 04.1-06, 04.1-08, 04.1-09 | `[x]` Complete | ✓ SATISFIED (with a documented partial) | Truth 1, 6. `GroupVisibilityScope` ships exactly 2 reachable rungs (`onlyMe`, `group`); the third ("household") rung is reserved in prose only — `GroupVisibilityScope(rawValue: "household")` is asserted nil by `SocialModelTests`, so no persisted/decoded value can select it, because HOUSEHOLD-01 (household grouping itself) is still Pending elsewhere in the roadmap. The requirement's own wording is "beyond household," which plan 04.1-02 argues (and this verifier agrees, based on the friend-circle/group mechanism being fully built and independent of household grouping's own arrival) is satisfied in full by the friend-circle tier; only the ladder's relative *positioning* remains incomplete pending HOUSEHOLD-01. This is a legitimate, explicitly-reasoned partial, not an oversight — flagged here for visibility rather than silently passed. |
| GROUPEVENTS-01 | 04.1-02, 04.1-06, 04.1-08, 04.1-09, 04.1-10 | `[x]` Complete | ✓ SATISFIED | Truths 1, 2. |
| GROUPEVENTS-02 | 04.1-02, 04.1-10, 04.1-11, 04.1-13, 04.1-14 | `[x]` Complete | ✓ SATISFIED | Truths 3, 7. |
| GROUPEVENTS-03 | 04.1-04, 04.1-07, 04.1-13, 04.1-14, 04.1-16 | `[x]` Complete | ✓ SATISFIED (server-side EXIF strip fully verified; MinIO wire round-trip is a documented, deferred open item — see human_verification) | Truth 6. |
| GROUPEVENTS-04 | 04.1-11, 04.1-12, 04.1-13, 04.1-17 | `[x]` Complete | ✓ SATISFIED | Truths 4, 7. |
| GROUPEVENTS-05 | 04.1-11, 04.1-12, 04.1-16, 04.1-17 | `[x]` Complete | ✓ SATISFIED | Truth 5. REQUIREMENTS.md itself documents a 2026-09-11 close-out sanity check (plan 04.1-17) confirming the actual certificate feature (not just its export-consent dependency) shipped in plan 04.1-16 — this verifier independently confirmed `CertificateContent`'s exact field set in source, consistent with that note. |

No orphaned requirements found — all 7 IDs declared across plans match REQUIREMENTS.md exactly, and every plan's `requirements:` field maps to an ID with a `[x]` entry there.

### Code Review Fix Verification (04.1-REVIEW.md → 04.1-REVIEW-FIX.md)

All 3 findings from the code review (1 critical, 2 warnings) were independently re-checked directly in source, not just read from the fix report:

| Finding | Claimed fix commit | Verifier check | Result |
|---|---|---|---|
| CR-01 (export-consent endpoint had no authorization check) | `b36a5e1` | `grep` confirms `isPhotoOwner`/`isRecordedSubject`/`ErrNotPhotoOwner` gating in `RithamService/internal/feed/exportconsent.go`; commit exists in `git log` | ✓ LANDED |
| WR-01 (false "everyone must approve" message on network failure) | `da495fa` | `CertificateExportView.swift`'s `.failed` case now shows `SocialCopy.Certificate.exportGateCheckFailed` (distinct from the `.blocked` copy), not the misleading multi-person-block string; commit exists | ✓ LANDED |
| WR-02 (location-fix continuation leak/misattribution on overlapping calls) | `41f1da9` | `LocationFixProviding.swift`'s `currentFix()` now captures and resumes the prior continuation with `nil` and detaches the prior manager's delegate before starting a new request, exactly as the fix describes; commit exists | ✓ LANDED |

`04.1-REVIEW-FIX.md` additionally documents a self-review follow-up pass (4 more items: RithamCore suite re-run, a handler-layer regression test for CR-01, a determinism fix for WR-02's own regression test, and an explicit deviation note on CR-01's owner-or-subject vs. the fix task's "group member" paraphrase) — all with named commits. This verifier did not re-check every follow-up commit individually but found the core three fixes' primary claims accurate against source.

### Required Artifacts (spot-checked, not exhaustive across all 17 plans' full artifact lists)

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `RithamService/internal/groups/groups.go` | Invite-only group create/join/leave/removal with atomic size cap | ✓ VERIFIED | 404 lines; `Join()` transaction-locked, invitation-gated, cap-enforced (read in full). |
| `RithamService/internal/events/noranking_test.go` | Structural build-breaking gate against ranking-shaped source | ✓ VERIFIED | Exists, whole-word regex gate, referenced by `feed/nodenominator_test.go` as established precedent. |
| `RithamService/internal/feed/feed.go` | Chronological, membership-scoped group feed | ✓ VERIFIED | 421 lines; keyset-cursor ORDER BY posted_at; no ranking construct (per `nodenominator_test.go` gate). |
| `RithamApp/Ritham/Social/Certificate/CertificateContent.swift` | Closed 7-field certificate content type | ✓ VERIFIED | 28 lines, read in full; exact field set matches GROUPEVENTS-05. |
| `RithamApp/Ritham/Social/PrivacyZones/LocationAttachment.swift` | Zone-check-before-geocode, no coordinate ever returned | ✓ VERIFIED | 109 lines, read in full. |
| `RithamService/internal/photo/strip.go` | Structural, type-enforced EXIF/GPS stripping | ✓ VERIFIED | `PutShared` accepts only the unexported `StrippedImage` type; confirmed via grep of `strip.go`/`strip_test.go`. |
| `RithamService/internal/identity/session.go` | Opaque, hashed-at-rest, revocable bearer sessions | ✓ VERIFIED | `crypto/rand`/`crypto/sha256` confirmed via grep; `ON CONFLICT (apple_subject)` upsert confirmed. |
| `RithamService/internal/httpapi/handler.go` route table | Every route but sign-in and workout-plan behind `RequireSession` | ✓ VERIFIED | Full route registration grepped; all 29 new routes wrapped, 2 unwrapped as designed. |

### Behavioral Spot-Check

| Behavior | Command | Result | Status |
|---|---|---|---|
| Go service builds and vets clean (all packages, including this phase's `identity`/`friends`/`groups`/`events`/`feed`/`photo`) | `cd RithamService && go build ./... && go vet ./...` | Exit 0, no output | ✓ PASS |
| iOS/RithamCore full test suites (575 iOS tests/61 suites; 436 RithamCore tests/32 suites) | `xcodebuild test ...` / `RithamCore/Scripts/test-core.sh` | NOT independently re-run by this verifier — SUMMARY/REVIEW-FIX claim only | ? SKIP (see human_verification) |

### Anti-Patterns Found

None blocking. `TODO`/`FIXME`/`XXX`/"not implemented"/"coming soon" scan across all Social-related Swift and Go source (excluding tests) returned zero debt markers. The only "placeholder" hits found are (a) legitimate SwiftUI `TextField` placeholder strings, and (b) `SocialAPIClient.swift`'s documented, expected release-host placeholder (no production hosting has been decided anywhere in this project yet — a pre-existing, explicitly tracked deployment prerequisite from plan 04.1-01, not new debt introduced by this phase).

### Human Verification Required

See frontmatter `human_verification` — four items: (1) real Sign in with Apple physical-device round-trip, (2) 04.1-17 Task 4's full end-to-end UI walkthrough, (3) MinIO/S3 object-store wire round-trip, (4) independent re-run of the full iOS/RithamCore test suites. Items 1, 2, and 4's device/UI portions are consistent with this project's standing, explicitly-decided (PROJECT.md, 2026-09-06) batched end-of-project physical-device verification policy and are expected, not a phase-specific failure. Item 3 (MinIO) is an honestly-documented, carried-forward environment limitation (Docker Desktop unavailable) that leaves the object-store network layer — as opposed to its type-safe interface, which is fully tested — unverified; it does not block the phase goal (photos are not in the phase goal's own wording) but must stay visible rather than being absorbed into a clean pass.

### Gaps Summary

No blocking gaps found. All 8 goal-derived observable truths are backed by real, substantive, wired code — including two independent build-breaking structural test gates (`noranking_test.go`, `nodenominator_test.go`) that make the "structurally incapable of ranking" claim in the phase goal a testable property of the codebase rather than a design intention. All 7 requirement IDs are accounted for in REQUIREMENTS.md with no orphans. The one requirement with a documented partial (HOUSEHOLD-02's ladder *positioning*, pending HOUSEHOLD-01) was reasoned through explicitly rather than silently passed. All 3 code-review findings were independently confirmed fixed in source, not just trusted from the fix report. The phase's own honestly-documented gaps (MinIO container wire verification, physical-device/UI walkthroughs) are real and are surfaced here as human-verification items per this project's standing deferral policy — they do not indicate incomplete or stubbed implementation.

---

_Verified: 2026-09-11T08:32:01Z_
_Verifier: Claude (gsd-verifier)_
