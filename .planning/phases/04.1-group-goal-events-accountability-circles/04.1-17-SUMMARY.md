---
phase: 04.1-group-goal-events-accountability-circles
plan: 17
subsystem: testing
tags: [swift-testing, go-testing, structural-gates, source-scanning, privacy-review, roadmap]

# Dependency graph
requires:
  - phase: 04.1-01 through 04.1-16
    provides: "Every Phase 4.1 social feature (identity, friends, groups, goal-events, privacy zones, completion logging, group feed, certificates) this plan's gates cover"
provides:
  - "SocialCoverageTests.swift: ten named phase-wide structural gates over Ritham/Social/ (54 files, 8 subdirectories), each asserting a real non-vacuous coverage floor"
  - "certificatepath_test.go: TestSharedTierRequiresPipeline (module-wide source-scan, no caller outside internal/photo may write to the shared object-storage tier) and TestPutSharedSignatureIsOpaque (source-text pin of the asymmetric writer signature)"
  - "RithamService/README.md: two newly surfaced deployment prerequisites (transport security, account deletion) and a full Phase 5 privacy-review handoff section naming all eight data categories this phase collects"
  - "REQUIREMENTS.md: sanity-checked all seven phase requirements against what's actually built; annotated ACCOUNT-01 (certificate-archive-restoration gap) and GROUPEVENTS-05 (confirmed no longer premature)"
affects: [Phase 5 (LAUNCH-04 privacy review directly consumes the README handoff section)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Swift-side ranking/counting-word gate deliberately omits bare 'position' (unlike the Go-side internal/events/noranking_test.go precedent) because AddPrivacyZoneView.swift's legitimate Map(position: $cameraPosition) MapKit call would false-positive under a bare ban -- documented explicitly in the test file rather than left as a silent asymmetry"
    - "Word-boundary-safe Toggle( detection via negative-lookbehind regex, needed because CheerReactionRow.swift's onToggle(cheer) closure parameter contains the literal substring 'Toggle(' as a false positive under a bare .contains check"
    - "Typography-floor gate matches precise SwiftUI font-role invocation shapes (.font(.caption), Font.caption) rather than a bare .caption/.footnote substring, since CompletionLoggingModel.swift's draft.caption and the wire-shape property item.caption are legitimate, unrelated stored-property names"
    - "Go-side module-wide source-scan gate (TestSharedTierRequiresPipeline) walks the whole module via filepath.WalkDir rather than the two-fixed-directory os.ReadDir pattern internal/events/noranking_test.go and internal/feed/nodenominator_test.go established, since 'no file outside the photo package' requires covering every package, not two known ones"

key-files:
  created:
    - RithamApp/RithamTests/SocialCoverageTests.swift
    - RithamService/internal/photo/certificatepath_test.go
  modified:
    - RithamApp/Ritham.xcodeproj/project.pbxproj
    - RithamService/README.md
    - .planning/REQUIREMENTS.md

key-decisions:
  - "Rule 1 deviation: the plan's Task 1 text states the Social directory has 'at least nine subdirectories.' Verified directly (find -type d) that it has exactly eight (Certificate, Completion, Feed, Friends, GoalEvents, Groups, Identity, PrivacyZones); RithamCore's own flat Social/ directory is a different module and not a ninth candidate. Asserted the real eight by name (stronger than any count), matching Phase4CoverageTests.swift's own precedent for handling exactly this kind of plan-text/reality mismatch."
  - "Documentation-accuracy gap found and annotated (not fixed as code, since it isn't one): ACCOUNT-01's requirement text claims signing in on a new device 'restores ... certificate archive.' CertificateRecord (plan 04.1-16) is SwiftData-local only with no server table and no sync route, so this is false as built -- the friend-graph and group-membership restoration half is real and correct. Annotated REQUIREMENTS.md in place (checkbox stays checked; the core account-creation/sign-in behavior is correct), tracked in STATE.md's Pending Todos as a Phase 5/backlog decision, not a phase-close blocker."
  - "GROUPEVENTS-05's checkbox, flagged by 04.1-12-SUMMARY.md as possibly premature at the time, is now verified accurate: plan 04.1-16 shipped the actual certificate feature. Annotated REQUIREMENTS.md to close the loop rather than leaving the concern unaddressed."
  - "Made no edit to ROADMAP.md: all of Task 3's acceptance criteria (the dated two-rung annotation present, all seventeen plan entries listed) were already satisfied by the existing text. 04.1-17-PLAN.md's own checklist mark stays unchecked, matching 02-16-PLAN.md's identical precedent -- a close-out plan whose Tasks 1-3 are done but whose final human-verification checkpoint is deferred stays uncounted as fully executed."

requirements-completed: [ACCOUNT-01, HOUSEHOLD-02, GROUPEVENTS-01, GROUPEVENTS-02, GROUPEVENTS-03, GROUPEVENTS-04, GROUPEVENTS-05]

coverage:
  - id: D1
    description: "SocialCoverageTests.swift: ten named structural gates over Ritham/Social/ (registry completeness, feature-area completeness by name, no ranking/counting field, no denominator beside a completion count, certificate photo restriction, geocoder containment, no data-bearing ring, two-rung visibility ladder, no platform List/Form/Toggle, typography floor), each asserting a real non-vacuous floor"
    requirement: "ACCOUNT-01"
    verification:
      - kind: unit
        ref: "RithamApp/RithamTests/SocialCoverageTests.swift (all 10 named tests, confirmed via full-target run: 574 tests total, up from a 564 baseline)"
        status: pass
    human_judgment: false
  - id: D2
    description: "certificatepath_test.go: no file outside internal/photo may write to the shared object-storage tier, and PutShared's asymmetric writer signature (StrippedImage, never []byte) is pinned at the source-text level"
    requirement: "GROUPEVENTS-03"
    verification:
      - kind: unit
        ref: "RithamService/internal/photo/certificatepath_test.go#TestSharedTierRequiresPipeline (scanned 26 files), #TestPutSharedSignatureIsOpaque"
        status: pass
    human_judgment: false
  - id: D3
    description: "RithamService/README.md records two newly surfaced deployment prerequisites (transport security, account deletion) and a Phase 5 privacy-review handoff naming all eight collected-data categories plus the third-party geocoding processor disclosure"
    requirement: "GROUPEVENTS-03"
    verification:
      - kind: other
        ref: "grep -c 'Phase 5' README.md (2); grep -c 'Deployment prerequisites' README.md (3); manual count of eight numbered privacy-review categories"
        status: pass
    human_judgment: false
  - id: D4
    description: "Full-suite green across all three test surfaces: RithamService (go test ./... and go vet ./...), RithamCore (swift test), and the full RithamApp iOS target, run twice consecutively with identical counts"
    verification:
      - kind: integration
        ref: "go test ./... (9 packages, all ok); go vet ./... (clean); swift test in RithamCore (436 tests, 32 suites); two consecutive xcodebuild test full-target runs (574 tests, 61 suites each, identical)"
        status: pass
    human_judgment: false
  - id: D5
    description: "The end-to-end human walkthrough of the whole phase's social feature (Sign in with Apple, friends, groups, goal-events, RSVP, completion logging with photo/location opt-ins, Privacy Zones, group feed, cheers, certificate export) -- Task 4's checkpoint"
    verification: []
    human_judgment: true
    rationale: "Requires the Go backend running via docker compose (Docker Desktop unavailable in this environment -- Subscription Service Agreement declined), a real device or touch-injection tool to drive Sign in with Apple and camera/GPS capture, and thirteen sequential UI interaction steps -- none automatable here. Deferred to the project's existing batched end-of-project physical-device verification pass per PROJECT.md's 2026-09-06 Key Decision and 04.1-VALIDATION.md's Manual-Only Verifications table, matching the identical deferral pattern 02-16-SUMMARY.md and 04.1-05-SUMMARY.md already established."

# Metrics
duration: 50min
completed: 2026-09-11
status: complete
---

# Phase 04.1 Plan 17: Phase Close-Out — Structural Gates, Full-Suite Green, and the Privacy-Review Record Summary

**Ten new client-side structural gates and one server-side gate convert every phase-wide prohibition Phase 4.1 depended on into an automated, non-vacuous test; both suites (plus RithamCore) are green together; and RithamService/README.md now carries the full Phase 5 privacy-review handoff and two newly surfaced deployment prerequisites.**

## Performance

- **Duration:** ~50 min
- **Started:** 2026-09-11T12:50:00+05:30 (approx)
- **Completed:** 2026-09-11T13:04:35+05:30
- **Tasks:** 3 of 4 (Task 4 is a deferred human-verify checkpoint, see below)
- **Files modified:** 5 (2 created, 3 modified)

## Accomplishments

- `SocialCoverageTests.swift` — ten named tests, nested inside `StepRegistryTouchingSuites`, covering: every Phase 4.1 `OnboardingStep` resolves to its real registered screen (registry completeness); all eight real Social feature subdirectories exist by name; no ranking/contest-shaped word or collection-size-into-string-interpolation exists anywhere in `Ritham/Social/`; no file that renders completions also references the RSVP step or a headcount; the certificate composer's source references only the opaque server-backed photo asset; exactly one file in the whole client constructs a `CLGeocoder`, and it is `LocationAttachment.swift` by name; no trimmed circle or arc exists in any social surface; `GroupVisibilityScope` ships exactly two rungs with the third's raw value resolving to `nil`; no platform `List`/`Form`/`Toggle` exists (word-boundary-safe against `onToggle(`'s false positive); no font role below the app's `label` floor exists (matched against the precise `.font(.caption)` shape, not a bare substring that would false-positive on `draft.caption`).
- `certificatepath_test.go` — `TestSharedTierRequiresPipeline` walks every non-test `.go` file in the module outside `internal/photo` (26 files) and fails on a direct `PutShared(` call or `minio.New(` construction; `TestPutSharedSignatureIsOpaque` pins the asymmetric writer signature at the source-text level.
- `RithamService/README.md` — extended (not rewritten) with two newly surfaced deployment prerequisites (no transport security anywhere in the stack; account deletion unbuilt, an App Store Guideline 5.1.1(v) submission blocker) and a full Phase 5 privacy-review handoff section naming all eight data categories this phase collects, including the third-party (Apple) geocoding processor disclosure `04.1-07-SUMMARY.md` flagged.
- `REQUIREMENTS.md` sanity check (this plan's own known-tracking-note ask): all seven phase requirements checked against actual built behavior. Found and annotated one real gap (ACCOUNT-01's certificate-archive-restoration claim) and confirmed one previously-flagged concern is now resolved (GROUPEVENTS-05).
- Full suite green: `go test ./...` + `go vet ./...` (RithamService), `swift test` (RithamCore, 436 tests/32 suites), and two consecutive full-target `xcodebuild test` runs (RithamApp, 574 tests/61 suites each, identical counts, no flake occurrence).

## Task Commits

1. **Task 1: Phase-wide client structural gates with non-vacuous coverage floors** - `d779d0c` (test)
2. **Task 2: Server-side pipeline gate and the deployment and privacy-review record** - `459a6ed` (test)
3. **Task 3: Full-suite green and the roadmap annotation check** - `a514442` (docs)

**Plan metadata:** (this commit, made after this SUMMARY is written)

## Files Created/Modified

- `RithamApp/RithamTests/SocialCoverageTests.swift` - the ten-test phase-wide structural gate suite
- `RithamApp/Ritham.xcodeproj/project.pbxproj` - regenerated via `xcodegen generate` so the new test file compiles
- `RithamService/internal/photo/certificatepath_test.go` - module-wide shared-tier write gate + signature pin
- `RithamService/README.md` - two new deployment prerequisites + Phase 5 privacy-review handoff section
- `.planning/REQUIREMENTS.md` - ACCOUNT-01 and GROUPEVENTS-05 tracking-accuracy annotations

## Decisions Made

See `key-decisions` in frontmatter: the nine-vs-eight-subdirectory plan-text correction, the ACCOUNT-01 documentation-accuracy annotation, the GROUPEVENTS-05 confirmation, and the no-edit-needed conclusion for ROADMAP.md.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Plan text bug] "At least nine subdirectories" corrected to the real eight, asserted by name**
- **Found during:** Task 1
- **Issue:** The plan's action text states the Social directory has "at least nine subdirectories, each named explicitly." Direct verification (`find RithamApp/Ritham/Social -mindepth 1 -type d`) shows exactly eight: `Certificate`, `Completion`, `Feed`, `Friends`, `GoalEvents`, `Groups`, `Identity`, `PrivacyZones`. `RithamCore/Sources/RithamCore/Social/` is flat (no subdirectories) and belongs to a different module/target, so it is not a candidate ninth entry.
- **Fix:** `theSocialDirectoryContainsEveryExpectedFeatureArea` asserts the real eight subdirectories exist, each named explicitly and checked individually — a stronger, more durable floor than any count claim (a rename or relocation of any one of them fails by name, not just by a number silently drifting). Documented the correction in the test file's own header comment, matching `Phase4CoverageTests.swift`'s established precedent for this exact class of plan-text/reality mismatch.
- **Files modified:** `RithamApp/RithamTests/SocialCoverageTests.swift`
- **Verification:** Test passes; `find` output re-confirmed before writing the test.
- **Committed in:** `d779d0c` (Task 1 commit)

**2. [Rule 2 - Documentation-accuracy gap] Annotated ACCOUNT-01's certificate-archive-restoration claim**
- **Found during:** Task 3 (this plan's own known-tracking-note ask to sanity-check REQUIREMENTS.md)
- **Issue:** ACCOUNT-01 states that signing in with the same Apple ID on a new device "restores their friend graph, group memberships, and certificate archive." The friend-graph and group-membership halves are real (server-side tables keyed on the Apple-identity user id). The certificate-archive half is not: `CertificateRecord` (`HealthDataStore.saveCertificate`/`loadCertificates`, plan 04.1-16) is SwiftData-local only — no server table, no sync route (`grep -rl certificate RithamService --include=*.go` finds only the unrelated `exportconsent.go`). Signing in on a genuinely new device would not restore a person's certificates.
- **Fix:** Added a dated annotation directly above the ACCOUNT-01 entry in `REQUIREMENTS.md` recording the gap precisely, matching this project's existing annotation convention (the "Social & Groups" `*Revised 2026-09-09*` header, `03-10`'s `ROADMAP.md` annotation) rather than rewriting the requirement. Checkbox stays checked — the core account-creation/sign-in behavior this requirement is fundamentally about is fully built and correct; only one restoration claim within it doesn't hold. Recorded as a Pending Todo in `STATE.md` for a future Phase 5/backlog decision (a certificate-sync route, or a text correction), not treated as a phase-close blocker.
- **Files modified:** `.planning/REQUIREMENTS.md`, `.planning/STATE.md`
- **Verification:** Confirmed via direct source read of `StrippedPhotoAsset.swift`/`HealthDataStore.swift`/`CertificateTests.swift` and a repo-wide grep for any server-side certificate persistence.
- **Committed in:** `a514442` (Task 3 commit)

**3. [Rule 2 - Documentation-accuracy confirmation] GROUPEVENTS-05 verified no longer premature**
- **Found during:** Task 3
- **Issue:** `04.1-12-SUMMARY.md` explicitly flagged that GROUPEVENTS-05 may have been marked complete prematurely — at that point only the export-consent gate had shipped, not the certificate feature itself.
- **Fix:** Verified plan 04.1-16 has since shipped the real feature (`CertificateContent`'s exact seven fields, `CertificateComposer`, reveal/archive/export flows, all covered by `CertificateTests.swift` and this plan's own `theCertificateComposerAcceptsOnlyAServerBackedPhotoReference`). Added a confirming annotation to `REQUIREMENTS.md` to close the loop explicitly rather than leaving the earlier concern unaddressed.
- **Files modified:** `.planning/REQUIREMENTS.md`
- **Verification:** Cross-referenced `CertificateTests.swift`'s seven-field assertion and `SocialCoverageTests.swift`'s independent source-level gate.
- **Committed in:** `a514442` (Task 3 commit)

---

**Total deviations:** 3 auto-fixed (1 plan-text bug, 2 documentation-accuracy annotations)
**Impact on plan:** All three are corrections to test/documentation accuracy, not scope creep. No production code behavior changed.

## Issues Encountered

- The plan's Task 1 `<verify>` command (`-only-testing:RithamTests/SocialCoverageTests`) matches zero tests once the suite is nested under `StepRegistryTouchingSuites` and exits 0 — a silent false pass. Confirmed this is a pre-existing filter quirk in this Xcode/xcodebuild version (reproduced identically against the already-shipped `Phase4CoverageTests`, not a regression from this plan's new file). Verified instead via a full-target run reporting the correct executed-test count (574, up from a 564 baseline) and every one of the ten named `SocialCoverageTests` tests individually passing in the log.
- `iPhone 15` simulator is unavailable in this Xcode 26 install (matching `02-09-SUMMARY.md`'s and `04.1-05-SUMMARY.md`'s own documented substitution precedent); substituted `iPhone 17` throughout.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None introduced by this plan.

## Next Phase Readiness

- Every phase-wide prohibition Phase 4.1 relied on is now an automated gate (`SocialCoverageTests.swift` client-side, `certificatepath_test.go` server-side), both verified non-vacuous via a real scanned-file floor.
- `RithamService/README.md` is ready for Phase 5's LAUNCH-04 privacy reviewer: all eight collected-data categories, the third-party geocoding processor disclosure, and all five deployment prerequisites (three carried forward, two newly surfaced by this phase) are recorded in the repository itself.
- **Task 4 (the end-to-end human walkthrough of the whole social feature) is deferred** to the project's existing batched end-of-project physical-device verification pass, per `PROJECT.md`'s 2026-09-06 Key Decision and `04.1-VALIDATION.md`'s Manual-Only Verifications table — the same deferral pattern `02-16-SUMMARY.md` and `04.1-05-SUMMARY.md` already established. This environment has no Docker (the checkpoint's own step 1 requires it to start the backend) and no touch-injection tool to drive Sign in with Apple, camera capture, or GPS on Simulator. Phase 04.1 stays in a pending-verification state for this one item; Phase 5 planning may proceed regardless, per the same 2026-09-06 decision.
- No open blockers specific to this plan beyond the deferred checkpoint and the two documentation-accuracy items tracked in `STATE.md`'s Pending Todos.

---
*Phase: 04.1-group-goal-events-accountability-circles*
*Completed: 2026-09-11*

## Self-Check: PASSED

All created/modified files confirmed present on disk; all three task commit hashes (`d779d0c`, `459a6ed`, `a514442`) confirmed in git log.
