---
phase: 04.1
slug: group-goal-events-accountability-circles
status: planned
nyquist_compliant: true
wave_0_complete: true
created: 2026-09-09
updated: 2026-09-09
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (client)** | Swift Testing (`import Testing`) — confirmed via existing suites (`Phase4CoverageTests.swift`, `AgeValidationTests.swift`); no XCTest usage in `RithamTests` |
| **Framework (backend)** | Go's built-in `testing` package (`go test`) — confirmed via existing `generate_test.go`/`handler_test.go` |
| **Config file** | None dedicated for either — Swift Testing runs via the `RithamTests` Xcode target; Go tests run via the module's standard toolchain |
| **Quick run command (backend)** | `cd RithamService && go test ./...` |
| **Quick run command (client)** | `xcodebuild test -project RithamApp/Ritham.xcodeproj -scheme Ritham -destination 'platform=iOS Simulator,name=iPhone 15' -only-testing:<TargetSuite>` |
| **Full suite command** | `cd RithamService && go test ./...` AND `xcodebuild test -project RithamApp/Ritham.xcodeproj -scheme Ritham -destination 'platform=iOS Simulator,name=iPhone 15'` — this project has no quick/full split; the existing precedent runs the full target every time |
| **Estimated runtime** | ~unknown until Wave 0 lands (first phase with a Go persistence layer + new Swift Testing suites) |

---

## Sampling Rate

- **After every task commit:** `go test ./...` (backend tasks) or the relevant targeted `xcodebuild test -only-testing:` invocation (client tasks)
- **After every plan wave:** Full `go test ./...` plus full `xcodebuild test` (matching Phase 2/Phase 3's `StepRegistry` cross-suite-serialization precedent — any new test suite touching shared static state must respect `.serialized`, see `STATE.md`)
- **Before `/gsd-verify-work`:** Full suite green, plus the two required structural negative tests (EXIF-strip, certificate-uses-stripped-photo) passing explicitly
- **Max feedback latency:** ~120s (matches this project's existing full-target xcodebuild precedent; no faster targeted-suite convention established yet for this phase)

---

## Per-Task Verification Map

Filled in 2026-09-09 after planning completed and `gsd-plan-checker` verified all 17 plans
(`## VERIFICATION PASSED (with one warning)` — this table was the warning; now resolved). Rows
are plan-level (each plan's primary automated command); see the plan file itself for its full
per-task breakdown (2-4 tasks each).

| Plan | Wave | Requirements | Primary Automated Command | File Exists | Status |
|------|------|--------------|-----------------------------|-------------|--------|
| 04.1-01 | 1 | ACCOUNT-01 | `cd RithamService && go build ./... && go vet ./...` | ✅ | ⬜ pending execution |
| 04.1-02 | 1 | HOUSEHOLD-02, GROUPEVENTS-01, GROUPEVENTS-02, GROUPEVENTS-04 | `cd RithamCore && swift test --filter SocialModelTests` | ✅ | ⬜ pending execution |
| 04.1-03 | 2 | ACCOUNT-01 | `cd RithamService && go test ./internal/identity/... -run TestVerifyIdentityToken` | ✅ | ⬜ pending execution |
| 04.1-04 | 3 | GROUPEVENTS-03 | `cd RithamService && go test ./internal/photo/... -run 'TestStripAndReencode\|TestOrientation' -v` | ✅ | ⬜ pending execution |
| 04.1-05 | 3 | ACCOUNT-01 | `xcodebuild test ... -only-testing:RithamTests/SocialIdentityTests` | ✅ | ⬜ pending execution |
| 04.1-06 | 4 | HOUSEHOLD-02, GROUPEVENTS-01 | `cd RithamService && go test ./internal/friends/... -run TestFriendGraph` | ✅ | ⬜ pending execution |
| 04.1-07 | 4 | GROUPEVENTS-03 | `xcodebuild test ... -only-testing:RithamTests/PrivacyZoneTests` | ✅ | ⬜ pending execution |
| 04.1-08 | 5 | GROUPEVENTS-01, HOUSEHOLD-02 | `cd RithamService && go test ./internal/groups/... -run TestGroups` | ✅ | ⬜ pending execution |
| 04.1-09 | 5 | HOUSEHOLD-02, GROUPEVENTS-01 | `xcodebuild test ... -only-testing:RithamTests/FriendsUITests` | ✅ | ⬜ pending execution |
| 04.1-10 | 6 | GROUPEVENTS-02, GROUPEVENTS-01 | `cd RithamService && go test ./internal/events/... -run 'TestGoalEvent\|TestRSVP'` | ✅ | ⬜ pending execution |
| 04.1-11 | 6 | GROUPEVENTS-01, HOUSEHOLD-02 | `xcodebuild test ... -only-testing:RithamTests/GroupsUITests` | ✅ | ⬜ pending execution |
| 04.1-12 | 7 | GROUPEVENTS-04, GROUPEVENTS-05 | `cd RithamService && go test ./internal/feed/... -run TestGroupFeed` | ✅ | ⬜ pending execution |
| 04.1-13 | 7 | GROUPEVENTS-02, GROUPEVENTS-05 | `xcodebuild test ... -only-testing:RithamTests/GoalEventsUITests` | ✅ | ⬜ pending execution |
| 04.1-14 | 8 | GROUPEVENTS-02, GROUPEVENTS-03 | `xcodebuild test ... -only-testing:RithamTests/CompletionLoggingTests` | ✅ | ⬜ pending execution |
| 04.1-15 | 9 | GROUPEVENTS-04, GROUPEVENTS-02 | `xcodebuild test ... -only-testing:RithamTests/GroupFeedTests` | ✅ | ⬜ pending execution |
| 04.1-16 | 10 | GROUPEVENTS-05, GROUPEVENTS-03 | `xcodebuild test ... -only-testing:RithamTests/CertificateTests` | ✅ | ⬜ pending execution |
| 04.1-17 | 11 | ACCOUNT-01, HOUSEHOLD-02, GROUPEVENTS-01..05 | `xcodegen generate && xcodebuild test ... -only-testing:RithamTests/SocialCoverageTests` (phase-wide structural gate — mirrors `Phase4CoverageTests`) | ✅ | ⬜ pending execution |

*Status: ⬜ pending execution · ✅ green · ❌ red · ⚠️ flaky. All rows currently "pending execution" — planning and verification are complete, `/gsd-execute-phase` has not run yet.*

---

## Wave 0 Requirements

All satisfied by the planned Wave 1-3 plans (this phase's actual Wave 0 foundational work landed
inside plans 01/02/03/04, not a separately-numbered Wave 0 — `gsd-plan-checker` confirmed no
`MISSING` references remain anywhere across the 17 plans):

- [x] `RithamService/internal/identity/` package + tests — covered by Plan 04.1-01/03 (now carries
  Sign in with Apple token verification, not the superseded device-keypair model)
- [x] `RithamService/internal/photo/photo_test.go` — covered by Plan 04.1-04 (`TestStripAndReencode`/`TestOrientation`, plus a real HEIC fixture pinning the reject path)
- [x] `RithamService/go.mod` first real dependencies — covered by Plan 04.1-01 (`go build`/`go vet` gate)
- [x] `RithamApp/RithamTests/SocialCoverageTests.swift` — covered by Plan 04.1-17 (phase-wide structural gate, non-vacuous scanned-file-count floor, per the Phase 4 "vacuous gate" lesson)
- [x] Framework install: none needed — confirmed, both Swift Testing and Go's `testing` package already in use project-wide

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| Real Sign in with Apple flow (device round-trip through Apple's servers) | ACCOUNT-01 | Requires a real Apple ID sign-in prompt; Simulator/automated test can exercise the token-verification code path with a fixture token but not the live Apple authorization UI | Deferred to this project's existing batched end-of-project physical-device verification pass (per `PROJECT.md` Key Decisions) — do not gate this phase's close-out on it individually |
| Real two-device friend/group flow against real (non-loopback) hosting | GROUPEVENTS-01, GROUPEVENTS-04 | No real hosting provider is chosen yet (`04.1-RESEARCH.md` Environment Availability); local dev/testing can verify single-device logic and API contracts but not real cross-device delivery | Blocked until a hosting decision is made (flagged as an unresolved deployment prerequisite, not a phase-planning blocker) |
| Photo/location capture against real device camera/GPS | GROUPEVENTS-03 | Simulator has no real camera/GPS; HEIC transcode and Privacy Zone evaluation need a real device to verify end-to-end | Deferred to the same batched physical-device verification pass |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — confirmed by `gsd-plan-checker`, 2026-09-09
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references — none found
- [x] No watch-mode flags
- [x] Feedback latency < 120s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-09-09 (gsd-plan-checker: VERIFICATION PASSED, 0 blockers, 1 warning
— this document's own staleness, now resolved by this edit)
