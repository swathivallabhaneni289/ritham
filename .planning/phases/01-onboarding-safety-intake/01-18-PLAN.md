---
phase: 01-onboarding-safety-intake
plan: 18
type: execute
wave: 9
depends_on: ["01-17", "01-13"]
files_modified:
  - RithamApp/Ritham/App/StepBootstrap.swift
  - RithamApp/Ritham/App/RithamApp.swift
  - RithamApp/RithamTests/PhaseCoverageTests.swift
autonomous: false
requirements: [ONBOARD-01, CROSSGEN-05, HEALTH-01]

must_haves:
  truths:
    - "Every onboarding step resolves to a real screen, with none left unimplemented"
    - "The full onboarding flow runs end to end on a device"
    - "The band motif and every screen reflow correctly at accessibility text sizes"
    - "A real ten-minute walk completes calibration on physical hardware"
  artifacts:
    - path: "RithamApp/RithamTests/PhaseCoverageTests.swift"
      provides: "Assertion that no onboarding step is left unimplemented"
      contains: "unregisteredSteps"
  key_links: []
---

<objective>
Verify the whole phase — automatically where that is meaningful, and by hand where 01-VALIDATION.md
establishes that a test cannot capture the thing being verified.

Purpose: Ritham has a permanent 13+ age floor (D-14, 01-CONTEXT.md) with no consent flow of any kind —
an under-13 age shows a plain blocking message, and 13+ gets full, identical access immediately. There is
nothing left to deep-link back into, so the only remaining work is closing out onboarding step
registration and running the phase's verification.

01-VALIDATION.md names two verifications as genuinely manual — the band motif's reflow at accessibility
sizes, and CoreMotion accuracy on a real walk. Each is manual for a stated reason: rendering correctness
at accessibility sizes is not meaningfully assertable in a unit test, and the simulator produces no
motion data at all. Those become checkpoints here rather than fabricated automated gates.

Output: A coverage test proving no step was left unimplemented, and the phase's human verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-onboarding-safety-intake/01-VALIDATION.md
@.planning/phases/01-onboarding-safety-intake/01-RESEARCH.md

<interfaces>
<!-- Already on disk. -->
- `StepRegistry.unregisteredSteps`, `StepRegistry.view(for:flow:)`, `OnboardingStep.allCases`
- `RithamApp.swift` — created by plan 01-09, model container attached by plan 01-11. Keep the change
  additive.
- Build and test: `./Scripts/build-app.sh build` / `./Scripts/build-app.sh test [-only-testing:...]`
- Core suite: `cd RithamCore && ./Scripts/test-core.sh`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step bootstrap and phase coverage assertions across both suites</name>
  <files>RithamApp/Ritham/App/StepBootstrap.swift, RithamApp/Ritham/App/RithamApp.swift, RithamApp/RithamTests/PhaseCoverageTests.swift</files>
  <read_first>
    - `.planning/phases/01-onboarding-safety-intake/01-VALIDATION.md` — the Per-Task Verification Map, every row of which must be represented by a passing test somewhere
    - `.planning/phases/01-onboarding-safety-intake/01-09-SUMMARY.md` — `StepRegistry.unregisteredSteps`
    - The registration summaries — 01-13, 01-15, 01-16 — for the exact `registerAll()` entry points to call
  </read_first>
  <action>
    Create `StepBootstrap.swift` with a single `enum StepBootstrap` exposing
    `static func registerAllSteps()` that calls each screen group's registrar in turn:
    `AboutYouRegistration.registerAll()`, `CalibrationRegistration.registerAll()`, and
    `ScreeningRegistration.registerAll()`. Make it idempotent so repeated calls in tests do not duplicate
    registrations.

    This file exists here, in the phase's last wave, on purpose. Plans 01-13, 01-15, and 01-16 run in the
    same wave and each contributes a registrar; if each of them also appended its call to a shared
    bootstrap function, that one file would be a write conflict between parallel executors. Owning the
    call site in a single later plan removes the conflict without serialising the screen work. Record that
    reasoning in a header comment so a later phase adding a screen group knows to extend this file rather
    than reintroduce per-plan wiring.

    Invoke `StepBootstrap.registerAllSteps()` from `RithamApp.swift` before the root view is presented.
    Keep the edit additive alongside the model container attachment.

    Then write `PhaseCoverageTests` as the phase's completeness gate, asserting the things that are only
    checkable once every plan has landed. Call `StepBootstrap.registerAllSteps()` in test setup so the
    registry reflects the shipped configuration rather than a partially registered one:

    - `StepRegistry.unregisteredSteps` is empty. Plan 01-09 introduced a labelled fallback view so the
      shell could ship before the screens existed; this assertion is what proves none of those fallbacks
      survived to the end of the phase.
    - `StepRegistry.view(for:flow:)` returns a view for every `OnboardingStep` case without trapping.
    - Walking `OnboardingRouter` from `welcome` to termination visits only registered steps, so no
      reachable path lands on an unimplemented screen.
    - Every `ConditionTag` case has a non-empty, unique `displayName`, since the disclaimer tag renders
      them to users.
    - `GateEscalation.baseGates(for:)` returns a value for every `ConditionTag` case, so no tag can reach
      resolution without a defined gate in either domain.

    Add a comment mapping each 01-VALIDATION.md Per-Task Verification Map row to the suite that covers it,
    so a reviewer can confirm no row was dropped: the two escalation rows and the eating-pattern trigger
    row to `GateResolutionTests`; the tag expiry row to `ConditionTagExpiryTests`; the calibration
    threshold row to `CalibrationThresholdTests`; the no-fork row to `OnboardingFlowStateTests`; and the
    build row to `Scripts/build-app.sh build`. Note that these run under `RithamCore/Scripts/test-core.sh`
    rather than the `xcodebuild` invocations 01-VALIDATION.md drafted, because the pure-Swift core was
    extracted into a package so those gates could run before Xcode was installed; the behaviours asserted
    are unchanged.
  </action>
  <verify>
    <automated>./Scripts/build-app.sh test -only-testing:RithamTests/PhaseCoverageTests 2>&1 | grep -E 'TEST SUCCEEDED|Test Suite .* passed'</automated>
  </verify>
  <acceptance_criteria>
    - `./Scripts/build-app.sh test -only-testing:RithamTests/PhaseCoverageTests` exits 0
    - `grep -c 'unregisteredSteps' RithamApp/RithamTests/PhaseCoverageTests.swift` is at least 1
    - `grep -c 'registerAll' RithamApp/Ritham/App/StepBootstrap.swift` is at least 3
    - `grep -c 'StepBootstrap.registerAllSteps' RithamApp/Ritham/App/RithamApp.swift` is at least 1
    - Calling `StepBootstrap.registerAllSteps()` twice does not duplicate any registration
    - A test asserts every `ConditionTag` has a defined gate in both domains
    - `cd RithamCore && ./Scripts/test-core.sh` exits 0
    - `./Scripts/build-app.sh test` exits 0
    - The comment maps all six 01-VALIDATION.md rows to a named suite
  </acceptance_criteria>
  <done>One bootstrap file registers every screen group and is invoked at app start; both suites pass together; every onboarding step resolves to a real screen; and every validation-map row is traceable to a passing test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification of the two manual-only behaviours</name>
  <files>(none — this task verifies already-built behaviour and writes no source)</files>
  <read_first>
    - `.planning/phases/01-onboarding-safety-intake/01-VALIDATION.md` — the Manual-Only Verifications table and its stated reason for each of the two rows
  </read_first>
  <action>
    Before pausing, confirm both automated suites are green and report their results to the developer:
    `cd RithamCore && ./Scripts/test-core.sh` and `./Scripts/build-app.sh test`. Then present the
    verification steps below and wait. Do not simulate, stub, or claim either of these two verifications:
    each is manual precisely because automating it would not exercise the thing being checked.
  </action>
  <what-built>
    The complete Phase 1 onboarding and safety intake: the calibration session in both walk and lift
    modes, the explanation-register choice with tap-to-expand definitions, age and dietary pattern, the
    privacy explainer, the permanent 13+ age floor with its plain blocking message below that line, the
    full fixed-choice screening questionnaire with clearance interstitials and condition tags, the health
    profile with its disclaimer surfaces, and Settings with section-scoped editing and the re-screen
    banner.

    Automated coverage is green across both suites: the pure-Swift core including the sixteen escalation
    rules, and the app target including routing and persistence.
  </what-built>
  <why-human>
    01-VALIDATION.md's Manual-Only Verifications table establishes two behaviours that automated tests
    cannot capture, each for a stated reason: rendering correctness at accessibility text sizes is not
    meaningfully assertable in a unit test; and CoreMotion produces no data in the simulator, so a real
    walk on physical hardware is the only way to exercise that path.
  </why-human>
  <how-to-verify>
    **A. Accessibility reflow and visual correctness (Simulator is fine)**
    1. Run the app and step through onboarding at the default text size.
    2. In Settings, Accessibility, Display and Text Size, raise the text size to AX3, then AX5.
    3. On the welcome, register, privacy, and calibration screens, confirm the decorative header
       compresses or drops out entirely and that no text is composited over a diagonal band or the
       halftone texture.
    4. Confirm the age, dietary pattern, gate section, checklist, interstitial, and eating-pattern
       screens are flat charcoal with no motif at any text size.
    5. Confirm no text anywhere renders smaller than the standing footer disclaimer, and that the footer
       and the compact disclaimer tag are both comfortably readable.
    6. Confirm every coral or lime filled control has dark text on it, never off-white.
    7. Confirm the compact disclaimer tag's expand affordance is easy to hit with a thumb, and that
       tapping it reveals the full disclaimer.

    **B. Calibration on physical hardware**
    8. On a physical device, start a walk calibration and walk for ten or more continuous minutes.
       Confirm it completes and that the completion screen shows no score, grade, level, or rating.
    9. Repeat while declining location permission entirely. Confirm the session still completes.
    10. Pause mid-walk and resume. Confirm the session does not complete on interrupted time.
    11. Start a lift calibration. Confirm it completes only after three working sets across two
        exercises, and not at three sets across one.
    12. Skip calibration on a fresh run. Confirm nothing is blocked and the app continues normally.

    Report anything that does not match, including screenshots for any visual issue.
  </how-to-verify>
  <resume-signal>Type "approved" when both groups pass, or describe what failed with the step number.</resume-signal>
  <verify>
    <automated>cd RithamCore && ./Scripts/test-core.sh 2>&1 | grep -E 'Test run with [0-9]+ test.* passed'</automated>
  </verify>
  <acceptance_criteria>
    - Both automated suites reported green to the developer before the pause
    - Group A approved: headers drop out at AX3 and AX5, health screens are flat at every text size, no text renders below the footer disclaimer's size, and filled coral or lime controls carry dark labels
    - Group B approved: a real ten-minute walk completes on device with location declined, an interrupted walk does not complete, the lift completes only at three sets across two exercises, and skipping blocks nothing
  </acceptance_criteria>
  <done>The developer has exercised and approved both manual-only behaviours on real hardware, or has reported specific failures for follow-up.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan's contribution)

| Symbol | Kind | File |
|---|---|---|
| `StepBootstrap.registerAllSteps()` | enum namespace — sole caller of every screen registrar | `App/StepBootstrap.swift` |
| `PhaseCoverageTests` | test suite | `RithamTests/PhaseCoverageTests.swift` |
</artifacts_produced>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-114 | Tampering | A step left on the unimplemented fallback shipping to users | medium | mitigate | `PhaseCoverageTests` asserts `unregisteredSteps` is empty and that every reachable path visits only registered steps. |
| T-01-115 | Information Disclosure | A condition tag with no defined gate reaching resolution and producing no restriction | high | mitigate | `PhaseCoverageTests` asserts every `ConditionTag` case has a defined gate in both domains, so no tag can be added without a gate. |
| T-01-SC | Tampering | package-manager installs | low | accept | SUPERSEDED by D-13. This plan adds no third-party package; no npm, pip, or cargo install occurs anywhere in this phase. The `resend` and `@supabase/supabase-js` packages flagged `[SUS]` by 01-RESEARCH.md's Package Legitimacy Audit belonged to the superseded Node backend design and were never installed, and the Go consent service they would have served is gone entirely (D-14). |
</threat_model>

<verification>
- `cd RithamCore && ./Scripts/test-core.sh` exits 0
- `./Scripts/build-app.sh test` exits 0
- `StepRegistry.unregisteredSteps` is empty
- Both manual-only verification groups approved by the developer
</verification>

<success_criteria>
- Every onboarding step resolves to a real screen and every condition tag has a defined gate
- The two behaviours a test cannot capture are verified by hand and approved
</success_criteria>

<output>
Create `.planning/phases/01-onboarding-safety-intake/01-18-SUMMARY.md` when done
</output>
