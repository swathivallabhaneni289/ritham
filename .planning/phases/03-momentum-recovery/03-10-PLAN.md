---
phase: 03-momentum-recovery
plan: 10
type: execute
wave: 9
depends_on: ["03-06", "03-07", "03-08", "03-09"]
files_modified:
  - RithamApp/RithamTests/Phase3CoverageTests.swift
  - .planning/ROADMAP.md
  - .planning/phases/03-momentum-recovery/03-VALIDATION.md
  - RithamApp/Ritham.xcodeproj/project.pbxproj
autonomous: true
requirements: [MOMENTUM-05, MOMENTUM-06]

must_haves:
  truths:
    - "Each of this phase's three new step cases resolves to its real screen type, not a placeholder, asserted by a dedicated coverage suite (03-RESEARCH.md Pitfall 4)."
    - "No share, export, copy-link or invite-to-see affordance exists anywhere in this phase's Momentum or Movement Snapshot surfaces (MOMENTUM-06, D-06)."
    - "ROADMAP.md's Phase 3 criterion 5 carries a dated annotation recording that only the private-by-default half of MOMENTUM-06 ships this phase, mirroring the Phase 2 criterion 6 precedent (D-06)."
    - "The full test target passes with no filter, matching the phase-close precedent set by Phase 2's own close-out plan."
  artifacts:
    - RithamApp/RithamTests/Phase3CoverageTests.swift
  key_links:
    - "Phase3CoverageTests is the analogue of the shipped Phase 2 coverage suite: the earlier unregistered-steps gate only proves a step resolves to something, not that it resolves to the real screen."
    - "The ROADMAP annotation is what keeps MOMENTUM-06's deferred half a dated, scoped deferral rather than a silently unchecked criterion at phase close."
---

<objective>
Close Phase 3: prove every new step resolves to its real screen, prove no sharing affordance exists
on any Momentum or Movement Snapshot surface, record MOMENTUM-06's scoped deferral as a dated
ROADMAP annotation, and land a green full-target run.

Purpose: Phase 2's own close-out plan established this shape, and the two structural gates here
(real-screen resolution and no-share) are the ones that cannot be asserted from inside any single
feature plan.
Output: one new coverage suite, two planning-document edits, and a full-suite green run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-momentum-recovery/03-CONTEXT.md
@.planning/phases/03-momentum-recovery/03-VALIDATION.md
@.planning/phases/03-momentum-recovery/03-06-SUMMARY.md
@.planning/phases/03-momentum-recovery/03-08-SUMMARY.md
@.planning/phases/03-momentum-recovery/03-09-SUMMARY.md
@RithamApp/RithamTests/Phase2CoverageTests.swift
</context>

<!-- planner-discipline-allow: ShareLink -->
<!-- planner-discipline-allow: UIActivityViewController -->

<tasks>

<task type="auto">
  <name>Task 1: Phase 3 step-coverage suite and the no-sharing structural gate</name>
  <files>RithamApp/RithamTests/Phase3CoverageTests.swift, RithamApp/Ritham.xcodeproj/project.pbxproj</files>
  <read_first>RithamApp/RithamTests/Phase2CoverageTests.swift in full (the exact suite shape to mirror, including its nesting inside the serialized registry parent, its step-to-real-type table, and its placeholder-name assertion), RithamApp/RithamTests/StepRegistrySerialization.swift, 03-UI-SPEC.md's "MOMENTUM-06 — Visibility" section, and 03-CONTEXT.md D-06.</read_first>
  <action>
Create `RithamApp/RithamTests/Phase3CoverageTests.swift` as
`@Suite("Phase3CoverageTests", .serialized)`, `@MainActor`, nested inside
`extension StepRegistryTouchingSuites { ... }` — mandatory, because it resets and re-bootstraps the
step registry's shared static state, exactly like the shipped Phase 2 coverage suite.

Mirror that suite's structure with a static table pairing each of this phase's three new steps with
the concrete screen type its owning plan created, and assert:
- Each step's registered presenter type equals its real screen type.
- No registered presenter type name looks like a placeholder.
- Each step's view factory resolves without trapping.
- The registry reports no unregistered steps after bootstrap.

Add the MOMENTUM-06 structural gate to the same suite: a test named
`noMomentumSurfaceOffersASharingAffordance` that reads every Swift file under this phase's two
feature directories and asserts zero non-comment occurrences of a share-sheet trigger — the SwiftUI
share link view and the UIKit activity view controller — plus zero occurrences of any pasteboard
write. Filter comment-only lines before matching, so a header comment that documents the
prohibition does not fail the gate it documents. Read the files from the test bundle's own source
path or from a checked-in list of file names; state in the summary which technique was used and
why, since a test that silently finds zero files would pass vacuously — assert the file count found
is greater than zero as part of the same test.

State in the file's header that this suite exists for the same reason the Phase 2 one does: the
earlier unregistered-steps gate proves only that a step resolves to something, and cannot tell a
placeholder from the real screen.
  </action>
  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/StepRegistryTouchingSuites/Phase3CoverageTests</automated>
  </verify>
  <acceptance_criteria>
    - `Scripts/build-app.sh test -only-testing:RithamTests/StepRegistryTouchingSuites/Phase3CoverageTests` passes with at least 5 tests.
    - Named test `noMomentumSurfaceOffersASharingAffordance` exists, passes, and asserts a non-zero scanned-file count so it cannot pass vacuously.
    - The step-to-type table covers exactly the three cases this phase added and no others.
    - `grep -rhE "ShareLink|UIActivityViewController|UIPasteboard" RithamApp/Ritham/Momentum/ RithamApp/Ritham/MovementSnapshot/ | grep -v '^\s*//' | wc -l` returns 0.
    - `Scripts/build-app.sh test -only-testing:RithamTests/StepRegistryTouchingSuites/Phase2CoverageTests` and `-only-testing:RithamTests/StepRegistryTouchingSuites/PhaseCoverageTests` both still pass.
  </acceptance_criteria>
  <done>Every Phase 3 step resolves to its real screen, and the absence of any sharing affordance across both new feature directories is asserted by a non-vacuous test.</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP annotation for MOMENTUM-06's scoped deferral, and validation-map task IDs</name>
  <files>.planning/ROADMAP.md, .planning/phases/03-momentum-recovery/03-VALIDATION.md</files>
  <read_first>.planning/ROADMAP.md's Phase 2 success criterion 6 (the 2026-09-04 dated correction, which is the exact precedent to mirror in tone and placement) and its Phase 3 success criterion 5, 03-CONTEXT.md D-06 in full, and .planning/phases/03-momentum-recovery/03-VALIDATION.md's Per-Task Verification Map.</read_first>
  <action>
Edit `.planning/ROADMAP.md`'s Phase 3 success criterion 5 in place, appending a dated italicized
annotation in exactly the style of Phase 2's criterion 6 correction. Use scoped `Edit` operations
on that criterion's text only — never rewrite the whole file, which would destroy every phase entry
outside the edit window.

The annotation must state, per D-06: Phase 3 delivers only the private-by-default half of
MOMENTUM-06 — streak and shield state has no visibility surface beyond the user's own device this
phase, with no public leaderboard and no sharing of any kind, asserted by a structural no-sharing
test at phase close. The opt-in household half is not built in Phase 3 because Household does not
exist until Phase 4, so there is nothing to opt into yet. Note also that even once Phase 4 ships
the household requirement, the accountability-contact tier specifically stays deferred, since that
is the v2 household requirement already out of scope. Note that the data model already carries a
visibility scope from day one per D-07, so Phase 4 can add a household scope without a data
migration. Date the annotation with the date the task is executed.

Then edit `.planning/phases/03-momentum-recovery/03-VALIDATION.md`'s Per-Task Verification Map,
replacing the placeholder task identifiers and the empty plan column with this phase's real plan
and task numbers, and replacing the two hardcoded example commands with the repository's own
stable entry points — the core package's toolchain-adaptive test script and the app build script's
test subcommand — since the hardcoded ones name a specific simulator model that is not guaranteed
to exist on every machine, the exact mistake the app build script's header comment records the
previous validation document making. Update the file-exists column to reflect the suites that now
exist, tick the Wave 0 requirement checkboxes for the suites that were created, add the threat
reference column entries for the threats each row now maps to, and mark the validation sign-off.

Do not mark any manual-only row as complete: 03-VALIDATION.md's Manual-Only Verifications table
and PROJECT.md's 2026-09-06 decision both defer physical-device and accessibility-size verification
to a single end-of-project batched pass. Phase 3's close-out bar is code-complete plus a green
automated suite, not a manual pass.
  </action>
  <verify>
    <automated>grep -c "MOMENTUM-06" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md's Phase 3 criterion 5 carries a dated annotation naming the private-by-default half as shipped and the household half as scoped-deferred to Phase 4, in the same italicized style as Phase 2's criterion 6.
    - `git diff --stat .planning/ROADMAP.md` shows a small, scoped change — every other phase entry is byte-identical.
    - 03-VALIDATION.md's Per-Task Verification Map has no placeholder task identifier remaining and no empty plan cell.
    - 03-VALIDATION.md's automated commands reference the repository's own test entry points, not a hardcoded simulator model name.
    - No manual-only row is marked complete, and the deferral to the end-of-project batched pass is still stated.
  </acceptance_criteria>
  <done>MOMENTUM-06's partial delivery is a dated, explained annotation on the roadmap rather than a silently unchecked criterion, and the phase's validation contract closes with real task identifiers and runnable commands.</done>
</task>

<task type="auto">
  <name>Task 3: Full-target suite green</name>
  <files>RithamApp/Ritham.xcodeproj/project.pbxproj</files>
  <read_first>.planning/STATE.md's Blockers/Concerns section — in particular the resolved cross-suite registry race and the still-open low-frequency SwiftData container flake affecting three pre-existing un-serialized suites — and .planning/phases/02-core-tracking-adjusted-guidance/02-16-SUMMARY.md for the precedent this task follows.</read_first>
  <action>
Regenerate the Xcode project one final time (`cd RithamApp && xcodegen generate`) so every file
this phase added is in the build, then run the whole test target with no filter, plus the core
package suite.

Both must be green. If a run fails, diagnose before re-running: a failure in one of this phase's
own suites is a real defect to fix in this plan. A crash in one of the three pre-existing
un-serialized container-creating suites named in STATE.md's Blockers section is the known,
separate, pre-existing flake — record the occurrence in the summary and in STATE.md's Blockers
entry with the run count observed, re-run to confirm the phase's own suites are green, and do not
fold a fix for it into this phase. This phase's own new container-creating suites are already
nested under a serialized parent and must not be the source of such a failure; if one of them is,
that is this plan's defect to fix.

Run the full target at least twice consecutively and record both results, following the
consecutive-run discipline plan 02-16 established when verifying a concurrency fix.

Do not perform, and do not create a blocking task for, any physical-device or accessibility-text-size
verification: PROJECT.md's 2026-09-06 decision and 03-VALIDATION.md's Manual-Only Verifications
table both batch those to a single end-of-project pass, and Phase 3's close-out bar is
code-complete plus a green automated suite.

In the summary, list every artifact this phase produced — new domain types, SwiftData records,
store methods, step cases, registrars, views and test suites — so a later drift or review pass does
not flag any of them as unacknowledged.
  </action>
  <verify>
    <automated>Scripts/build-app.sh test</automated>
  </verify>
  <acceptance_criteria>
    - `Scripts/build-app.sh test` (no filter) passes on two consecutive runs, with both results recorded in the summary.
    - `RithamCore/Scripts/test-core.sh` passes.
    - `Scripts/build-app.sh build` succeeds.
    - The summary lists every artifact this phase produced, grouped by kind.
    - Any occurrence of the known pre-existing container flake is recorded in the summary and in STATE.md's Blockers entry, and is not fixed in this phase.
    - No physical-device or accessibility-size task is created or marked outstanding for this phase.
  </acceptance_criteria>
  <done>The full test target and the core package suite are both green on consecutive runs, and the phase's complete artifact inventory is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Momentum/Snapshot surfaces → off-device | MOMENTUM-06 requires zero sharing affordance in this phase; this is the phase-wide structural check |
| registered step → real screen | A placeholder presenter resolves the coverage gate while shipping a non-functional surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-03 | Information Disclosure | Momentum and Snapshot feature directories | medium | mitigate | `noMomentumSurfaceOffersASharingAffordance` scans both directories for share-sheet and pasteboard triggers with comment filtering and a non-zero-file-count assertion, so the gate cannot pass vacuously |
| T-3-18 | Spoofing | step registration | medium | mitigate | `Phase3CoverageTests` asserts each new step's concrete presenter type and rejects placeholder-looking type names, mirroring the shipped Phase 2 coverage suite |
| T-3-19 | Repudiation | ROADMAP criterion 5 | low | mitigate | The partial delivery of MOMENTUM-06 is recorded as a dated, explained annotation rather than left as a silently unchecked criterion, following the Phase 2 criterion 6 precedent |
| T-3-SC | Tampering | both manifests | low | accept | This phase added no package dependency to either manifest; the Package Legitimacy Gate remains not applicable, and a future addition re-opens it |
</threat_model>

<verification>
- `Scripts/build-app.sh test` passes with no filter on two consecutive runs.
- `RithamCore/Scripts/test-core.sh` passes.
- ROADMAP.md's diff is scoped to Phase 3's criterion 5 and its plan list.
- 03-VALIDATION.md carries no placeholder task identifier and no hardcoded simulator model name.
</verification>

<success_criteria>
- MOMENTUM-05's framing and MOMENTUM-06's private-by-default scope are both enforced by passing structural gates.
- All three new step cases resolve to their real screens.
- MOMENTUM-06's deferred half is a dated roadmap annotation, not a silent gap.
- The phase closes code-complete with a green full automated suite, with manual verification correctly deferred to the end-of-project batched pass.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 03-10)

New test suites: `Phase3CoverageTests` (nested under `StepRegistryTouchingSuites`).
Modified planning documents: `.planning/ROADMAP.md` (Phase 3 criterion 5 dated annotation),
`.planning/phases/03-momentum-recovery/03-VALIDATION.md` (real task identifiers, runnable
commands, sign-off).
New types: none. New SwiftData `@Model` records: none. New `HealthDataStore` methods: none.
New `OnboardingStep` cases: none. New SwiftUI views: none.
</artifacts_produced>

<output>
Create `.planning/phases/03-momentum-recovery/03-10-SUMMARY.md` when done
</output>
