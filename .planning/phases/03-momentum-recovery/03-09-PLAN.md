---
phase: 03-momentum-recovery
plan: 09
type: execute
wave: 8
depends_on: ["03-05", "03-06", "03-07"]
files_modified:
  - RithamCore/Sources/RithamCore/Onboarding/OnboardingStep.swift
  - RithamApp/Ritham/MovementSnapshot/Views/MovementSnapshotView.swift
  - RithamApp/Ritham/MovementSnapshot/Views/MovementSnapshotToggleView.swift
  - RithamApp/Ritham/MovementSnapshot/MovementSnapshotRegistration.swift
  - RithamApp/Ritham/Momentum/Phase3StepRegistration.swift
  - RithamApp/Ritham/Settings/SettingsView.swift
  - RithamApp/Ritham/Home/HomeHubView.swift
  - RithamApp/RithamTests/MovementSnapshotViewTests.swift
  - RithamApp/Ritham.xcodeproj/project.pbxproj
autonomous: true
requirements: [MOMENTUM-07]

must_haves:
  truths:
    - "The Daily Movement Snapshot is off by default and is turned on only by an explicit user choice in Settings (MOMENTUM-07, D-09)."
    - "The snapshot's calendar screen displays no streak, no shield, no target, no milestone and no progress element of any kind — structurally, because its whole feature directory references none of those types (MOMENTUM-07, D-09)."
    - "The snapshot's day cells are derived from logged activity, not from qualifying activity, so the snapshot carries no target semantics even implicitly."
    - "The opt-in control is the app's existing two-option chip pattern, not a first-ever native switch with an unverified custom tint (03-UI-SPEC.md Component 9)."
    - "The new step case is registered in the same plan that introduces it, keeping the shipped unregistered-steps gate green."
  artifacts:
    - RithamApp/Ritham/MovementSnapshot/Views/MovementSnapshotView.swift
    - RithamApp/Ritham/MovementSnapshot/Views/MovementSnapshotToggleView.swift
    - RithamApp/Ritham/MovementSnapshot/MovementSnapshotRegistration.swift
    - RithamApp/RithamTests/MovementSnapshotViewTests.swift
  key_links:
    - "The snapshot lives in its own feature directory so the 'carries no streak, shield or target' requirement is enforceable as a directory-scoped import and identifier check rather than as a per-file reading."
    - "Phase3StepRegistration gains this feature's registrar; the app bootstrap is not touched again, matching the aggregate-registrar precedent."
---

<objective>
Build MOMENTUM-07's Daily Movement Snapshot: an opt-in Settings toggle and a plain calendar screen
showing which days had logged activity, with no streak, shield, target or milestone attached to it
in any form.

Purpose: MOMENTUM-07's whole point is a quiet counterpart to Momentum that carries none of its
game mechanics. That is easiest to guarantee structurally — a separate feature directory that
imports and references none of the Momentum types — rather than by reviewing each view for
absence.
Output: one Settings sheet, one calendar step screen, one registrar, two modified screens, one new
test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-momentum-recovery/03-CONTEXT.md
@.planning/phases/03-momentum-recovery/03-UI-SPEC.md
@.planning/phases/03-momentum-recovery/03-05-SUMMARY.md
@.planning/phases/03-momentum-recovery/03-07-SUMMARY.md
@RithamApp/Ritham/Settings/SettingsView.swift
@RithamApp/Ritham/Settings/WorkoutFrequencyView.swift
</context>

<!-- planner-discipline-allow: MomentumSummary -->
<!-- planner-discipline-allow: MomentumProgressBlocks -->
<!-- planner-discipline-allow: ShieldRow -->
<!-- planner-discipline-allow: MilestoneBadgeList -->
<!-- planner-discipline-allow: RithamColor.hot -->

<tasks>

<task type="auto">
  <name>Task 1: Settings opt-in for the Daily Movement Snapshot</name>
  <files>RithamApp/Ritham/MovementSnapshot/Views/MovementSnapshotToggleView.swift, RithamApp/Ritham/Settings/SettingsView.swift, RithamApp/RithamTests/MovementSnapshotViewTests.swift, RithamApp/Ritham.xcodeproj/project.pbxproj</files>
  <read_first>03-UI-SPEC.md Components 8 and 9 in full (especially Component 9's binding decision and its measured-contrast rationale for rejecting a native switch), RithamApp/Ritham/Settings/WorkoutFrequencyView.swift (the sheet-presented fixed-choice preference shape), and RithamApp/Ritham/Settings/SettingsView.swift's existing sheet entries.</read_first>
  <action>
Create a new feature directory `RithamApp/Ritham/MovementSnapshot/Views/` and add
`MovementSnapshotToggleView.swift`: a sheet-presented preference screen inside
`RithamScreen(surface: DecorativeSurface.flat, headline: ...)` containing the toggle label and
helper text from `MomentumCopy.Snapshot`, a two-option single-selection `ChoiceQuestionView` using
that namespace's on and off option labels, the inline save-error text reusing
`OnboardingCopy.Errors.savingFailed`, and a primary CTA that dismisses. It persists through
`HealthDataStore.saveMovementSnapshotOptIn` in an `onChange` handler, with the initial selection
supplied through the initializer so the first render never re-persists the value it was handed —
the same shape the shipped workout-frequency screen uses.

Write the binding decision from 03-UI-SPEC.md Component 9 into the file's header comment: this
opt-in deliberately uses the app's existing two-option chip control rather than a native switch,
because the codebase has zero precedent for that control, its default on-state track colour is a
system accent, and tinting it with this app's accent would produce a knob-on-track pair measured
at roughly 2.8 to 1 — below even the non-text contrast floor. The chip control is already
contrast-verified and is the sanctioned binary-choice control app-wide.

Add one entry to `SettingsView` presenting this sheet, following the exact shape of its existing
diet-plan and workout-frequency entries: a dedicated presentation-state property, a secondary CTA
row, a sheet presentation alongside the existing ones, and a private helper loading the current
opt-in fresh at presentation time.

Create `RithamApp/RithamTests/MovementSnapshotViewTests.swift` as
`@Suite("MovementSnapshotViewTests", .serialized)` nested inside
`extension MomentumContainerTouchingSuites { ... }`, `@MainActor`, using the same in-memory
container helper the other store-backed suites use. Cover:
- The opt-in defaults to off on an empty store.
- Selecting the on option persists true; selecting off persists false; both reload correctly.
- The screen's label, helper and two option strings equal the `MomentumCopy.Snapshot` constants.
- The Settings row presents the sheet with the currently persisted value.

Regenerate the Xcode project (`cd RithamApp && xcodegen generate`) and commit the regenerated
`project.pbxproj` with this task.
  </action>
  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/MomentumContainerTouchingSuites/MovementSnapshotViewTests</automated>
  </verify>
  <acceptance_criteria>
    - `Scripts/build-app.sh test -only-testing:RithamTests/MomentumContainerTouchingSuites/MovementSnapshotViewTests` passes.
    - `grep -rh "Toggle(" RithamApp/Ritham/MovementSnapshot/ | grep -v '^\s*//' | wc -l` returns 0 — the app's zero-native-switch precedent is preserved.
    - A test asserts the opt-in default is off on an empty store.
    - `grep -c "OnboardingCopy.Errors.savingFailed" RithamApp/Ritham/MovementSnapshot/Views/MovementSnapshotToggleView.swift` returns 1, with no second error string declared.
    - `Scripts/build-app.sh test -only-testing:RithamTests/SettingsPhase2Tests` still passes.
  </acceptance_criteria>
  <done>The snapshot is opt-in, off by default, and its control is the app's existing contrast-verified chip pattern rather than a first-ever native switch.</done>
</task>

<task type="auto">
  <name>Task 2: Plain calendar screen, step case, and feature registrar</name>
  <files>RithamCore/Sources/RithamCore/Onboarding/OnboardingStep.swift, RithamApp/Ritham/MovementSnapshot/Views/MovementSnapshotView.swift, RithamApp/Ritham/MovementSnapshot/MovementSnapshotRegistration.swift, RithamApp/Ritham/Momentum/Phase3StepRegistration.swift, RithamApp/RithamTests/MovementSnapshotViewTests.swift, RithamApp/Ritham.xcodeproj/project.pbxproj</files>
  <read_first>03-UI-SPEC.md Component 8 and its Decorative Surface Assignments row for this screen, 03-CONTEXT.md D-09, the MovementSnapshotDay type and the store's snapshot-days query from plan 03-05, RithamApp/Ritham/Momentum/MomentumRegistration.swift and Phase3StepRegistration.swift as written by plan 03-06.</read_first>
  <action>
Add exactly one new case, `movementSnapshot`, to `OnboardingStep`, appended after the sleep
check-in case plan 03-08 added. Do not reorder or rename any existing case.

Create `RithamApp/Ritham/MovementSnapshot/Views/MovementSnapshotView.swift` conforming to `View`
and `OnboardingStepPresenting` with `static let step: OnboardingStep = .movementSnapshot`, inside
`RithamScreen(surface: DecorativeSurface.flat, headline: ...)` per the UI-SPEC surface table.

It renders a plain month grid of day cells for the displayed month, loading
`HealthDataStore.movementSnapshotDays(in:)` for that month's range in an `onAppear`. A day with
logged activity is marked; a day without is unmarked. Distinguish the two within the existing
paper-and-ink field only — the marked state must not use the app's accent fill, because in this
phase's visual vocabulary that accent means Momentum progress and this screen must carry none of
that association (03-UI-SPEC.md Component 8). Use a shape or opacity difference, so the
distinction never rests on colour alone.

Show the catalog's no-entries empty state when the loaded range has no marked day. Offer plain
month-back and month-forward controls at the standard tap-target size, and nothing else.

This screen must render no streak count, no shield row, no weekly target, no progress strip and no
milestone list, and its file must not reference any of those types. That is why the whole feature
lives in its own directory rather than under the Momentum one: the constraint becomes a
directory-scoped identifier check instead of a per-file reading. Record that rationale in the file
header, along with the note that this is a deliberate departure from 03-RESEARCH.md's suggested
file placement under the Settings directory, made for exactly this structural reason.

Create `RithamApp/Ritham/MovementSnapshot/MovementSnapshotRegistration.swift` as a `@MainActor
enum` whose `registerAll()` registers `MovementSnapshotView.self`, copying the shape of the
existing feature registrars. Add exactly one line to `Phase3StepRegistration.registerAll()` calling
it. Do not touch the app bootstrap — this phase's single bootstrap line already exists.

Extend `MovementSnapshotViewTests` with a second suite in the same file, nested inside
`extension StepRegistryTouchingSuites { ... }` and marked serialized, covering:
- The new step resolves to `MovementSnapshotView` after bootstrap.
- The registry reports no unregistered steps after bootstrap.
And in the container-backed suite:
- A month whose days include a stored cardio session marks exactly that day.
- A month whose days include a stored lift session marks exactly that day.
- A month with a stored session that does not clear the qualification bar still marks that day —
  the snapshot reflects logged activity, never qualifying activity.
- A month with no stored session renders the catalog's no-entries empty state.
- A test named `theSnapshotScreenReferencesNoMomentumType` implemented as a comment-filtered
  identifier check over the feature directory, asserting zero references to the Momentum summary
  type, the Momentum state record, the progress-block component, the shield row and the milestone
  list.
  </action>
  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/MomentumContainerTouchingSuites/MovementSnapshotViewTests</automated>
  </verify>
  <acceptance_criteria>
    - `Scripts/build-app.sh test -only-testing:RithamTests/MomentumContainerTouchingSuites/MovementSnapshotViewTests` passes.
    - `Scripts/build-app.sh test -only-testing:RithamTests/StepRegistryTouchingSuites/PhaseCoverageTests` passes — the unregistered-steps gate is green with the third new case.
    - Named test `theSnapshotScreenReferencesNoMomentumType` exists and passes.
    - `grep -rhE "MomentumSummary|MomentumStateRecord|MomentumProgressBlocks|ShieldRow|MilestoneBadgeList|currentStreak|shieldCount|weeklyTarget" RithamApp/Ritham/MovementSnapshot/ | grep -v '^\s*//' | wc -l` returns 0.
    - `grep -rh "RithamColor.hot" RithamApp/Ritham/MovementSnapshot/Views/MovementSnapshotView.swift | grep -v '^\s*//' | wc -l` returns 0 — the day grid uses no accent fill.
    - `Phase3StepRegistration.registerAll()` gained exactly one line and `StepBootstrap.swift` is unchanged by this plan.
  </acceptance_criteria>
  <done>The snapshot's calendar screen exists as a registered step, marks days from logged activity with no accent fill, and its whole feature directory references no Momentum type.</done>
</task>

<task type="auto">
  <name>Task 3: Opt-in-gated hub entry</name>
  <files>RithamApp/Ritham/Home/HomeHubView.swift, RithamApp/RithamTests/MovementSnapshotViewTests.swift, RithamApp/Ritham.xcodeproj/project.pbxproj</files>
  <read_first>RithamApp/Ritham/Home/HomeHubView.swift as modified by plans 03-07 and 03-08, RithamApp/RithamTests/HomeHubTests.swift, and 03-CONTEXT.md D-09.</read_first>
  <action>
Add one secondary CTA to `HomeHubView` routing to the new snapshot step through the flow's open
method, rendered only when the stored opt-in is on. Load the opt-in in the hub's existing
`onAppear` alongside the Momentum summary load; do not add a second appearance handler.

Place this CTA below the existing tracking and Momentum entries and visually apart from the
Momentum summary section, so the snapshot never reads as part of the Momentum block. Write the
D-09 rationale into the file next to it: the snapshot is the deliberately quiet counterpart to
Momentum and must carry no streak, shield or target association, including by adjacency.

When the opt-in is off, render nothing at all for this feature on the hub — no disabled row, no
greyed entry, no "turn this on" prompt. An off opt-in is indistinguishable from the feature not
existing.

Extend `MovementSnapshotViewTests`'s container-backed suite with:
- The hub's routing list includes the snapshot step exactly when the opt-in is on.
- The hub's routing list omits it entirely when the opt-in is off, with no placeholder entry of any
  kind.
- A test named `theSnapshotEntryIsNotAdjacentToTheMomentumSummary` asserting, at the hub's model or
  section-ordering level, that the snapshot entry is not rendered inside the Momentum summary
  section.
  </action>
  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/HomeHubTests</automated>
  </verify>
  <acceptance_criteria>
    - `Scripts/build-app.sh test -only-testing:RithamTests/HomeHubTests` passes with no regression.
    - `Scripts/build-app.sh test -only-testing:RithamTests/MomentumContainerTouchingSuites/MovementSnapshotViewTests` passes with at least 12 tests across its two suites.
    - Named test `theSnapshotEntryIsNotAdjacentToTheMomentumSummary` exists and passes.
    - A test asserts the hub renders no snapshot-related element at all when the opt-in is off.
    - `grep -c "onAppear" RithamApp/Ritham/Home/HomeHubView.swift` is unchanged from before this task — no second appearance handler was added.
  </acceptance_criteria>
  <done>The snapshot is reachable from the hub only when the user has opted in, is rendered apart from the Momentum block, and is entirely absent when opted out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot feature → Momentum state | MOMENTUM-07 and D-09 require the snapshot to carry no streak, shield or target — enforced as a directory-scoped structural boundary |
| Settings opt-in → feature visibility | An opt-out must leave no residual affordance anywhere |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-15 | Tampering | snapshot feature boundary | medium | mitigate | The whole feature lives in its own directory whose files reference no Momentum type, pinned by `theSnapshotScreenReferencesNoMomentumType` and by a directory-wide comment-filtered identifier gate; plan 03-05 already removed the possibility of a foreign key by deriving day cells rather than persisting entries |
| T-3-16 | Information Disclosure | opt-out residue | low | mitigate | An off opt-in renders nothing for this feature on the hub — no disabled row, no prompt — pinned by a test asserting complete absence |
| T-3-17 | Tampering | opt-in control contrast | low | mitigate | The app's existing contrast-verified chip control is used instead of a first-ever native switch with an unverified custom tint, per the UI-SPEC's measured rationale; pinned by a zero-native-switch grep gate over the feature directory |
| T-3-SC | Tampering | `RithamApp/project.yml` | low | accept | This plan adds no package dependency; the Package Legitimacy Gate is not applicable this phase |
</threat_model>

<verification>
- `Scripts/build-app.sh build` succeeds.
- `-only-testing:RithamTests/MomentumContainerTouchingSuites/MovementSnapshotViewTests`, `-only-testing:RithamTests/MomentumContainerTouchingSuites/MovementSnapshotTests`, `-only-testing:RithamTests/HomeHubTests`, `-only-testing:RithamTests/SettingsPhase2Tests` and `-only-testing:RithamTests/StepRegistryTouchingSuites/PhaseCoverageTests` all pass.
- `RithamCore/Scripts/test-core.sh` still passes after the third step-enum case is added.
- `StepBootstrap.swift` is unchanged by this plan.
- The regenerated `project.pbxproj` is committed with this plan.
</verification>

<success_criteria>
- MOMENTUM-07's snapshot exists as a separate, opt-in surface with its own plain calendar view.
- It carries no streak, shield, target or milestone — structurally, by directory-scoped reference absence.
- Its opt-in defaults to off and leaves no residue when off.
- Its step case is registered in the same plan that introduces it, keeping the shipped coverage gate green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 03-09)

New `OnboardingStep` case: `movementSnapshot`.
New SwiftUI views: `MovementSnapshotView`, `MovementSnapshotToggleView`.
New registrars: `MovementSnapshotRegistration`.
Modified: `Phase3StepRegistration.registerAll()` gains one call; `SettingsView` gains one
preference entry and sheet; `HomeHubView` gains one opt-in-gated routing CTA.
New test suites: `MovementSnapshotViewTests` (a container-backed suite plus a registry-touching
suite nested under `StepRegistryTouchingSuites`).
New SwiftData `@Model` records: none. New `HealthDataStore` methods: none.
</artifacts_produced>

<output>
Create `.planning/phases/03-momentum-recovery/03-09-SUMMARY.md` when done
</output>
