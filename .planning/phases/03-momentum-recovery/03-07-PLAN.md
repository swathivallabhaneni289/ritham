---
phase: 03-momentum-recovery
plan: 07
type: execute
wave: 6
depends_on: ["03-05", "03-06"]
files_modified:
  - RithamApp/Ritham/Momentum/Views/MomentumTargetView.swift
  - RithamApp/Ritham/Settings/SettingsView.swift
  - RithamApp/Ritham/Home/HomeHubView.swift
  - RithamApp/RithamTests/MomentumTargetPickerTests.swift
  - RithamApp/RithamTests/HomeHubTests.swift
  - RithamApp/Ritham.xcodeproj/project.pbxproj
autonomous: true
requirements: [MOMENTUM-01, MOMENTUM-03, MOMENTUM-06]

must_haves:
  truths:
    - "A user can change the weekly Momentum target to any of 2, 3, 4 or 5 from Settings, using the same fixed-choice chip picker the shipped workout-frequency preference uses, and no free-text or arbitrary number is offered (MOMENTUM-01)."
    - "An out-of-range target can never be persisted: the screen offers only supported values and the store rejects anything else without persisting (MOMENTUM-01, T-3-01)."
    - "The interim hub shows this week's progress, the current streak and the shield count, reading the same standalone summary Phase 4's home screen will reuse (D-08)."
    - "The hub's Momentum section renders in the scrollable content area, never inside the hub's existing decorative header region (03-UI-SPEC.md Decorative Surface Assignments)."
    - "No share, export or invite affordance appears on the hub's Momentum section (MOMENTUM-06, D-06)."
  artifacts:
    - RithamApp/Ritham/Momentum/Views/MomentumTargetView.swift
    - RithamApp/RithamTests/MomentumTargetPickerTests.swift
  key_links:
    - "MomentumTargetOption's value list and HealthDataStore.supportedMomentumTargets must be pinned to each other by a test, exactly as the shipped weekly-frequency option type already is — two separately declared lists that silently drift is a known trap in this codebase."
    - "HomeHubView reads MomentumSummaryReader, the same standalone unit MomentumView reads, so Phase 4's CROSSGEN-01 home screen can take today's target and current streak from the same place without rework."
---

<objective>
Make the weekly Momentum target user-adjustable from Settings and surface the Momentum summary on
Phase 2's interim hub.

Purpose: MOMENTUM-01's adjustable target has no user-reachable control until this plan, and D-08
requires the hub to show today's progress and the current streak through a standalone read that
Phase 4 can reuse. Both are direct reuses of shipped shapes — the workout-frequency sheet and the
hub's existing CTA list — so this plan adds no new interaction pattern.
Output: one new Settings-sheet screen, two modified screens, one new test suite and one extended
one.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-momentum-recovery/03-CONTEXT.md
@.planning/phases/03-momentum-recovery/03-UI-SPEC.md
@.planning/phases/03-momentum-recovery/03-PATTERNS.md
@.planning/phases/03-momentum-recovery/03-05-SUMMARY.md
@.planning/phases/03-momentum-recovery/03-06-SUMMARY.md
@RithamApp/Ritham/Settings/WorkoutFrequencyView.swift
@RithamApp/Ritham/Home/HomeHubView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Weekly Momentum target picker</name>
  <files>RithamApp/Ritham/Momentum/Views/MomentumTargetView.swift, RithamApp/RithamTests/MomentumTargetPickerTests.swift, RithamApp/Ritham.xcodeproj/project.pbxproj</files>
  <read_first>RithamApp/Ritham/Settings/WorkoutFrequencyView.swift in full — it is the direct template, including its option wrapper type, its init-supplied initial selection, its onChange persistence, and its inline save-error text — plus 03-UI-SPEC.md Component 10 and its Copywriting Contract rows for the target prompt and helper.</read_first>
  <action>
Create `RithamApp/Ritham/Momentum/Views/MomentumTargetView.swift` as a near-verbatim adaptation of
the shipped workout-frequency screen, with these substitutions and nothing else changed in shape:
- A `MomentumTargetOption: Hashable, Identifiable` wrapper over `Int`, with a static list built
  from the four supported values. Copy the shipped option type's doc-comment rationale for why a
  wrapper exists rather than a retroactive conformance on the standard-library integer type, and
  why the list is declared separately rather than derived from the main-actor-isolated store
  constant at declaration time.
- `RithamScreen(surface: DecorativeSurface.flat, headline: ...)`, per the UI-SPEC surface table.
- The prompt and helper strings from `MomentumCopy.Target`.
- A single-selection `ChoiceQuestionView` over the four options.
- The inline save-error text reusing `OnboardingCopy.Errors.savingFailed` verbatim.
- A primary CTA that dismisses.
- An `onChange` handler persisting through `HealthDataStore.saveMomentumTarget`, setting the error
  flag on a throw, exactly as the shipped screen's own persist helper does.
- An initializer taking the already-loaded current target, so the first render never fires the
  change handler and re-persists the value it was just handed.

Record this as an explicit planning decision in the file's header comment, because it deviates
from one line of the orchestrator's own framing: this screen is a Settings-presented sheet with no
step case, not a registered step. The reasons are concrete — the Settings screen is itself a
sheet, so routing from it through the flow's open method would push a screen onto the root
navigation stack behind the presented sheet; every shipped Settings sub-screen in this codebase is
a sheet for exactly that reason; 03-UI-SPEC.md Component 10 says this screen follows the shipped
workout-frequency screen exactly, which is a sheet; and 03-RESEARCH.md's Pattern 4 names three
full-screen surfaces for step cases and does not include this one.

Create `RithamApp/RithamTests/MomentumTargetPickerTests.swift` as
`@Suite("MomentumTargetPickerTests", .serialized)` nested inside
`extension MomentumContainerTouchingSuites { ... }`, `@MainActor`, using the same in-memory
container helper the other store-backed suites use. Name the suite distinctly from the RithamCore
`MomentumTargetTests` suite so a filtered run selects exactly one of them. Cover:
- A test named `pickerOptionsMatchTheStoreSupportedTargets` asserting the option list's values, as
  a set, equal `HealthDataStore.supportedMomentumTargets` — the same drift guard the shipped
  weekly-frequency suite already carries for its own two lists.
- Selecting each supported value persists it and reloads identically.
- The screen's prompt and helper strings equal the `MomentumCopy.Target` constants.
- Attempting to persist an unsupported value through the store throws and leaves the previously
  stored value in place, asserted at the store level.

Regenerate the Xcode project (`cd RithamApp && xcodegen generate`) and commit the regenerated
`project.pbxproj` with this task.
  </action>
  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/MomentumContainerTouchingSuites/MomentumTargetPickerTests</automated>
  </verify>
  <acceptance_criteria>
    - `Scripts/build-app.sh test -only-testing:RithamTests/MomentumContainerTouchingSuites/MomentumTargetPickerTests` passes.
    - Named test `pickerOptionsMatchTheStoreSupportedTargets` exists and passes.
    - `grep -rhE "TextField|Stepper|Slider" RithamApp/Ritham/Momentum/Views/MomentumTargetView.swift | grep -v '^\s*//' | wc -l` returns 0 — fixed choice only, never free entry.
    - `grep -c "OnboardingCopy.Errors.savingFailed" RithamApp/Ritham/Momentum/Views/MomentumTargetView.swift` returns 1, and no second error string is declared in the file.
    - `grep -c "DecorativeSurface.flat" RithamApp/Ritham/Momentum/Views/MomentumTargetView.swift` returns 1.
    - The file's header comment records the sheet-not-step decision with its four stated reasons.
  </acceptance_criteria>
  <done>The weekly target is adjustable through a fixed-choice sheet that mirrors the shipped frequency preference exactly, with its option list pinned to the store's supported set by a test.</done>
</task>

<task type="auto">
  <name>Task 2: Settings entry point for the Momentum target</name>
  <files>RithamApp/Ritham/Settings/SettingsView.swift, RithamApp/RithamTests/MomentumTargetPickerTests.swift, RithamApp/Ritham.xcodeproj/project.pbxproj</files>
  <read_first>RithamApp/Ritham/Settings/SettingsView.swift in full — especially its existing workout-frequency entry, its sheet presentation state, and its currentWeeklyFrequency helper that loads fresh at presentation time — plus RithamApp/RithamTests/SettingsPhase2Tests.swift for the existing Settings assertion style.</read_first>
  <action>
Add one new entry to `SettingsView`, alongside the existing diet-plan, health-profile, always-free
and workout-frequency rows: a secondary CTA labelled for the weekly Momentum target that presents
`MomentumTargetView` as a sheet.

Follow the shipped workout-frequency entry's exact shape: a dedicated presentation-state property,
a sheet presentation at the bottom of the body alongside the existing ones, and a private helper
that loads the current target from the store fresh at presentation time rather than caching it in
the view's own state, so a reopen always reflects whatever was persisted when the sheet was last
dismissed. The store's loader already supplies the stated default when nothing is stored yet.

Place the new row adjacent to the workout-frequency row, since both are weekly-cadence
preferences, and keep the screening-answers section last and unchanged.

Add no Momentum state display to Settings — the target picker is a preference entry, not a
Momentum surface, and Settings must not become a second place the streak or shield count renders.

Extend `MomentumTargetPickerTests` with:
- A test asserting the Settings row's label equals the intended catalog or literal string, matching
  how the existing Settings tests assert their own row labels.
- A test asserting the target loaded for sheet presentation reflects the most recently persisted
  value rather than a cached one, by persisting a new value and re-reading through the same helper
  path.
  </action>
  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/SettingsPhase2Tests</automated>
  </verify>
  <acceptance_criteria>
    - `Scripts/build-app.sh test -only-testing:RithamTests/SettingsPhase2Tests` passes with no regression.
    - `Scripts/build-app.sh test -only-testing:RithamTests/MomentumContainerTouchingSuites/MomentumTargetPickerTests` passes.
    - `grep -c "MomentumTargetView" RithamApp/Ritham/Settings/SettingsView.swift` returns at least 1.
    - `grep -rhE "currentStreak|shieldCount|MomentumSummary" RithamApp/Ritham/Settings/SettingsView.swift | grep -v '^\s*//' | wc -l` returns 0 — Settings shows the preference, never Momentum state.
    - The new sheet uses the same fresh-load-at-presentation-time pattern as the existing frequency entry.
  </acceptance_criteria>
  <done>The weekly Momentum target is reachable in one tap from Settings, presented as a sheet with a freshly loaded current value, and Settings displays no Momentum state itself.</done>
</task>

<task type="auto">
  <name>Task 3: Momentum summary section on the interim hub</name>
  <files>RithamApp/Ritham/Home/HomeHubView.swift, RithamApp/RithamTests/HomeHubTests.swift, RithamApp/Ritham.xcodeproj/project.pbxproj</files>
  <read_first>RithamApp/Ritham/Home/HomeHubView.swift in full (its bounded-header surface, its CTA list inside a single vertical stack, its two sheet presentations, and its header rationale about routing only through the flow's open method), RithamApp/RithamTests/HomeHubTests.swift, 03-CONTEXT.md D-08, and 03-UI-SPEC.md's Decorative Surface Assignments row for the hub.</read_first>
  <action>
Add a Momentum summary section to `HomeHubView`, inside the existing vertical stack in the
scrollable content area, above the existing CTA list. The hub's own decorative surface stays
exactly as it is — do not change it, and do not place any Momentum element inside the header
region. State that constraint in a comment next to the new section: the hub's header already
renders a circular ornament, and the progress display is deliberately a block strip in the content
area precisely so two circular motifs never share one viewport.

The section loads a `MomentumSummary` through `MomentumSummaryReader` in an `onAppear`, holding it
in view state, and renders:
- `MomentumProgressBlocks` for this week.
- The streak line from the copy catalog, with monospaced numerals.
- `ShieldRow` with the summary's shield count.
- The no-sessions empty state from the catalog when the user has logged nothing yet.

Add one primary CTA routing to the Momentum detail screen through the flow's open method with the
new step case, matching the shape of the hub's existing CTAs exactly — no new navigation container
and no second navigation path.

Do not add a sleep-check-in state indicator, a badge, a dot, or any "you have not checked in"
prompt of any kind to this screen. RECOVERY-01 invariant 3 requires a skipped check-in to be
indistinguishable, app-wide, from a day the prompt was never shown. Write that constraint into the
file as a comment so a later edit does not reintroduce it as a helpful nudge.

Add no share, export, invite or copy-link affordance to this section.

Extend `HomeHubTests` with:
- A test asserting the hub's routing list now includes the Momentum step, alongside its existing
  Phase 2 destinations.
- A test asserting the summary the hub renders comes from the same reader type the detail screen
  uses, so D-08's single-read requirement holds.
- A test named `theHubShowsNoSleepCheckInStateIndicator` asserting the hub's rendered model exposes
  no member derived from sleep check-in state.
- A test asserting the hub's empty state string equals the catalog's no-sessions pair.
  </action>
  <verify>
    <automated>Scripts/build-app.sh test -only-testing:RithamTests/HomeHubTests</automated>
  </verify>
  <acceptance_criteria>
    - `Scripts/build-app.sh test -only-testing:RithamTests/HomeHubTests` passes with no regression in its existing assertions.
    - Named test `theHubShowsNoSleepCheckInStateIndicator` exists and passes.
    - `grep -c "DecorativeSurface.boundedHeaderOnly" RithamApp/Ritham/Home/HomeHubView.swift` returns 1 — the hub's surface is unchanged.
    - `grep -rhiE "sleep" RithamApp/Ritham/Home/HomeHubView.swift | grep -v '^\s*//' | wc -l` returns 0.
    - `grep -rhE "ShareLink|UIActivityViewController" RithamApp/Ritham/Home/HomeHubView.swift | grep -v '^\s*//' | wc -l` returns 0.
    - `grep -c "NavigationStack" RithamApp/Ritham/Home/HomeHubView.swift` returns 0 — routing still goes through the single shared container only.
  </acceptance_criteria>
  <done>The interim hub shows this week's progress, the streak and the shield count from the same standalone reader the detail screen uses, routes to the detail screen through the existing navigation contract, and shows no sleep-check-in state and no sharing affordance.</done>
</task>

</tasks>

<!-- planner-discipline-allow: ShareLink -->
<!-- planner-discipline-allow: UIActivityViewController -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings input → persisted weekly target | The only user-facing write path for a value the reconciliation fold depends on |
| hub rendering → sleep check-in state | RECOVERY-01 invariant 3 forbids any app-wide visual consequence of a skipped check-in |
| Momentum surfaces → off-device | MOMENTUM-06 requires zero sharing affordance in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Tampering | `MomentumTargetView` | medium | mitigate | The screen offers only the four supported values through a fixed-choice chip picker with no free-entry control, and the store still guards and throws behind it; the option list is pinned to the store's set by `pickerOptionsMatchTheStoreSupportedTargets` |
| T-3-03 | Information Disclosure | hub Momentum section | medium | mitigate | No share, export or invite affordance is added; pinned by a comment-filtered grep gate on the hub file and again by plan 03-10's directory-wide close-out gate |
| T-3-12 | Information Disclosure | sleep state leaking into the hub | medium | mitigate | The hub reads a summary type that structurally carries no sleep member (plan 03-05), and a grep gate plus `theHubShowsNoSleepCheckInStateIndicator` pin that no check-in indicator is introduced here |
| T-3-SC | Tampering | `RithamApp/project.yml` | low | accept | This plan adds no package dependency; the Package Legitimacy Gate is not applicable this phase |
</threat_model>

<verification>
- `Scripts/build-app.sh build` succeeds.
- `-only-testing:RithamTests/HomeHubTests`, `-only-testing:RithamTests/SettingsPhase2Tests` and `-only-testing:RithamTests/MomentumContainerTouchingSuites/MomentumTargetPickerTests` all pass.
- `-only-testing:RithamTests/StepRegistryTouchingSuites/PhaseCoverageTests` still passes.
- The regenerated `project.pbxproj` is committed with this plan.
</verification>

<success_criteria>
- MOMENTUM-01's target is user-adjustable across all four supported values from Settings, with the picker pinned to the store's supported set.
- MOMENTUM-03's Monday-anchored week is reflected in the hub's this-week progress display via the standalone reader.
- MOMENTUM-06 holds: no sharing affordance appears on the hub's Momentum section.
- D-08's requirement is met — the hub and the detail screen read the same standalone summary unit, ready for Phase 4's home screen to reuse.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (plan 03-07)

New SwiftUI views: `MomentumTargetView`.
New types: `MomentumTargetOption`.
Modified views: `SettingsView` (one new preference entry and sheet), `HomeHubView` (one Momentum
summary section and one routing CTA).
New test suites: `MomentumTargetPickerTests`.
New SwiftData `@Model` records: none. New `HealthDataStore` methods: none.
New `OnboardingStep` cases: none — the target picker is a Settings sheet by explicit decision,
recorded in its own file header.
</artifacts_produced>

<output>
Create `.planning/phases/03-momentum-recovery/03-07-SUMMARY.md` when done
</output>
